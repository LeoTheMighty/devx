# Retro — Story Graph (sgr101–sgr107) — 2026-08-06

Native retro (`/devx` Stage: Retro, D-3), run as `sgrret`. Evidence
reconstructed from the seven spec status logs, `gh pr view` metadata,
DEBUG.md / MANUAL.md / INTERVIEW.md rows filed during the epic, and the
`/devx-learn` candidates section written 2026-08-05 — disk, not memory.
Companion LEARN section: `LEARN.md § epic-story-graph (2026-08-06)`.

Scope: the seven merged phases of workstream `story-graph` (plan-62bcd1).
The epic also forced or surfaced nine debug specs and one dev-spec re-cut;
those are dispositioned below.

## Shipped items

| PR | Item | Phase | Merged (UTC) | Diff | How it shipped |
|---|---|---|---|---|---|
| [#110](https://github.com/LeoTheMighty/devx/pull/110) | sgr101 | 1 — parser completion + hardening | 08-02 20:42 | +589/−52 | interactive; single-pass review (fan-out disabled) |
| [#111](https://github.com/LeoTheMighty/devx/pull/111) | sgr102 | 2 — graph model | 08-02 21:23 | +1,952/−1 | interactive; single-pass + live-repo leg (3 extra defects) |
| [#112](https://github.com/LeoTheMighty/devx/pull/112) | sgr103 | 3 — renderer + `devx graph` CLI | 08-03 16:08 | +2,732/−12 | interactive; inherited red main, fixed forward (discipline-test widening) |
| [#114](https://github.com/LeoTheMighty/devx/pull/114) | sgr104 | 4 — regen hooks (claim + RED emission) | 08-04 16:52 | +1,800/−28 | interactive; the epic's only true 3-agent parallel review |
| [#117](https://github.com/LeoTheMighty/devx/pull/117) | sgr107 | 7 — downstream portability (E-7) | 08-05 18:23 | +162/−51 | interactive; concurrent with sgr105 + sgr106 |
| [#120](https://github.com/LeoTheMighty/devx/pull/120) | sgr106 | 6 — backfill (E-6) | 08-05 19:29 | +2,670/−58 | interactive; concurrent; single-pass + attended dry-run leg |
| [#118](https://github.com/LeoTheMighty/devx/pull/118) | sgr105 | 5 — mark-done + Phase-8 rewrite (E-5) | 08-05 20:04 | +2,480/−64 | interactive; CONFLICTING-PR CI starvation → Q#16 → claim released + re-claimed → resolved in resume session |

## Outcome

- **The diamond in the plan was actually exercised.** sgr104/105/106/107
  were declared `Parallel-safe` off sgr103, and on 2026-08-05 three
  concurrent sessions ran them for real (claims at 11:13, 11:25, 11:25).
  The mlc-era primitives held silently — spec locks, claim
  rebase-retry, additive-conflict rebases (sgr106 rebased twice under
  peer merges), and `gh pr merge`-from-worktree verification. Concurrency's
  cost surfaced entirely in the environment, not in correctness: peer
  vitest load reddened every local full gate, and peers merging under the
  slowest PR (#118) made it CONFLICTING, which starved it of CI (below).
- **Wall-clock: planning ~5 h (2026-08-02 09:00 → 13:57 local, four gates
  PASS same morning), execute ~3 calendar days** (first claim 08-02
  14:14 local, last merge 08-05 14:05 local). ~12.4K added lines across
  seven PRs.
- **Tests: ~2,945 (sgr101's gate) → ~3,197 (`vitest list` at this
  retro).** Not all of the +~250 is this epic's — the retro-listener epic
  and 28b267 merged in the same window — but the epic's own suites are
  identifiable: parser +34, graph-model 46, renderer/CLI + backfill +
  mark-done/status-log suites, and evals E-1…E-7 all GREEN at their
  phase's merge.
- **Review-pattern stats: ~51 unique actionable findings, all fixed
  in-place, every re-review clean** (sgr101 3, sgr102 4 + 3 live-run,
  sgr103 8, sgr104 9, sgr105 7, sgr106 14, sgr107 3). Only sgr104 ran the true 3-agent
  parallel shape (~25 raw → 9 unique). Session policy forbade subagent
  fan-out in five of seven stories, and two compensation shapes emerged:
  **sequential 3-lens** (sgr105 — 7 findings on ~800 lines) and
  **single-pass + attended real-repo leg** (sgr106 — 14 findings, the
  dry run catching the two most serious). The live-repo leg was the
  epic's best reviewer overall: sgr102's board build found 3 defects
  fixtures missed, sgr103's own-board render caught a bug that would have
  shipped a blank diagram in that very PR, sgr106's dry run caught 3
  derived edges that contradicted explicit `Parallel-safe` annotations.
- **G-1 evidence banked for outcome scoring:** model build 134 ms
  (sgr102), full render 0.26 s (sgr103), `devx graph --check` 0.275 s on
  the 194-node board (this retro, quiet machine) — ~7× under the 2 s
  budget. The one >90× excursion (candidate C4) occurred under two peer
  full-suite runs and is attributed to machine load, not the renderer.
- **The local full-suite gate degraded to noise this epic.** Red at
  baseline in 4 of 7 stories (spawn-heavy files under peer load; 24
  failures typical), `npm test` even exited 0 over a 24-red run
  (sgr105 — the summary line, not the exit code, caught it). Every
  affected story burned a bisect/isolation pass to disprove a
  regression; touched-surface subsets + remote CI were the actual gates.
  Filed: debug-620337, debug-5c8b21, debug-ecdcda (joining debug-7c1e93).
- **The #118 silent-CI saga validated the safe default and exposed a
  probe blind spot.** A CONFLICTING PR gets no merge ref, so
  `pull_request` workflows never fire; `await-remote-ci` read that as
  `empty` for 41 probes / ~50 min and escalated to INTERVIEW Q#16 —
  while `gh pr view --json mergeable` had the answer the whole time.
  Phase 7's "silent CI is a config bug, not a green light" rule held:
  the cost was latency and a human question, never a bad merge. Root
  cause proven in the resume session; probe fix filed as debug-c94f14.
- **Phase 8's after-merge prose tail — the mlcret-identified "last major
  non-CLI-driven phase" — now has its primitive** (`devx devx-helper
  mark-done`, sgr105): one locked call returning explicit pathspecs,
  killing the `git add -A` peer-sweep class structurally. dev-b931a1's
  staging-scope half is thereby shipped; its remainder (spec-lock
  release, clock-stamped merge line, post-merge dist rebuild) needs a
  re-cut — cross-referenced on that spec's status log in this PR.

## Findings

Tagged rows live in `LEARN.md § epic-story-graph (2026-08-06)` (E1–E10),
including the dispositions of the 2026-08-05 `/devx-learn` candidates
C1–C4 (C1 promoted into the new cross-epic honest-RED row; C2/C3
confirmed as-filed single observations; C4 resolved-in-part by this
retro's quiet-machine measurement).

Cross-epic promotions/updates made by this retro:

1. **New promotion — "A RED artifact must fail for the STATED reason."**
   3 concordant wrong-reason-RED shapes across 2 epics: infra breakage
   (mlc101), transient (sgr105), fixture self-contradiction (sgr107).
2. **Update — "First real run against the live repo":** +3 sgr instances
   (sgr102, sgr103, sgr106).
3. **Update — "3-agent parallel adversarial review":** environmental
   fallback now measured across 5 instances; two compensation shapes
   validated (sequential 3-lens; empirical real-repo leg).
4. **Update — "Externalize behavior-as-CLI-primitive":** sgr105 closes
   the Phase 8 after-merge prose tail.

## Workstream close

- Outcome armed on plan-62bcd1: measure-by **2026-09-20** (G-3's date;
  G-1 due 08-16 and G-2 due 08-23 score at the same sitting). G-2's
  attended legs are MANUAL.md MV-sgr107.1–.3.
- plan-62bcd1 → `status: done, stage: done`; PLAN.md row updated.
- Open human items leaving this epic: MV-sgr107.1–.3 (global install
  refresh + two downstream backfill PRs), the unchecked `human` items on
  the sgr103/sgr105/sgr106 QA walkthroughs, and the nine ready debug
  specs this epic filed (ea4f41, 3b9e07, 7e2b56, 5c8b21, 620337, 8a9586,
  d7e8e5, 9f24c7, c94f14 — all rowed in DEBUG.md).
