---
hash: a02000
type: plan
created: 2026-04-26T19:30:00-07:00
title: "Phase 6 — Focus group: persistent persona panel"
status: deferred
from: docs/ROADMAP.md#phase-6--focus-group-week-67
spawned: []
mode: YOLO
project_shape: empty-dream
thoroughness: balanced
stack_layers: [backend]
blocked_by: [f01000]
---

## Goal

Persistent persona panel consulted at every meaningful change — pre-plan reactions, pre-promotion UX checks, evolving reaction libraries fed by real telemetry.

## Scope

Five epics from [`ROADMAP.md § Phase 6`](../docs/ROADMAP.md#phase-6--focus-group-week-67):

- `epic-focus-group-init` — `/devx-init` Q5 expands archetypes into 4–6 personas + mandatory anti-persona.
- `epic-focus-group-pre-plan` — new-feature-reaction prompt after party-mode in `/devx-plan`.
- `epic-focus-group-pre-promotion` — pre-merge-ux-check at promotion gate; mode-derived block thresholds.
- `epic-devx-focus-group-skill` — `/devx-focus-group` with `--persona` flag.
- `epic-persona-evolution` — LearnAgent reads telemetry vs. panel predictions; weekly reaction-library updates.

Cross-references [`FOCUS_GROUP.md`](../docs/FOCUS_GROUP.md) for the full panel contract.

## Sub-specs to spawn

To be elicited by `/devx-plan` when this plan is claimed.

## Acceptance criteria

- [ ] Fresh `/devx-init` produces `focus-group/personas/` with 4–6 detailed personas + 1 anti-persona.
- [ ] `/devx-plan` runs the panel after party-mode and writes a `focus-group/sessions/` artifact.
- [ ] PROD-mode promotion is blocked when ≥2 personas flag a UX regression.

## Status log

- 2026-04-26T19:30 — Phase 6 placeholder created
