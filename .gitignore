# devx runtime (gitignored per docs/DESIGN.md "What lives where")
.worktrees/
.devx-cache/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~

# Node / Bun (Phase 0 cli + Phase 4 worker)
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
.bun/
dist/
build/

# Flutter (Phase 8 mobile)
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/.pub-cache/
mobile/.pub/
mobile/build/
mobile/ios/Pods/
mobile/ios/.symlinks/
mobile/ios/Flutter/Flutter.framework
mobile/ios/Flutter/Flutter.podspec
mobile/ios/Runner/GoogleService-Info.plist  # gitignored per MANUAL M4.3
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/key.properties

# Coverage
coverage/
*.lcov
.nyc_output/

# Secrets — hardcoded here, also enforced by permissions.file_writes.deny
.env
.env.*
!.env.example
secrets/
*.pem
*.key
id_rsa*
.aws/credentials
.ssh/

# devx local-only state (per-user, not project)
~/.devx/
