# DEBUG — Bugs to fix

Backlog for `/devx` debug routing. Each entry points at a spec file under
`debug/`. Repro-first: no repro → no fix.

- [x] `debug/debug-494590-2026-07-26T15:57-loop-token-accounting.md` — Loop token accounting implausibly low: loop-2026-07-24T16-46 self-reported ~11.5k in / ~9.3k out for 13 iterations producing +2,861 diff lines (a single real worker iteration exceeds that), so the 2M/item + 10M/run budgets can never trip. Hypothesis: meter reads only the worker's final emission, not cumulative session usage. Status: done. From: hfiret retro E9. PR: https://github.com/LeoTheMighty/devx/pull/88 (merged 6d1decc)
- [x] `debug/debug-9b9be5-2026-07-26T15:41-gate-evals-midflight-state-awareness.md` — `devx gate evals` lacks mid-flight state-awareness: after a `devx revise` cascade on a workstream with shipped phases, the RED gate demands every tests-first eval be RED — but evals whose phases already merged (green by definition) fail it with "expected RED but exited 0", and P0s make that blocking. No CLI path to the D-9 WAIVED verdict exists either (hand-edit of RED-report.md is the only route). Repro: rooted-light repo @ workstream cdea58, 2026-07-25 — `devx revise cdea58 --touched prd.md` after rlw102/rlw104 merged, then replay gates: prd PASS, coverage(design) PASS, coverage(plan) PASS, evals FAIL on shipped-green E-1/E-5/E-6 while E-2/E-3/E-4/E-7 are correctly RED. Wanted: defer evals whose "Verified in phase" points at a done dev spec, plus first-class `devx gate evals --waive <reason>` writing a valid WAIVED verdict. Status: done. From: rooted-light session 2026-07-25. PR: https://github.com/LeoTheMighty/devx/pull/89

- [x] `debug/debug-9c4e21-2026-07-14T12:15-manual-append-read-check-write-race.md` — appendManualEntry read-check-write race can double-append or clobber concurrent MANUAL.md writes. Status: done. PR: https://github.com/LeoTheMighty/devx/pull/71 (merged 25cf144). (Pre-existing class from the /devx-init failure path; made hotter by pin102's installSkills — found in PR #70's 3-agent review.)
- [x] `debug/debug-6a913f-2026-07-15T08:27-tour-gather-no-debug-spec-support.md` — devx tour gather AND devx merge-gate cannot resolve debug/ specs (findSpecForHash-style dev/ hardcoding duplicated per command), so debug-loop PRs ship without tours and the Phase 8 gate false-negatives with "manual merge required". (Found during debug-9c4e21 Phase 7.5 + Phase 8; claim already has --type debug — audit every hash-resolving CLI and centralize per-type spec resolution.) Status: done. PR: https://github.com/LeoTheMighty/devx/pull/72 (merged 1284e01).
- [x] `debug/debug-e3f1c2-2026-07-15T13:05-install-global-exec-bit.md` — install:global produces non-executable devx (dist/cli.js missing +x). Status: done. From: user report 2026-07-15. PR: https://github.com/LeoTheMighty/devx/pull/78
- [x] `debug/debug-cf65aa-2026-07-24T13:22-loop-merge-tail-phase4-line.md` — loop merge tail never emits the dvx103 `phase 4:` line (every loop-shipped spec reds main on the next branch cut) + iteration prompt lets workers end with verification outstanding (hfi102 burned 3 iterations and was abandoned ~done). Status: done. From: hfi104 iteration-1 learning + loop-2026-07-24 post-run review. PR: https://github.com/LeoTheMighty/devx/pull/82
- [x] `debug/debug-dc7514-2026-07-25T08:55-loop-infra-failure-classification.md` — Loop counts infra hangs (WorkerTimeoutError, ~0 output tokens) as item failures and abandons into wrong state: `blocked` + dead owner + DEV.md status-text drift + bookkeeping-only preserved worktree; one hung item wedged the whole backlog (hfi103, run loop-2026-07-24T21-19; hand-reset in 5f83f3e). Fix: `infra-error` iteration class (abort the run, not the item), abandon-path state hygiene, sleep-aware ceiling. Status: done. From: loop-2026-07-24 post-mortem. PR: https://github.com/LeoTheMighty/devx/pull/84 (merged 9c671d8)
- [x] `debug/debug-b365ac-2026-07-15T12:14-yaml-devdep-breaks-tarball-install.md` — `yaml` is a devDependency but dist/lib/config-io.js imports it at runtime, so a clean packed-tarball install crashes with ERR_MODULE_NOT_FOUND on every invocation. Status: done. From: debug-e3f1c2 verification (throwaway-prefix tarball install). PR: https://github.com/LeoTheMighty/devx/pull/79
- [x] `debug/debug-a7c3f9-2026-07-28T14:12-backlog-lock-timeout-systemic-budget.md` — BacklogLockTimeoutError during a loop claim (live peer holding locks/backlog.lock past 30s) counts toward MAX_CONSECUTIVE_CLAIM_FAILURES; mlc104 widened the locked claim section to up to 5 network ops, so slow-remote + N loops can convert healthy contention into a spurious systemic stop via the waiting peers. Wanted: live-holder timeouts route like claim-contended (budget untouched) without masking wedged holders. Status: done. From: mlc104 Phase 4 review (EC-8). PR: https://github.com/LeoTheMighty/devx/pull/115
- [x] `debug/debug-b41f7c-2026-07-28T15:35-attach-branch-loop-hazards.md` — Loop `discardWorktree` force-deletes an inherited (mss102 attach-mode) claim branch: `git branch -D claim.branch` (driver.ts:1254) assumes the claim created the branch, but a branch-handoff follow-up's branch predates the claim and its commits sit below `baseSha` (invisible to `isBookkeepingOnlyWorktree`) — if origin's copy is gone, the handed-off work is destroyed. Also the stale-branch warning (driver.ts:1264) still promises "the next claim will fail", which mss102 made false. Status: done. Blocked-by: mss103. From: mss102 Phase 4 review (BH-3/BH-4). PR: https://github.com/LeoTheMighty/devx/pull/116
- [x] `debug/debug-c81f04-2026-07-28T23:30-backlog-mutate-r3-flake-on-main.md` — `test/backlog-mutate.test.ts` R3 reproduction ("N processes flipping distinct rows under the lock lose nothing") fails intermittently on macos-latest/node 20, reddening **main**: 2 of the last 4 main runs on 2026-07-28 (30407045825, 30406668976) died on `flips lost under concurrency despite the backlog lock: w3k4a`, while 30406815287 + 30406756616 passed the same suite. Either a real lost-update window in `withBacklogLock` under CI timing or a timing-sensitive harness — both need knowing, and a flaky main gate trains everyone to re-run. Status: done. Merged via https://github.com/LeoTheMighty/devx/pull/127 (squash 03a3ace). From: mss103 Phase 7 remote-CI investigation.
- [x] `debug/debug-74632d-2026-07-29T00:50-loop-driver-fixture-teardown-enotempty.md` — `test/loop-driver.test.ts` intermittently dies in FIXTURE TEARDOWN on macos-latest/node 20 with `ENOTEMPTY: rmdir '<tmp>/devx-loop-driver-XXXX/origin.git'` (run 30410808302, in mss103's new `E-3: budget-rail split` test at `:46` — the cleanup, not an assertion); ubuntu passed the same suite in the same run and macOS passed it locally 4× during mlc106. Leading hypothesis: a git subprocess spawned by the fixture outlives `rmSync`, and macOS refuses the rmdir where Linux tolerates it. Must NOT be "fixed" by swallowing the teardown error — a leaked handle is the same class that would half-remove a worktree in production. Status: done (fixed at root cause, not by swallowing: `mlcret` PR #103 disabled `gc.auto` / `receive.autogc` / `maintenance.auto` on both fixture sides in `test/helpers/loop-git-fixture.ts`, killing the background `git gc` that outlived `rmSync`; the `maxRetries: 10` in `afterEach` is belt-and-braces on top). Verified 2026-08-12: `loop-driver.test.ts` 62/62 green under full suite load. From: mlc106 Phase 7 remote-CI investigation.
- [x] `debug/debug-7c1e93-2026-08-04T10:45-loop-concurrency-suite-load-timeout.md` — `test/loop-concurrency.test.ts`'s G-1 harness times out (>600s) under full-suite parallel load but passes in 95s alone, at the same commit. CI runs the identical suite green in 27s on both runners (57s test time vs. 6,085s locally, ~100×), so the leading cause is macOS process-spawn cost + worker starvation, not a `devx loop` deadlock — but confirm before simply raising the timeout. Also captures the wider finding: the local gate costs ~30 min for work CI does in ~30s. Status: done. From: tur101 local gate. PR: https://github.com/LeoTheMighty/devx/pull/124 (merged 10a0105)
- [x] `debug/debug-ea4f41-2026-08-03T09:52-qa-walkthrough-hash-collision.md` — `/devx` Phase 5's QA-walkthrough emission names the file `test/test-<story-hash>-qa-walkthrough.md`, reusing the STORY's hash; the by-hash resolver requires hashes unique across type dirs, so `devx merge-gate <hash>` (Phase 8's gate) and `devx tour gather <hash>` (Phase 7.5) both fail with `resolves to 2 spec files` — the story becomes unmergeable by the skill. Every existing TEST.md row uses a fresh own-hash, so the instruction is the outlier. Wanted: canonical `test/test-<new-hash>-<ts>-<slug>.md` + frontmatter + a regression test asserting cross-dir hash uniqueness. Status: done. Merged via https://github.com/LeoTheMighty/devx/pull/126 (squash 22e748c). From: sgr103 (PR #112), where it was hit live and worked around by renaming.
- [x] `debug/debug-3b9e07-2026-08-03T09:48-loop-omits-phase4-status-log-line.md` — `devx loop` never emits the mandatory `phase 4:` status-log line (it writes `loop iteration N:` + Change/Learning bullets), so every loop-shipped story violates `test/devx-status-log-discipline.test.ts` the moment its merge-tail commit sets `status: done` — **main sat red for 3 consecutive runs on 2026-08-03** (rtl106 14:57, rtl104 15:00) and every open PR inherited it. The assertion's post-merge-only timing is fixed in sgr103 (PR #112); the loop-side emission is not. `LEARN.md` cross-epic "attended-era contracts break on first unattended contact", recurring. Status: done. From: sgr103 (PR #112). PR: https://github.com/LeoTheMighty/devx/pull/128
- [x] `debug/debug-7e2b56-2026-08-03T14:40-emit-retro-worktree-root.md` — `devx plan-helper emit-retro-story` resolves its repo root by walking up from cwd, so a run from inside `.worktrees/<x>/` writes the emitted spec, the spliced `DEV.md`, and (since sgr104) the regenerated `GRAPH.md` into the WORKTREE, on a feature branch — the main checkout never sees the emission. Both siblings already solve this and disagree with it: `claimSpec` refuses a linked-worktree root (mlc101), `devx graph` retargets to the main checkout ("load-bearing" per its header). Pre-dates sgr104, which only adds a third file to the same wrong place. Status: done. From: sgr104 Phase 4 review (all 3 agents independently). PR: https://github.com/LeoTheMighty/devx/pull/129
- [x] `debug/debug-ecdcda-2026-08-05T13:10-manage-spawn-5s-timeouts-under-load.md` — 7 tests across `test/manage-spawn.test.ts` + `test/manage-spawn-integration.test.ts` fail with a bare `Test timed out in 5000ms` whenever the box is loaded, waiting on real spawned child processes at vitest's 5s default — while a sibling test in the same file doing the same work takes **31.5s** and passes on its own generous timeout. **Proven pre-existing, not diff-induced**: clean tree (changes stashed) failed 7, dirty tree failed 6, and 28b267 touches only `src/lib/learn/*`. Fourth instance of the timing-dependent-test class; `debug-5c8b21`'s AC 3 sweep ("tests within 2x of their timeout") would catch these at ~1x — read that one first, this may collapse into it. Status: done (2026-08-12: dead-owner claim from session /devx-2026-08-11T1434 released — PID dead, worktree empty, its investigation already committed to main as d5336ff; absorbed debug-620337). Reproduced 2026-08-12 at d5336ff: 3 failed / 723 passed. From: 28b267 Phase 5. PR: https://github.com/LeoTheMighty/devx/pull/125 (merged a3ad57f)
- [x] `debug/debug-5c8b21-2026-08-03T16:20-loop-concurrency-timeout-headroom.md` — `test/loop-concurrency.test.ts`'s G-1 case runs 467s under parallel suite load against a 600s per-test cap (~1.3x headroom), so a warm/loaded machine reds the gate on timeout, not correctness: measured 122s isolated vs >600s under load (~5x amplification). Not a regression — `claim-contention` (unchanged code) slowed by the identical 1.71x across the same two runs, i.e. machine-level. Third instance of the timing-dependent-concurrency-test class after `debug-c81f04` + `debug-74632d`, so AC 3 is a suite-wide sweep for tests within 2x of their timeout. Status: done. From: sgr104 Phase 5. PR: https://github.com/LeoTheMighty/devx/pull/130
- [x] `debug/debug-8a9586-2026-08-05T11:47-loop-merge-tail-no-regen-host.md` — Loop merge tail leaves GRAPH.md stale: `driver.ts` closes items via its own `setSpecStatus`/`markBacklogRowDone` path with no `regenerateGraph` host, so FR-4 is satisfied only on the attended path. Includes the open design question of whether the tail should call `markDone`. Status: done. From: sgr105 Phase 4 self-review. PR: https://github.com/LeoTheMighty/devx/pull/131
- [x] `debug/debug-d7e8e5-2026-08-05T12:20-gh-transient-401-merge-tail.md` — check-hold + merge-gate exit 2 on a single transient gh GraphQL 401 (observed ~50% of calls for ~15 min on 2026-08-05 while REST + auth were healthy); attended cost is a manual retry loop, unattended cost is a green item stranded unmerged by the loop's merge tail. Wanted: bounded shared retry on transient gh failures. Status: done. From: sgr107 Phase 8. PR: https://github.com/LeoTheMighty/devx/pull/132
- [x] `debug/debug-9f24c7-2026-08-05T12:20-unparseable-spec-frontmatter-silent.md` — `readEngineState` never inspects `doc.errors`, so a spec whose frontmatter YAML doesn't parse reads as an all-defaults `EngineState` — `status: null`, `blocked_by: []`, `phase: null`, gates false — indistinguishable from a spec that records nothing, in `devx next`, reconcile, the loop pick, every gate CLI and the graph model. **5 shipped specs were invisible this way** (`mgr102`, `mgr103` — unquoted `title:` containing `: `; `cfg204`, `cli303`, `cli304` — `title:` opening with a backtick). sgr106 fixed the 5 data instances and added `frontmatterParseError`; the CLASS is still open — the next unquoted title goes silent again. Note `applyEnginePatch` already throws on the same input, so reader and writer disagree about the same file. Status: done. From: sgr106 attended backfill run (PR #TBD). PR: https://github.com/LeoTheMighty/devx/pull/137 (merged 3603021)
- [x] `debug/debug-c94f14-2026-08-05T14:05-await-remote-ci-conflicting-pr-blind.md` — `await-remote-ci` reads a CONFLICTING PR as `empty`: PR #118 sat 50min at 41 blind probes + an INTERVIEW escalation while `gh pr view --json mergeable` said `CONFLICTING` the whole time — GitHub builds no merge ref for a conflicted PR, so `pull_request` workflows never fire. Wanted: a distinct `pr-conflicting` probe state + Phase 7 self-service routing (merge base into branch, push, re-probe). Status: done. From: sgr105 resume session (root cause of INTERVIEW Q#16). PR: https://github.com/LeoTheMighty/devx/pull/133
- ~~`debug/debug-620337-2026-08-05T12:57-loop-worker-manage-crash-local-gate-red.md` — `loop-worker` + `manage-crash-restart-loop` fail reproducibly from an agent worktree (18/28 on the sgr105 branch, **16/28 at its pre-sgr105 baseline** — bisected, not caused by the story), so Phase 5s local gate is red before any diff is applied and agents are trained to wave failures through. Empty captured stdout, a 0min awake-time ceiling, a 30s mgr105 timeout; CI on main is green over the same period. Worse than `debug-5c8b21`/`debug-b7f2c1`: reproduces at baseline with the files run alone.~~ Status: superseded 2026-08-12 — folded into `debug-ecdcda`. Its central premise (reproduces from an agent worktree, at baseline) was REFUTED by 7c1e93 H1: main checkout 19.39s vs linked worktree 19.42s, 55/55 green in both. Both named files passed in the 2026-08-12 full-suite run at d5336ff. Residue lives in `debug-ecdcda` (test-side blocking) + `debug-5c8b21` (measured caps). Kept for audit. From: sgr105 Phase 5.
- [x] `debug/debug-4d1a9c-2026-08-07T11:37-claim-commits-on-current-branch-not-base.md` — `devx devx-helper claim` writes the claim commit to whatever branch the MAIN worktree has checked out (`claim.ts:1071` commits with no branch assertion), then `git push origin <pushTarget>` (`claim.ts:1097`) pushes the *local* ref of that name — unmoved — so git says `Everything up-to-date`, `isRejectedPush` never fires, no rollback runs, and the helper exits 0 with a `claimSha` that reached nothing. The item's worktree then bases off the unmoved target, so the spec inside the branch that becomes the PR still reads `status: ready`. Reproduced against `455f02c` with a throwaway fixture. Fix shape (user-chosen): refuse with a typed `ClaimError("validate")` beside the existing canonical-root assertion — never mutate a working tree a peer session may be mid-edit in. Status: done. From: downstream `friend-finder-mesh` report `debug-3fa71c` (cost a hand-repair during `ffm106`). PR: https://github.com/LeoTheMighty/devx/pull/123 (merged 782f81e)
- [x] `debug/debug-7b3e2a-2026-08-07T12:40-merge-gate-reads-yaml-null-branch-as-string.md` — `merge-gate`'s hand-rolled `readFrontmatter` (merge-gate.ts:111) stores YAML `branch: null` as the STRING `"null"`, and the fallback guard `typeof fm.branch === "string" && fm.branch.length > 0` (merge-gate.ts:333) accepts it — so `deriveBranch` is never reached and the gate queries `gh pr list --head null` → `[]` → `{"merge":false,"reason":"no PR yet"}` exit 2, permanently, for any spec that never had `branch:` set. Proven on 4d1a9c: `gh pr list --head feat/debug-4d1a9c` returned PR #123 while the gate said no PR; flipping only that one field flipped the gate to `{"merge":true}`. Attended cost is a hunt for a PR that is plainly already open; unattended cost is the loop's merge tail stranding a green CI'd item forever. Same class as `debug-9f24c7` (hand-rolled parser disagreeing with YAML about `null`). Status: done. From: 4d1a9c Phase 8. PR: https://github.com/LeoTheMighty/devx/pull/134
- [x] `debug/debug-5e1a77-2026-08-19T16:08-unenforceable-timeouts-false-green.md` — **16 tests run PAST their own timeout and still report PASSED**: `test/loop-driver.test.ts:1271` takes 221,677ms against vitest's 5,000ms default (44x over) and the suite calls it green; also loop-driver `:903`/`:1820`/`:861`/`:551`, `learn-watch:1269`, `devx-claim:754`, `stub:148`, all >100s under a 5s cap. Cause is fault (2) already named in `vitest.shared.ts` — `realExec` is `spawnSync`, and a blocked event loop cannot fire the `setTimeout` inside `@vitest/runner`'s `withTimeout`, so the cap is absent rather than generous. 21 tests under 2x, all 21 in `SYNC_BLOCKING_TESTS` across 15 files; of the 65 under 5x only 5 are in the async pass (`loop-worker` ×4, `manage-spawn-integration:124`) where caps DO fire. Fifth instance of the timing-dependent-test class and the first whose failure mode is a false PASS, not a red build — `debug-ecdcda` predicted this sweep would catch them "at ~1x" and then closed on the symptom, leaving the async-exec-seam mechanism unowned. Do NOT fix by raising caps: a cap that cannot fire is not enforcement at any value. Status: done. From: debug-5c8b21 AC-3 suite-wide sweep (measured full-suite run, 2026-08-19, 139 files green). PR: https://github.com/LeoTheMighty/devx/pull/135
- [ ] `debug/debug-2e1174-2026-08-21T16:05-blocking-pass-timeout-headroom.md` — Tests in the ASYNC blocking pass sit within 15% of vitest's 5s default and fail CI on load alone: `loop-driver`'s MED-4 scenario clocks 4,373ms locally / 5,519ms on macos CI, and has now reddened two PRs whose diffs did not touch the driver. Explicitly NOT `debug-5e1a77`'s anti-pattern — that is about sync-blocking caps that CANNOT fire; these fire correctly and are simply set below the work. One instance fixed in the uwg102 PR with a measured cap; the class needs the `timeout-headroom` sweep. Status: ready. From: uwg102 Phase 7.
- [ ] `debug/debug-75563d-2026-09-02T09:20-red-eval-sha-lock-unwired.md` — **The RED eval lock is inert for every workstream in the repo.** `stampEvalShas()` (`src/lib/engine/evals-lock.ts:85`) has zero callers in `src/` — the library shipped, the Gate 4 call site did not — so no PASS ever writes `gate_status.red_eval_shas`, `verifyStepBodies()` has nothing to verify, and `/devx` Phase 5's hard stop can never fire. The lock's own grandfathering rule then reads every workstream as "predates the stamp" and reports instead of blocking, which is why this has been silent. Three shipped prose surfaces state the lock as active fact: CLAUDE.md § "Fix the code, not the eval", `.claude/commands/devx.md` Phase 5, and `.claude/commands/devx-plan.md` RED step 2b. Also needs a decision on what `stepBody()` means for a `.ts` eval — it strips result-of-record LINES, which is written for step-bearing markdown. Status: ready. From: `/devx red a494be` (Gate 4 PASS wrote `evals_red: true` and no shas).
