# LEARN — Interim learnings backlog

What we changed about the *process* (not the product) while shipping each epic. Append-only; per-epic sections.

This file is the interim home for retrospective findings until Phase 5 ships `epic-retro-agent` + `epic-learn-agent` (and the proper `LESSONS.md` workflow with confidence-gated auto-apply). At that point `LearnAgent` will ingest this file's contents into `LESSONS.md` and the per-epic retro stories below become no-ops.

See [`docs/ROADMAP.md` § Locked decisions](./docs/ROADMAP.md#locked-decisions-cross-epic) — "Interim retro discipline".

---

## How this file is updated

Every epic ends with a `*ret` story (e.g. `audret`, `cfgret`) that:

1. Runs the native `/devx retro` stage against the epic's shipped stories
   (re-targeted from `bmad-retrospective` at v2x101 per `v2/07-decisions.md`
   D-3; the row contract below is unchanged).
2. Appends findings to this file under the epic's section.
3. Applies low-blast-radius items immediately; files higher-blast items as `MANUAL.md` rows or new dev specs.

## Entry shape

Each finding is one row under its epic section:

```
- [confidence] [blast-radius] What worked / didn't / changes — applied | filed-as <ref> | pending
```

- **confidence**: `low` / `med` / `high` — based on signal strength (one anecdote vs. recurring pattern).
- **blast-radius**: `memory` / `skill` / `template` / `config` / `docs` / `code` — what surface the change touches; ceiling for auto-apply per `MODES.md`.
- **action**: `applied` (in this PR), `filed-as <ref>` (new dev spec / MANUAL row), or `pending` (waiting on something).

## Per-epic sections

Sections are added by each `*ret` story when it runs. Empty sections below are placeholders so the order matches `DEV.md`.

### Phase 0 — Foundation (plan: plan-a01000)

#### epic-bmad-audit

*Initial four findings below were extracted by hand on 2026-04-27 from status logs of aud101–aud103, ahead of `audret` running formally. Findings E1–E5 below them were added by the formal `bmad-retrospective` pass run during the audret PR (PR #19). The formal pass reconciled rather than duplicated the hand-extracted entries; the BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-bmad-audit-retro-2026-04-27.md`.*

- [high] [skill+docs] **Stale `branch:` frontmatter on every spec.** Planner emitted `develop/dev-<hash>` as a default; project runs `git.integration_branch: null` + `branch_prefix: feat/`, so every claim had to "correct branch to feat/dev-<hash>". 3/3 stories hit this. — applied: `/devx-plan` + `/dev-plan` skills now derive `branch:` from `devx.config.yaml`; `docs/DESIGN.md § Spec file convention` adds a "Branch field — derived from `devx.config.yaml`" sub-section; sample frontmatter updated.
- [high] [docs] **Forward-pointing notes between sibling stories work well.** aud101 left a SKILL-not-vendored note for aud103, aud102 left three forward-pointing notes (qa-lens, shadow-vs-wrap, TEA ranking), aud103 resolved them all with explicit markers. The convention is: "leave a note in the produced artifact for the downstream story; the downstream story closes them out as part of its own ACs." — applied: noted here; promote to template/CLAUDE.md once a third epic confirms.
- [med] [docs] **No remote CI workflow yet → noisy "no required checks" log lines on every PR.** Resolves once `ini503` lands. — pending: ini503. *Update from formal pass: audret (PR #19) is the first PR to land after `.github/workflows/devx-ci.yml` was wired by cli305; this is the first /devx run that actually exercises CI-configured gating end-to-end.*
- [med] [config] **`bmad-audit.md` Risk 3 explicitly called out the missing `bmad-retrospective` wiring.** This is the trigger for the entire interim retro discipline (locked 2026-04-27). — applied: `LEARN.md` exists, retro stories filed for all 9 active epics, `/devx-plan` and `/dev-plan` updated to emit `*ret` rows.
- [E1] [high] [skill] **`bmad-agent-qa` ghost reference still unfixed at retro time.** `/devx-plan` and `/dev-plan` Phase 6 party-mode lens lists name a `bmad-agent-qa` skill that doesn't exist in `_bmad/_config/skill-manifest.csv`. `bmad-audit.md` §4.2 row 1 prescribes replacement with `bmad-tea` (Murat). — applied in audret PR #19: replaced in both `.claude/commands/devx-plan.md` and `.claude/commands/dev-plan.md`. Single-line text edit each; QA lens now resolves to a real BMAD skill.
- [E2] [med] [docs] **Long-lived doc artifacts should embed their own re-run trigger.** `bmad-audit.md` §5 names the precise inputs whose change should re-trigger the audit (`_bmad/_config/manifest.yaml → modules[].version`, etc.) instead of relying on out-of-band staleness detection. The pattern is reusable for research reports, plan files, and any artifact whose validity depends on inputs the document itself names. — pending-concordance: 1/4 epics observe this. Promote to `/devx-plan` epic-shape default once a second epic produces a re-run-trigger-bearing artifact.
- [E3] [med] [skill] **Party-mode personas can name filesystem paths that don't exist.** `epic-bmad-audit.md` party-mode minutes locked `_bmad/_cfg/manifest.yaml` as a path; the actual path is `_bmad/_config/manifest.yaml`. aud103 caught the mismatch and added a §5 footnote in `bmad-audit.md`, but the underlying epic file still carries the wrong path. Lesson: `/devx-plan` Phase 6 should verify any filesystem path a persona names before locking it as a decision. — pending-concordance: 1/4 epics. Promote to a Phase 5 LearnAgent rule (or earlier party-mode prompt-card) once a second epic confirms.
- [E4] [low] [docs] **Risk-severity calibration (gradient, not uniform) is a quality marker.** `bmad-audit.md` §3 ranks five risks by blast radius: TEA orphan = High, sprint-planning shadow + retrospective gap + manifest drift = Medium, UX timing = Low-conditional. The gradient lets downstream phases sequence work meaningfully. Codify as a §3 expectation in `/devx-plan` epic-shape prompts (not "list risks", but "rank risks by blast radius and name severity per row"). — pending-concordance: 1/4 epics. Note here; revisit at next retro.
- [E5] [med] [docs] **"Known gap to wire in Phase N" markers in plan locked-decisions should produce *interim discipline* rows in the parent plan, not just deferred-epic placeholders.** `epic-bmad-audit.md`'s "Locked decisions fed forward" already named the missing-`bmad-retrospective` gap. But the *interim discipline* (this LEARN.md + per-epic `*ret` story) had to be invented mid-flight when the user noticed the gap on 2026-04-27 — the original Phase 0 plan didn't carry it. Lesson: when a plan locks "X is a known gap, deferred to Phase N," it should also lock "interim approximation of X for Phases 0..N-1." — pending-concordance + higher blast radius (touches `/devx-plan` Phase 4 + `docs/DESIGN.md` "Locked decisions" convention); flag as a candidate dev spec under the Phase 5 LearnAgent epic when that lands.

#### epic-config-schema

*The first four findings below were extracted by hand on 2026-04-27 from the cfg201–cfg204 status logs, ahead of `cfgret` running formally. Findings E1–E7 below them were added by the formal `bmad-retrospective` pass run during the cfgret PR. The formal pass reconciled rather than duplicated the hand-extracted entries; the BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-config-schema-retro-2026-04-27.md`.*

- [high] [docs] **Spec ACs vs. epic "locked decisions" precedence was undocumented.** cfg202 hit a real conflict: spec ACs said XDG-on-Linux, epic-config-schema.md "locked decisions" said `~/.devx/` everywhere. Author followed spec ACs (the right call) but with no rule to point at. — applied: `docs/DESIGN.md § Source-of-truth precedence` now codifies spec ACs > epic locked decisions > plan frontmatter > `devx.config.yaml` > skill defaults, with a "fix the loser in the same PR" rule. *Update from formal pass: cfg202 fixed only the rule's home doc, not the loser (epic-config-schema.md line 122). cfgret PR closes that gap — see E4 below.*
- [high] [code] **Self-review (edge-case-hunter) consistently catches real bugs.** cfg201 (5 issues fixed), cfg204 (12 findings, 5 actionable). The hits include real semantics issues (ajv-strict mutex idiom, eemeli/yaml setIn-on-Seq trap, type-coercion regex gaps) — not just lint. — applied: noted; recommend reinforcing self-review as a non-skippable step in `/devx` Phase 6 (already there per logs, but call it out in CLAUDE.md if absent). *Update from formal pass: cfg202 (1 load-bearing fix) + cfg203 (2) round out the picture — every cfg story turned up at least one self-review hit. Cross-epic-patterns row now applied: CLAUDE.md "Working agreements" gets an explicit "Self-review is non-skippable" bullet in cfgret PR.*
- [med] [code] **eemeli/yaml `setIn` on a Scalar replaces the node and loses inline comments.** Workaround: mutate `Scalar.value` in place, fall back to `setIn` only when the path doesn't exist. cfg202's "no comments lost" diff regression catches this. — applied: lives in source; flag as a `bmad-quick-dev` lesson if `bmad-quick-dev` ever touches yaml round-tripping.
- [low] [docs] **YOLO single-branch auto-merge confirmed across 3 stories** (cfg202 user-feedback, cfg204 self-driven, mirrored across cli/sup epics). — applied: `/devx` skill already enforces; record as a green check, no further action.
- [E1] [high] [skill] **`bmad-create-story` step in `/devx` Phase 2 silently skipped across all 4 Phase 0 epics.** No `_bmad-output/implementation-artifacts/story-*.md` file was produced for any of cfg201–cfg204 (or aud, cli, sup, ini); the only files in that directory are `epic-bmad-audit-retro-2026-04-27.md` and `sprint-status.yaml`. Spec ACs have been the de-facto source of truth; in YOLO + empty-dream they cover what `bmad-create-story` would generate. The /devx skill prescribes the step but reality has been "skip when spec ACs are sufficient." — pending-concordance + user-review: 4/4 epics observed, but the fix is a product decision (enforce / make conditional / drop). Filed for first LearnAgent pass when Phase 5 lands; no MANUAL row because it's a skill-prompt change, not a user-action. CLAUDE.md "How /devx runs" Phase 2 now carries an inline note acknowledging the drift.
- [E2] [high] [skill+docs] **Retro stories (`*ret`) are absent from `sprint-status.yaml`.** cfgret (this story) and audret (PR #19) are both filed in DEV.md but neither has a row in `_bmad-output/implementation-artifacts/sprint-status.yaml`. The /devx Phase 8.6 cleanup step "flip the matching `<hash>` to done" silently no-ops. Lesson: when `/devx-plan` or `/dev-plan` emits a `*ret` story, it must also append a sprint-status.yaml row. — pending-concordance: 2/2 retros observed; promote to skill-edit when a third retro confirms (cliret next).
- [E3] [high] [docs] **Pre-existing parent-story `sprint-status.yaml` flips drifting.** aud101–103 (`backlog`), cfg201 (`backlog`), sup405 (`backlog`) — all merged but yaml not updated by their respective /devx cleanup commits. Phase 8.6 IS supposed to flip these. Drift currently harmless (no consumer reads the yaml yet) but becomes a behavior bug the moment LearnAgent or `/devx-manage` lands. — applied for cfg201 (in-scope, same epic) in cfgret PR. Filed remainder as `MANUAL.md MP0.1` for user decision (mechanical four-row chore-commit, or new debug spec).
- [E4] [med] [docs] **"Fix the loser" half of the precedence rule was skipped.** cfg202 followed `docs/DESIGN.md § Source-of-truth precedence` but did NOT update the losing-side `epic-config-schema.md` "Locked decisions fed forward" line 122 — it kept claiming `~/.devx/` cross-platform when implementation is XDG-on-Linux. Survived four stories. — applied in cfgret PR: epic-config-schema.md line 122 now matches implementation.
- [E5] [med] [docs] **CLAUDE.md "How /devx runs" section was stale relative to the project's single-branch config.** Lines 184–185 prescribed `develop/dev-<hash>` worktrees off `develop`; line 195 said "Push + PR to `develop`". Self-inconsistent with the same file's §"Branching model" section that explicitly states the project is single-branch on `main`. — applied in cfgret PR: section rewritten with `feat/dev-<hash>` off `main`, `gh pr merge --squash --delete-branch` (the form that works without "Allow auto-merge"), explicit claim-commit-push-before-PR rule, and an inline note about the bmad-create-story drift.
- [E6] [low] [docs] **Phase-0 stub-policy carve-out for the only real-functional command (`devx config`).** cfg204 AC#7: no-args invocation prints usage to stderr and exits 0 (not 64) — Phase 0 stub policy applies even though config is real. The right ergonomic default for Phase 1+ commands too: keeps CLI behavior consistent across stub and real, so users can't distinguish "not implemented" from "needs args" by exit code alone. — pending-concordance: single instance; revisit when a second real Phase 1+ command lands.
- [E7] [low] [docs] **`docs/CONFIG.md § Schema validation` no-op resolved.** epic-config-schema.md flagged CONFIG.md as carrying a stale path (`_bmad/devx/config-schema.json`) for the JSON schema. Verified during cfgret: CONFIG.md line 443 now reads "ships embedded in the devx npm package and is resolved at runtime via `require.resolve`" — already corrected at some point during cfg201–204. No action needed; flagged here so the audit→correction trail is closed.

#### epic-cli-skeleton

*The first five findings below were extracted by hand on 2026-04-27 from cli301–cli305 status logs, ahead of `cliret` running formally. Findings E1–E7 below them were added by the formal `bmad-retrospective` pass run during the cliret PR. The formal pass reconciled rather than duplicated the hand-extracted entries; the BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-cli-skeleton-retro-2026-04-27.md`.*

- [high] [docs] **Same precedence issue as cfg202.** cli302: party-mode minutes proposed adding a `preview:` follow-up line to stub stderr; spec ACs required stderr to "match exactly." Author followed the spec and pinned a single-line property test so any future preview-line bolt-on must update both the spec and the regression test. — applied via the same `docs/DESIGN.md § Source-of-truth precedence` fix above. *Update from formal pass: unlike cfg202, there is no "loser" locked-decision in `epic-cli-skeleton.md` to backport — the precedence rule is "fix the loser if one exists," not "edit the epic file unconditionally" (formal-pass §3.6).*
- [high] [code] **Symlinked `bin` (npm i -g) breaks `isMainEntry` checks.** cli301 self-review caught it (HIGH); fix uses `realpathSync` on both sides + regression test. — applied: lives in source; note for future CLI scaffolds (and any `/devx-init` work).
- [high] [code] **`npm test` must `npm run build` first if subprocess smoke depends on `dist/`.** cli301 self-review found this (MED). — applied: lives in source.
- [med] [code] **"Claim commit not pushed before PR open" race.** cli301 had to `reset --hard origin/main` after the squash subsumed an unpushed claim commit. Originally tagged for filing as `debug-flow01`. — superseded: the lesson is captured in `feedback_devx_push_claim_before_pr.md` (auto-memory) plus the explicit "Push the claim commit to `origin/main` before opening the PR" rule in CLAUDE.md "How /devx runs" Phase 1. cfgret + audret + cliret claim commits all pushed before PR open, confirming the corrective behavior. No phantom debug spec filed.
- [low] [config] **Trust-gradient threshold = 0 in YOLO single-branch makes auto-merge the steady-state.** cli302/cli304 explicitly cite "count=0/threshold=0". — applied: matches `/devx` skill behavior; record only.
- [E1] [high] [docs+config] **Retro-row schema in `sprint-status.yaml` was undefined.** Adding cliret to the yaml needed a decision about whether `*ret` rows go under their parent epic or in a separate "retros" section. **Resolved in cliret PR**: place `*ret` rows under the epic they retroe, ordered after parent stories. This keeps DEV.md ordering and sprint-status ordering parallel and makes "epic complete" a single-section check. Mechanical backfill applied for audret + cfgret + cliret. — applied in cliret PR.
- [E2] [high] [docs] **Third confirmation that `*ret` stories are absent from `sprint-status.yaml`** — audret + cfgret + cliret = 3/3 retros to date. cfgret filed this as E2 (`promote to skill-edit when a third retro confirms (cliret next)`) — cliret IS that third confirmation. Promoted to LEARN.md cross-epic patterns. Mechanical 3-row backfill applied here; the skill-prompt edit to `/devx-plan` + `/dev-plan` so future retro rows auto-emit is `skill` blast-radius (user-review-required) and is filed as `MANUAL.md MP0.2`. — applied (backfill) + filed-as `MANUAL.md MP0.2` (skill change).
- [E3] [med] [docs] **`debug-flow01` referenced in cli301 status log was never actually filed.** `debug/` directory does not exist; `DEBUG.md` is empty. The lesson lives in memory + CLAUDE.md instead. Reconciled by marking the hand-extracted row above as "superseded" rather than filing a phantom debug spec. Generalizable lesson: formal-pass retros should verify "filed-as X" references resolve to a real X — silent broken audit trail is worse than an explicit "superseded" marker. — applied in cliret PR.
- [E4] [med] [docs] **First remote CI workflow shipped at cli305** is a project-state inflection point. PRs #1–11 (every story before cli305) ran under "no remote CI workflow detected → local gates authoritative" per /devx Phase 7. PRs #12 onward (including ini501 PR #18, audret PR #19, cfgret PR #20, this PR) run under remote CI authoritative on a macOS+Ubuntu Node 20 matrix. Workflow triggers on `pull_request → main` and `push → main`. Important context for future LearnAgent timeline analysis. — applied (recorded in retro file); revisit when next CI surface lands (Flutter, Worker).
- [E5] [med] [docs] **WSL host-crossover detection (cli305) is a clean second example of "per-platform deviation with explicit rationale + dedicated test."** First example was sup404's `${HOME}` substitution because `wsl.exe --exec` doesn't spawn a shell. cli305's WSL detection (`uname -r` + `npm config get prefix`) short-circuits cleanly when not WSL. Two epics confirm the pattern; pending-concordance toward cross-epic promotion at next retro that observes a third instance. — pending-concordance: 2/3.
- [E6] [low] [docs] **"Subprocess smoke tests catch packaging realities the in-process tests miss."** cli301's two self-review hits (realpathSync for symlinked bin + npm-build-before-test for missing `dist/`) both surfaced because the smoke tests exercised the globally-installed shape. Pattern worth memorializing as a /devx-init or `/devx-plan` epic-shape default for any CLI-shaped epic. — pending-concordance: 1/3 (single epic so far); revisit at next CLI-shape work.
- [E7] [med] [docs] **CLAUDE.md "How /devx runs" Phase 2 inline note about `bmad-create-story` skip needs the cumulative count.** cfgret PR added an inline note saying "Empirically across all 4 Phase 0 epics this step has been skipped." After cliret merges, the cumulative concordance is **17/17 stories across all 4 shipped Phase 0 epics** (aud × 3, cfg × 4, cli × 5, sup × 5). Updating the wording in this PR keeps the in-file evidence aligned with reality; the actual /devx skill change remains user-review-required. — applied in cliret PR.

#### epic-os-supervisor-scaffold

*The first four findings below were extracted by hand on 2026-04-27 from sup401–sup405 status logs, ahead of `supret` running formally. Findings E1–E7 below them were added by the formal `bmad-retrospective` pass run during the supret PR. The formal pass reconciled rather than duplicated the hand-extracted entries; the BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-os-supervisor-scaffold-retro-2026-04-27.md`.*

- [high] [code] **WSL Task Scheduler intentionally deviates from the AC.** sup404 substitutes `${HOME}` at install time because `wsl.exe --exec` doesn't spawn a shell. Deviation called out explicitly in the status log and tested. — applied: in source; the *pattern* (AC-deviation-with-explicit-rationale-in-status-log) is healthy and worth preserving; promote into template once a third epic confirms. *Update from formal pass: paired with cli305's WSL host-crossover detection, the cross-epic count is now 2/3; iniret is the natural third-confirmation candidate (ini503 / ini505 will exercise install-time platform realities).*
- [med] [template] **Idempotency state file pattern (SHA-256 of installed artifact at `~/.devx/state/<thing>.installed.json`) is a clean primitive that recurred** across sup401 (stub install) and likely will across other installer-style work (`ini505`, future Concierge/Manager state). — applied: noted; file as a candidate skill/template enhancement when `/devx-init` lands and we have a place to centralize it. *Update from formal pass: the pattern actually recurred at all 4 install surfaces within the sup epic alone — sup401 stub + sup402 launchd + sup403 systemd + sup404 task-scheduler. 4 internal observations is solid; cross-epic concordance requires ini505 to confirm.*
- [low] [code] **Test-count compounding is real.** Across the epic: sup401 116 → sup402 133 → sup403 153 → sup404 172 → sup405 199. Discipline is paying off; no action needed. — applied: green check. *Update from formal pass: +83 net tests is the largest growth of any Phase 0 epic (cli ~36, cfg ~37, aud no test deltas).*
- [med] [docs] **`docs/SUPERVISOR-TESTING.md` was added by sup405 as a how-to for manual host-level test cases that don't fit unit tests.** Pattern: when a story needs hands-on verification, file a per-feature `docs/<FEATURE>-TESTING.md` rather than burying steps in MANUAL.md. — applied via sup405 PR; promote to a `docs/TESTING/` convention when a second case shows up.
- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — fourth confirmation.** audret + cfgret + cliret + supret = 4/4 retros to date. Already cross-epic-promoted in cliret PR with mechanical 3-row backfill (audret + cfgret + cliret). supret applies the 4th-row backfill (its own row) here. Skill-prompt change for auto-emission remains user-review-required (`MANUAL.md MP0.2`). — applied (mechanical backfill of supret row in this PR); cross-epic-patterns row count bumped from 3/3 to 4/4.
- [E2] [high] [docs] **sup405's `sprint-status.yaml` row still `backlog` despite PR #17 merging.** Same drift class as cfg201 (which cfgret applied in-scope) and aud101–103 (in MP0.1 for /devx-manage). — applied in supret PR: sup405 flipped from `status: backlog` to `status: done`. aud101–103 stay in MP0.1 (cross-epic).
- [E3] [med] [docs+template] **Injectable-exec pattern recurred at every platform installer (3/3).** sup402 (launchctl) + sup403 (systemctl + loginctl) + sup404 (schtasks) — all use injectable platform binaries so unit tests mock the underlying tool. This is what lets all 199 tests run on the macOS+Ubuntu Node 20 CI matrix without per-platform fan-out. Captured as project structure (`supervisor.ts` refactored at sup402 to share helpers via `supervisor-internal.ts`). — pending-concordance: 1 epic (sup) with 3 internal observations; promote to `/devx-plan` epic-shape default for installer-style epics when ini505 confirms cross-epic.
- [E4] [med] [docs] **MANUAL.md as a designed signal, not an escape hatch.** sup402 deliberately filed `MANUAL.md MS.1` at story-implementation time because the on-host kill-and-watch-restart check requires real launchd. sup405's verification-failure path is similarly designed: failed `verifySupervisor()` files MANUAL but doesn't abort `/devx-init`. Convention: "verification failure files MANUAL but never aborts the install/init flow" — graceful degradation > hard-fail for "I can't verify this from CI" cases. — pending-concordance: 1 epic (sup) with 2 internal observations; ini506 (failure-mode handling) is the natural cross-epic confirmation.
- [E5] [med] [docs] **Status-log terseness — sup epic uniformly omits per-story self-review finding counts (5/5 stories).** cli/cfg/aud all enumerate ("5 issues fixed", "8 findings, 1 real", "4 findings"); sup401–405 say only "implemented + self-reviewed." Loses signal for downstream LearnAgent. Could be sup-specific (different /devx run profile, status-log writing rush) or a regression worth catching. — pending-concordance: 1/N (only sup epic to date); revisit at iniret. Cheap fix if it persists: a /devx skill prompt-card line "status log MUST enumerate self-review finding counts."
- [E6] [low] [docs] **sup405 status-log timestamp ordering anomaly.** sup405 status log claims `2026-04-26T20:30` claim time — earlier than sup402's 20:46 implementation timestamp and well before sup404's 21:55 merge. PR # ordering (#13–17 chronological) contradicts the claimed start. Most likely: status-log timestamps are imprecise/manual today, will tighten when /devx auto-writes them from wall-clock. Record only.
- [E7] [med] [docs] **CLAUDE.md "How /devx runs" Phase 2 inline note carried "reaffirmed in cliret retro" wording.** After supret merges, the cumulative reaffirmation count is 4/4 retros — wording bumped to "reaffirmed in every retro to date" so evidence stays aligned. The 17/17 parent-story count is unchanged (supret is a retro, not a parent). — applied in supret PR.

#### epic-init-skill

*All 12 findings below were added by the formal `bmad-retrospective` pass run during the iniret PR. Unlike audret/cfgret/cliret/supret, this section had no hand-extracted entries to reconcile against — it was placeholder-empty at retro start because ini was the only Phase 0 epic still in flight at the 2026-04-27 hand-extraction pass. The BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-init-skill-retro-2026-04-27.md`.*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 5th confirmation.** audret + cfgret + cliret + supret + iniret = 5/5 retros to date. Already cross-epic-promoted in cliret PR with mechanical 3-row backfill (audret + cfgret + cliret); supret applied the 4th-row backfill (its own row); iniret applies the 5th-row backfill (its own row) here. Skill-prompt change for auto-emission remains user-review-required (`MANUAL.md MP0.2`). — applied (mechanical backfill of iniret row in this PR); cross-epic-patterns row count bumped from 4/4 to 5/5 retros.
- [E2] [high] [docs] **ini507 + ini508 `sprint-status.yaml` rows still `backlog` despite PR #28 + PR #29 merging.** Same drift class as cfg201, sup405, aud101–103 (where /devx Phase 8.6 silently no-ops on pre-existing rows). cfgret + supret precedent: in-scope same-epic flips applied in the retro; cross-epic stale rows stay in MANUAL.md MP0.1. — applied in iniret PR: ini507 + ini508 flipped from `status: backlog` to `status: done`. aud101–103 stay in MP0.1 (cross-epic).
- [E3] [high] [docs] **Per-platform deviation with explicit rationale + dedicated test — promoted to cross-epic.** cli305 (WSL host-crossover detection) + sup404 (`${HOME}` substitution at install because `wsl.exe --exec` doesn't spawn a shell) + ini505 (re-uses cli305's WSL detection; files MANUAL.md not init failure) = 3/3 epics confirmed. supret had this at 2/3 pending-concordance. — applied in iniret PR: new Cross-epic-patterns row.
- [E4] [high] [docs] **MANUAL-as-designed-signal pattern — promoted to cross-epic.** sup × 2 internal at supret (sup402 MS.1 + sup405 verify-fail-but-don't-abort) + ini × 6 internal (ini503 free-tier-private + no-remote = 2 MANUAL filings; ini505 verify-fail; ini506 BMAD-fail + gh-auth + no-remote = 3 designed MANUAL filings) = 2 epics with rich coverage. The convention "verification failure files MANUAL but never aborts the flow + MANUAL is the designated channel for can't-verify-from-CI surfaces and graceful-degradation handoffs." — applied in iniret PR: new Cross-epic-patterns row.
- [E5] [med] [docs+template] **Idempotency state file pattern — promoted to cross-epic.** sup × 4 internal at supret (`~/.devx/state/supervisor.installed.json` SHA-256 across stub + launchd + systemd + task-scheduler) + ini505 re-uses `installSupervisor()` → same primitive = 2 epics. The related-but-distinct "deferred-work-queue-on-disk" sibling (`.devx-cache/pending-gh-ops.json` from ini503/ini506) stays pending-concordance because it's a sibling pattern (state for replay, not state for idempotent-install), not the same primitive. — applied in iniret PR: new Cross-epic-patterns row for the install-side primitive; sibling row noted here.
- [E6] [med] [docs] **`bmad-create-story` skip cumulative count bumped from 17/17 across 4 epics → 25/25 across 5 epics** (aud × 3 + cfg × 4 + cli × 5 + sup × 5 + ini × 8). The skill change remains user-review-required (`self_healing.user_review_required_for: [skills]`); only the count + reaffirmation list were bumped. — applied in iniret PR: CLAUDE.md "How /devx runs" Phase 2 inline note + the Cross-epic-patterns row both updated.
- [E7] [med] [docs] **Status-log terseness pattern bumps from 1 epic to 2.** sup 5/5 stories (uniform omission of self-review finding counts) + ini 4/8 mixed (ini501, ini505, ini507, ini508 omit; ini502, ini503, ini504, ini506 enumerate fully). Cross-epic concordance threshold is ≥3 epics; not yet promotable. The corrective is a `/devx` skill prompt-card line ("status log MUST enumerate self-review finding counts") at `skill` blast-radius (user-review-required). — pending-concordance: 2/3 epics. Revisit at first Phase 1 retro.
- [E8] [med] [docs] **End-to-end fixture-harness pattern (ini508)** — three fixture repos (`empty/`, `existing-no-ci/`, `partial-on-devx/`) + scripted-answer harness + mocked `gh` shim covering 7 scenarios (3 fixtures × primary path + idempotent-rerun + 3 failure modes + corrupt-config halt) in ~26s. Reusable for Phase 1's `/devx-plan` + `/devx` e2e tests, Phase 4 mobile-companion gh-webhook tests, any future onboarding-flow regression. — pending-concordance: 1 epic. Promotion candidate to a `/devx-plan` epic-shape default for "user-facing flow" epics when a 2nd flow-shaped epic confirms.
- [E9] [low] [docs] **First real-functional Phase 0 command precedent (ini506 → `devx init --resume-gh`).** cli302 stubbed 10 commands; ini506 closed open-question OQ#1 by registering `init` as the 12th command, real-functional from ship. Proves the stub-vs-real boundary is per-command rather than per-phase. The `devx --help` listing convention from cli303 picks up new commands automatically. — applied (recorded in retro file); useful precedent for any future story that graduates a stub to real-functional.
- [E10] [low] [docs] **Spec-vs-implementation supersession pattern (ini506 JSON-vs-YAML queue).** Spec technical note said "queue is YAML, not JSON, for hand-edit visibility." ini506 kept JSON because ini503 (already shipped) wrote `pending-gh-ops.json`. The deviation is consistent with upstream-story reality, recorded in ini506's status log ("spec technical-note about YAML noted as superseded"), and doesn't trigger a spec rewrite. Convention: "downstream story finds spec-tech-note contradicts upstream-story shipped reality → keep upstream reality, record supersession in status log, don't rewrite spec." — pending-concordance: 1 epic. Revisit when a Phase 1+ story surfaces the same.
- [E11] [low] [docs] **ini507/ini508 minimally-populated status logs** ("claimed by /devx" / "merged via PR #N" without intermediate implementation/test-count/review milestones). Same-epic internal observation that correlates with E7's self-review-omission pattern; the cross-epic trajectory is captured in E7. Likely root cause is /devx run-style variance rather than story-shape. — recorded; revisit as part of E7's eventual cross-epic promotion.
- [E12] [low] [docs] **Phase 0 closure.** iniret is the 5th and final shipped-epic retro of Phase 0 — Foundation. After this PR merges, all 5 Phase 0 epics have a formal retro on file (aud / cfg / cli / sup / ini). The Cross-epic-patterns section is now seeded with a meaningful set of confirmed conventions; Phase 1's first epic retro inherits a richer baseline than aud did.

### Phase 1 — Single-agent core loop (plan: plan-b01000)

#### epic-merge-gate-modes

*All 12 findings below were added by the formal `bmad-retrospective` pass run during the mrgret PR. Like iniret, this section had no hand-extracted entries to reconcile against — it was placeholder-empty at retro start. The BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-merge-gate-modes-retro-2026-04-28.md`. mrgret is the FIRST Phase 1 retro; Cross-epic-patterns row promotions inherit the richer Phase-0-closed baseline.*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 6th confirmation.** audret + cfgret + cliret + supret + iniret + mrgret = 6/6 retros to date. Already cross-epic-promoted in cliret PR. Skill-prompt change for auto-emission remains user-review-required (`MANUAL.md MP0.2`, unchanged). Cross-epic-patterns row count bumped from 5/5 → 6/6 retros. — applied (mechanical backfill of mrgret row in this PR).
- [E2] [high] [docs] **Pure-function + CLI-passthrough + adversarial truth-table testing is a tight trio.** mrg101 ships `mergeGateFor()` as a pure function (no I/O — verified via mocked-fs/child_process throw-on-access test); mrg102 wraps it in a CLI passthrough that collects signals and feeds the function; the 20-row truth-table + 9-row trust-gradient + 30-row CLI-integration tests give layered coverage. Pattern: when decision logic is grid-shaped, separate the decision (pure function, matrix tests) from the data collection (impure wrapper, integration tests). — pending-concordance: 1 epic. Phase 1's epic-devx-plan-skill (`devx plan-helper derive-branch` / `validate-emit`) is the natural cross-epic candidate.
- [E3] [high] [docs] **Dogfood mid-epic, not at retro time.** mrg103 was the FIRST PR ever merged via `devx merge-gate <hash>` — the CLI shipped in mrg102, one PR earlier. mrg102 itself dogfooded `devx merge-gate mrg102` against the live PR before the squash-merge (`{"merge":true}` exit 0 → squash). Strongest signal that the epic's deliverable works in production shape — stronger than a retro-time integration test because the consumer ran on real PRs. Pattern: ship the consumer mid-epic; exercise on the next PR in the same epic. — pending-concordance: 1 epic.
- [E4] [high] [docs] **Externalize mode logic as a CLI primitive consumed via passthrough.** mrg102 explicitly removes the "Behavior by mode" table from `.claude/commands/devx.md` Phase 8 and replaces it with `devx merge-gate <hash>`. Eliminates the regression class "skill body says X, code says Y" — the highest-leverage cross-epic risk per the source-of-truth-precedence retros (cfgret + cliret). Pattern: any time a skill body says "behavior depends on mode," the mode logic should live in TS as a pure function and the skill should call a CLI passthrough. — pending-concordance: 1 epic. Phase 1 epic-devx-plan-skill is the candidate; E2 + E4 will likely promote together.
- [E5] [med] [docs] **Dead-code-with-tests as a contract-locking primitive.** mrg103 ships `promoteIntegrationToDefault` with no production call site (project is single-branch). Compiled, type-checked, exhaustively unit-tested (31 tests including a 4×2×2 mode/CI/trust matrix). Cost: ~480 lines (one source + one test file). Value: zero-rework when split-branch users arrive + drift detection if `mergeGateFor` shifts. Pattern: when a contract has a known-future consumer but no current consumer, ship the contract + tests now and document the dead-code declaration in the file header. — pending-concordance: 1 epic. Revisit when a second deferred-consumer surface ships (the `epic-modes-and-gates` Phase 9 candidate is the natural next case).
- [E6] [med] [docs] **`bmad-create-story` skip cumulative count bumped from 25/25 across 5 Phase 0 epics → 28/28 across 6 epics** (Phase 0 + Phase 1 first epic). The skill change remains user-review-required (`self_healing.user_review_required_for: [skills]`); only the count + reaffirmation list were bumped. — applied in mrgret PR: CLAUDE.md "How /devx runs" Phase 2 inline note + the Cross-epic-patterns row both updated.
- [E7] [med] [docs] **Status-log terseness pattern: mrg actively bucks the trend (0/3 omissions vs sup 5/5 + ini 4/8).** mrg103 explicitly says "self-review found nothing actionable" — that's enumeration with zero count, NOT omission, which is the correct shape. The future SKILL prompt-card change should require explicit-zero specifically, not just any enumeration. Cross-epic count for the omission pattern stays at 2/3 (mrg adds a positive third-epic confirmation but doesn't tip the omission threshold). The 3-epic data point now strongly motivates the skill change at next retro promotion. — pending-concordance: 2/3 epics for the omission corrective; mrg is a positive third data point that strengthens the case.
- [E8] [med] [docs] **Self-review-non-skippable now reaffirmed across Phase 0 + Phase 1's first epic.** mrg101 (1 MED + 2 LOW = 3 findings), mrg102 (2 MED + 1 LOW + 1 prod-only ENOENT = 4), mrg103 (explicit zero, "found nothing actionable"). 6 epics total now. The Cross-epic-patterns row's "spans all 5 shipped Phase 0 epics" wording bumps to "spans all 5 shipped Phase 0 epics + the first Phase 1 epic (mrg)." — applied in mrgret PR.
- [E9] [low] [docs] **Three stories shipped in one /devx session continuously (~2.5 hours).** mrg101 T20:55 (PR #31) → mrg102 T22:55 (PR #32) → mrg103 T23:20 (PR #33). First Phase 1 epic ships faster than any Phase 0 epic. Mostly because mrg is purely TS / no platform deviation; partly because /devx itself has matured across 33 PRs. — recorded; useful baseline for Phase 1 cadence comparisons.
- [E10] [low] [docs] **Dist on main was stale during mrg102 → mrg103 transition.** When mrg103 ran `node dist/cli.js merge-gate mrg103` from the main worktree, it errored with "unknown command 'merge-gate'" because `dist/` wasn't rebuilt after mrg102's source merged. Workaround: `node .worktrees/dev-mrg103/dist/cli.js`. Marginal-benefit corrective (post-merge build hook on main) doesn't justify new automation in YOLO send-it. — pending-concordance: 1 epic; revisit if a second occurrence shows.
- [E11] [low] [docs] **First /devx run that consumed its own epic's deliverable mid-epic (mrg103 → mrg102's CLI).** Useful audit precedent for any future epic where the consumer of a new primitive ships within the same epic boundary. Sister observation to E3 (dogfood-mid-epic). — recorded.
- [E12] [low] [docs] **First Phase 1 epic to ship + retro.** After this PR merges, Phase 1 has 1/5 epics shipped (epic-merge-gate-modes done; epic-pr-template, epic-devx-plan-skill, epic-devx-skill, epic-devx-manage-minimal remaining). The Cross-epic-patterns section now spans Phase 0 + Phase 1; CLAUDE.md "Status" section bumps from "Phase 0 — Foundation (closed 2026-04-27)" to add a Phase 1 progress block. — applied in mrgret PR.

#### epic-pr-template

*All 9 findings below were added by the formal `bmad-retrospective` pass run during the prtret PR. Like mrgret, this section had no hand-extracted entries to reconcile against — it was placeholder-empty at retro start. The BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-pr-template-retro-2026-05-02.md`. prtret is the SECOND Phase 1 retro and the FIRST retro to ship a /devx loop that consumed mrg102 (`devx merge-gate`) AND prt102 (`devx pr-body`) end-to-end on its own PRs (#35 + #36).*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 7th confirmation.** audret + cfgret + cliret + supret + iniret + mrgret + prtret = 7/7 retros to date. Already cross-epic-promoted in cliret PR. Skill-prompt change for auto-emission remains user-review-required (`MANUAL.md MP0.2`, unchanged). Cross-epic-patterns row count bumped from 6/6 → 7/7 retros. — applied (mechanical backfill of prtret row in this PR).
- [E2] [high] [docs] **Spec amendment mid-flight is the right move when the planner missed an existing surface.** prt101 was claimed and halted in Phase 4 the first time when /devx noticed the existing ini503 PR-template write site (`init-gh.ts:248`, `_devx/templates/init/pull_request_template.md`) — shipping as written would have produced two write sites both targeting `.github/pull_request_template.md`. Halt was correct per CLAUDE.md "No silent product decisions" + `docs/DESIGN.md §185` source-of-truth-precedence. Resolution: amend the spec with explicit Phase-0-surface-removal ACs (delete `writePrTemplate()` from `init-gh.ts`, remove `prTemplate` from `WriteInitGhResult`, etc.) + substitution-marker hygiene AC, then resume. **Pattern: when /devx halts on a planner blind-spot, the resolution is amend-the-spec (option c), not paper-over-in-implementation (option a) and not file-a-second-spec (option b).** Option (c) keeps the audit trail single-rooted: the spec is the source of truth, the migration shows up as ACs, retro can see the full story. — pending-concordance: 1 epic. Promote when a 2nd halt-and-amend occurs.
- [E3] [high] [docs] **3-agent parallel adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor) catches more than single-pass.** prt102 self-review surfaced **12 raw items across 3 reviewers** (5 from Edge Case Hunter — BOM, headings-only AC, sliceAtMarker substring vs line-anchored, outside-project leak, empty template; 4 from Blind Hunter — multi-marker stripping with `replaceAll`, Windows path.sep, AC `includes()` fallback breaks line-anchoring discipline, trailing-newline contract drift; 3 from Acceptance Auditor — AC 5 integration-test claim, AC placeholder line-anchoring test gap, AC 1 wording precision); **after triage, 9 were actionable and fixed in-flight** (3 were marked "not a bug" / intentional design — the AC `includes()` fallback IS intentional per locked decision #5 visibility; AC 5's "real gh round-trip" is satisfied empirically; the trailing-newline contract is fine as documented). Re-review pass returned no regressions. Single-pass review (the canonical bmad-code-review shape with one adversarial reviewer) would have caught maybe 3-4 of the 12; the orthogonal framings (no-context vs edge-case vs spec-compliance) cover different failure-mode classes. Pattern: when the change has substantial surface area (>500 lines new code, multiple regex/substitution sites, integration with multi-marker on-disk state), parallel adversarial reviewers in different framings is the right shape. mrg102's single-reviewer pass was adequate for the smaller mrg surface; prt102's 3-reviewer pass was load-bearing for the substitution surface. — pending-concordance: 1 epic. Cross-epic candidate for the next medium-surface story.
- [E4] [high] [docs] **Live-on-github verification subsumes "real PR via gh against fixture repo" tests.** prt102 AC 5 said "verified via integration test that opens a real PR via `gh` against fixture repo and reads back via `gh pr view`" — interpreted literally that's a heavy test (requires fixture repo, network, secrets). The pragmatic interpretation that's actually load-bearing: every PR /devx opens going forward exercises the substitution live; the unit-test `**Spec:**` first-non-empty-line invariant is the same shape verified by `gh pr view` after merge. PR #36 itself dogfooded `devx pr-body` to render its own body. Pattern: when a deliverable's "integration test" is "the next PR /devx opens uses it," the live mechanism IS the test — fixture-repo gh-CLI tests are over-engineered. Sister to mrg E3 (dogfood-mid-epic) but tighter timing: the consumer ran on the SAME PR, not the next one. — pending-concordance: 1 epic + 1 sister. Promote with mrg E3 once a 3rd "deliverable consumed by its own PR" pattern lands (epic-devx-plan-skill is the natural candidate — `devx plan-helper` will be invoked by the very PR that ships it).
- [E5] [high] [docs] **Pure-fn + CLI-passthrough + adversarial-testing trio — concordance bumped to 2 epics.** prt102 ships `renderPrBody()` as a pure function (no I/O — verified separately + tested with 23 substitution unit tests) + `devx pr-body` CLI passthrough (collects spec / config / template, renders, exits) + 13 CLI integration tests + 5 help-test entries updated. Same shape as mrg E2 (mrg101 pure fn + mrg102 CLI passthrough + 20+9+30 layered tests). The pattern is **not** "every CLI command needs this"; it's **"when the decision logic is grid-shaped (substitution placeholders × line-anchoring discipline; mode × CI × coverage × trust-gradient), separate the decision (pure function, fixture tests) from the data collection (impure wrapper, integration tests)."** Bump pending-concordance from 1 epic (mrg) → 2 epics (mrg + prt). — pending-concordance: 2/3 epics. Promote at the 3rd (epic-devx-plan-skill `deriveBranch` / `validateEmit` are the natural next instances).
- [E6] [high] [docs] **Externalize behavior-by-mode logic as a CLI primitive consumed via passthrough — concordance bumped to 2 epics.** mrg E4 said: "any time a skill body says 'behavior depends on mode,' the mode logic should live in TS as a pure function and the skill should call a CLI passthrough." prt102 generalizes the rule: any time a skill body says "behavior depends on something with edge cases" (mode, template shape, AC checklist, line-anchoring discipline), externalize. The .claude/commands/devx.md Phase 7 update explicitly says "Never re-implement the substitution in the skill body — the CLI is the single source of truth." Same regression class avoided as mrg E4 ("skill body says X, code says Y"). — pending-concordance: 2/3 epics. E5 + E6 will likely promote together at epic-devx-plan-skill.
- [E7] [med] [docs] **`bmad-create-story` skip cumulative count bumped from 28/28 across 6 epics → 30/30 across 7 epics** (Phase 0 25/25 + mrg 3/3 + prt 2/2). The skill change remains user-review-required (`self_healing.user_review_required_for: [skills]`); only the count + reaffirmation list were bumped. — applied in prtret PR: CLAUDE.md "How /devx runs" Phase 2 inline note + the Cross-epic-patterns row both updated.
- [E8] [med] [docs] **Self-review-non-skippable confirmed across Phase 0 + Phase 1's first 2 epics.** prt101 (explicit-zero, "self-reviewed (zero actionable findings)" — correct shape per mrg E7), prt102 (9 findings via 3-agent parallel pass — see E3, all fixed in-flight, re-review clean). 7 epics total now. The Cross-epic-patterns row's "spans all 5 shipped Phase 0 epics + the first Phase 1 epic (mrg)" wording bumps to "spans all 5 shipped Phase 0 epics + the first 2 Phase 1 epics (mrg + prt)." — applied in prtret PR.
- [E9] [med] [docs] **First /devx loop to consume BOTH new Phase 1 primitives end-to-end on its own PRs.** prt101 PR #35: ran `devx merge-gate prt101` (mrg102's CLI) → exit 0 → squash. prt102 PR #36: ran `devx pr-body --spec ...` (prt102's CLI itself, dogfood) AND `devx merge-gate prt102` → both exit 0 → squash. PR #36 is the strongest possible audit precedent — a CLI's first production consumption is the PR that shipped it. Sister to mrg E11 ("first /devx run that consumed its own epic's deliverable mid-epic") but tighter (same PR, not next PR). The combination of (a) this PR's `devx pr-body` ran on prtret's body + (b) mrg E11's mid-epic dogfood = the bootstrap loop is now using its own primitives for routine work. — recorded; useful audit precedent for any future Phase 1+ epic where the consumer ships within the same epic boundary.

#### epic-devx-plan-skill

*All 11 findings below were added by the formal `bmad-retrospective` pass run during the plnret PR. Like mrgret and prtret, this section had no hand-extracted entries to reconcile against — it was placeholder-empty at retro start. The BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-devx-plan-skill-retro-2026-05-05.md`. plnret is the THIRD Phase 1 retro and the FIRST retro to flow through ALL of mrg102 (`devx merge-gate`), prt102 (`devx pr-body`), and (downstream-of-pln) pln103's `devx plan-helper validate-emit` discipline end-to-end.*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 8th confirmation.** audret + cfgret + cliret + supret + iniret + mrgret + prtret + plnret = 8/8 retros to date. Already cross-epic-promoted in cliret PR; **structurally closed** by pln102 (`emitRetroStory()` + `writeRetroAtomically()` co-emit all three artifacts). plnret itself was emitted by `/devx-plan` on 2026-04-28 (before pln102 shipped), so its sprint-status row required the standard backlog → done flip in this PR. **This is the last retro that needs manual backfill** — future `/devx-plan` runs consume the helper. Cross-epic-patterns row count bumped from 7/7 → 8/8 retros + closure note added; `MANUAL.md MP0.2` flipped to done. — applied (mechanical backfill of plnret + epic-devx-plan-skill done flips in this PR).
- [E2] [high] [docs] **Pure-fn + CLI-passthrough + adversarial-testing trio — promoted to cross-epic.** mrg E2 (mrg101 `mergeGateFor` + mrg102 CLI passthrough) → prt E5 (prt102 `renderPrBody` + `devx pr-body` CLI) → pln epic ships **three fresh instances** in one epic: pln101 `deriveBranch()` + `devx plan-helper derive-branch`; pln102 `emitRetroStory()` + `writeRetroAtomically()` + `devx plan-helper emit-retro-story`; pln103 `validateEmit()` + `devx plan-helper validate-emit`. Cross-epic count 3/3 (mrg + prt + pln). The pattern's micro-variant from pln102: when the I/O is sufficiently tight to the pure logic, keep them in the same file but export the pure piece separately (vs mrg's separate-file shape). Both work; same-file is appropriate when the I/O is just persistence for the pure output. — applied in plnret PR: new Cross-epic-patterns row.
- [E3] [high] [docs] **Externalize behavior-as-CLI-primitive consumed via skill-body passthrough — promoted to cross-epic.** mrg E4 (any-time-skill-says-mode externalize) + prt E6 (generalized to any-time-skill-says-edge-cases) + pln × 3 fresh applications (pln101 branch derivation, pln102 retro emission, pln103 cross-reference validation). Cross-epic count 3/3 (mrg + prt + pln). The skill body's role is reduced to "invoke the right CLI and react to its exit code"; the regression class "skill body says X, code says Y" is closed at planning surface. — applied in plnret PR: new Cross-epic-patterns row.
- [E4] [high] [docs] **3-agent parallel adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor) — promoted to cross-epic.** prt E3 was 1 epic + pending-concordance. pln epic exercises this **4 times within the epic** (pln102: 27 raw findings; pln103: 27; pln105: 16; pln106: 16 — total 86 raw across 4 substantial-surface stories), single-pass on the 2 below-threshold stories (pln101: 1 HIGH; pln104: 3 MED). Cross-epic count: prt + pln = 2 epics with rich coverage (4 internal observations in pln, 1 in prt) — promoted per the iniret precedent for "promote at 2 epics with rich internal coverage" rather than the section's nominal ≥3-epic threshold. The orthogonal framings cover different failure-mode classes (BH: regex / multi-marker / Windows portability; ECH: BOM / substring collisions / tmp-filename PID races; AA: AC code-block test gaps / wording precision). — applied in plnret PR: new Cross-epic-patterns row.
- [E5] [high] [docs] **Single-pass-below-500-LoC threshold heuristic confirmed by pln104 — first downstream application.** pln104's actual changeset: 40-line skill body edit + 250-line test fixture = below the substantial-surface threshold from prt E3. Used single-pass review; surfaced 3 MED findings, all fixed in-place. Re-review clean. **First story to apply the prtret cross-epic rule** — validates the heuristic. The threshold isn't arbitrary; below it, single-pass catches the relevant cases without paying the orthogonal-framing premium. — recorded; the rule is now load-bearing for future "is this story substantial-surface?" judgment calls.
- [E6] [high] [docs] **`bmad-create-story` skip cumulative count bumped from 30/30 across 7 epics → 36/36 across 8 epics** (Phase 0 25/25 + mrg 3/3 + prt 2/2 + pln 6/6). The skill change remains user-review-required (`self_healing.user_review_required_for: [skills]`); only the count + reaffirmation list were bumped. — applied in plnret PR: CLAUDE.md "How /devx runs" Phase 2 inline note + the Cross-epic-patterns row both updated.
- [E7] [high] [docs] **Self-review-non-skippable confirmed across Phase 0 + Phase 1's first 3 epics.** pln epic adds 6 confirmations: pln101 (1 HIGH — unconditional branch_prefix default re-introduced regression class for split-branch); pln102 (27 raw → HIGH/MED fixed across 3-agent pass — hash-prefix substring collision, sprint-status `- key:` over-anchoring, tmp-filename PID collision under future Phase 2 parallelism, mkdir-in-try/catch ordering, WARN-leftover-paths visibility); pln103 (27 raw → 11 substantive fixes — CRLF normalization, code-fence tracking, duplicate-spec detection, multi-marker locked-decision splits, greedy body collection clamp, frontmatter quote/comment/empty tolerance); pln104 (3 MED — tokenWarns index lookup hardened, doc-check drift cleaned up, skill-body overclaim softened); pln105 (16 raw → 14 fixed — across all four mode branches and the predicate verbatim assertion); pln106 (16 raw → 14 valid fixed — including BH-HIGH-2 placeholder-vs-real-hash visual concern, addressed via column-position assertion). 8 epics total now. The Cross-epic-patterns row's wording bumps to "spans all 5 shipped Phase 0 epics + the first 3 Phase 1 epics (mrg + prt + pln)." — applied in plnret PR.
- [E8] [high] [docs] **Status-log terseness pattern bumps from 2/3 (sup + ini) to 3/3 (sup + ini + pln) — promoted to cross-epic.** pln has 4/6 stories with terse status logs (pln103, pln104, pln105, pln106 — 3-4 lines each, no per-phase or per-finding-count breakdown), 2/6 rich (pln101, pln102 — full per-phase enumeration). Matches ini's 4/8 mixed; sup was 5/5 uniform omit. The terseness in pln is NOT explained by thin work — pln103/105/106 had substantial implementation (19 LoC, 29 new tests, 39 new tests respectively), and substantial reviews (27, 16, 16 findings) — the rich detail just didn't reach the spec status log. Cross-epic count 3/3 epics; mrg (0/3 omit) and prt (0/2 omit) are positive counterexamples sharpening the corrective. The corrective is a `/devx` skill prompt-card line ("Status-log entries MUST enumerate per-phase milestones AND self-review finding counts — use explicit-zero per `LEARN.md § epic-merge-gate-modes` E7 when applicable; never omit"). `skill` blast-radius → user-review-required → filed as `MANUAL.md MP1.1`. — applied in plnret PR: new Cross-epic-patterns row + MANUAL.md MP1.1.
- [E9] [med] [docs] **Atomic multi-artifact emission with fixed-order rename + WARN-not-rollback (pln102).** Locked decision #7 implemented verbatim in `writeRetroAtomically()`: write all three artifacts (spec / DEV.md / sprint-status.yaml) to `*.tmp` first; rename in fixed order; on any rename failure, prior renames stay committed and partial state is logged WARN with actual on-disk paths + leftover `.tmp` paths so the operator can recover or `git clean -f`. Better partial than zero. Test fixture covers each of the three rename failure points. The pattern is generalizable for any multi-artifact emission downstream — Phase 2 ManageAgent state writes (`schedule.json` / `manager.json` / `heartbeat.json`), Phase 4 mobile-companion offline-queue snapshots, any future linter that emits to multiple files. — pending-concordance: 1 epic. Revisit when next multi-artifact-emission story lands.
- [E10] [med] [docs] **pln103's `validate-emit` was dogfooded mid-epic against all 5 Phase 1 epics.** PR #40 body: "ran `devx plan-helper validate-emit` against all 5 Phase 1 epics — exit 0 across the board; warnings surface real source-of-truth drift between locked decisions and specs, which is exactly what the heuristic is meant to flag." Sister to mrg E11 (CLI consumed by next PR within same epic) and prt E9 (CLI consumed by SAME PR — tightest possible). pln103 sits between: consumed mid-epic against existing inputs that pre-date its existence. Cross-epic count for the dogfood-mid-epic family: mrg + prt + pln = 3 epics. Strong candidate for promotion at next consumer-shipping epic — held back here only to avoid promoting two adjacent patterns in one retro. — recorded; revisit at next epic that ships a CLI consumed within its own boundary.
- [E11] [med] [docs] **pln104 demonstrated valid scope shrinkage when later story discovers earlier core mechanic.** pln104 spec said "Source-of-truth-precedence enforcement at planning time"; the actual implementation surface was 40-line skill body edit + 250-line test fixture. Reason: pln103's `validate-emit` already shipped check #6 (locked-decision-token-missing-from-spec) — the structural enforcement was already in place. pln104's contribution was to document the override flow explicitly + prove the catch mechanism with a before/after fixture. Pattern: when an epic's later story discovers its core mechanic already shipped in an earlier story, the right move is to thin the story to doc + closure + test, not expand it to net-new code. Source-of-truth precedence: shipped-implementation reality > original spec scope. — pending-concordance: 1 epic; revisit when a 2nd story exhibits the same shape. The implementation-shipped-earlier-than-the-story-that-was-supposed-to-ship-it pattern likely recurs in epic-devx-skill (which depends on multiple already-shipped Phase 1 helpers).

#### epic-devx-skill

*All 12 findings below were added by the formal `bmad-retrospective` pass run during the dvxret PR. Like mrgret/prtret/plnret, this section had no hand-extracted entries to reconcile against — it was placeholder-empty at retro start. The BMAD-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-devx-skill-retro-2026-05-07.md`. dvxret is the FOURTH Phase 1 retro and the FIRST retro to flow through ALL of mrg102 (`devx merge-gate`), prt102 (`devx pr-body`), pln102 (`emitRetroStory()`), pln103 (`validateEmit()`), AND every dvx CLI primitive (`claim`, `should-create-story`, `await-remote-ci`) end-to-end on its own bookkeeping flow.*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 9th confirmation.** audret + cfgret + cliret + supret + iniret + mrgret + prtret + plnret + dvxret = 9/9 retros to date. **Structurally closed by pln102** (`emitRetroStory()` co-emits all three artifacts). dvxret was emitted by `/devx-plan` on 2026-04-28 *before* pln102 shipped, so it required the standard mechanical backfill in this PR. **AC #7 verdict — re-evaluation:** pln102's machinery DOES eliminate manual backfill for retros emitted by `/devx-plan` after pln102 merged. dvxret is the **second-to-last** retro requiring manual backfill (mgrret will be the last unless the planner re-emits it post-merge). The corrective shipped at pln102 (MP0.2 closed); legacy-emitted retros still require backfill (one more to go). Cross-epic-patterns row count bumped from 8/8 → 9/9 retros. — applied (mechanical backfill of dvxret + epic-devx-skill done flips in this PR).
- [E2] [high] [docs] **Dogfood-mid-epic — promoted to cross-epic with three-shape taxonomy.** mrg E11 (consumer-ships-runs-on-next-PR within epic — mrg103 consumed mrg102's CLI), prt E9 (consumer-ships-runs-on-SAME-PR — prt102's `devx pr-body` rendered prt102's own PR body), pln E10 (consumer-ships-runs-on-already-existing-inputs mid-epic — pln103's `validate-emit` ran on all 5 Phase 1 epics) — all 3 epics had **single-shape** instances. dvx ships **all three shapes simultaneously**: (a) dvx101's `claimSpec` was the first operation on every dvx102–dvx107 claim; (b) dvx105 + dvx106 each gated their own PR's merge with the very CLI they ship; (c) dvx107 ships `parseHandoffSnippet` test-only-lock with NO production consumer (validator pins skill-body prose shape). 4 epics × richer shape variance. **Promoted to Cross-epic patterns this PR with the three-shape taxonomy explicit**, so future epics know which shape applies. The test-only-lock shape (sub-pattern (c)) is sister to mrg103's dead-code-with-tests but for prose, single-epic at promotion — pending-concordance separately for that sub-shape (revisit when a 2nd skill-body prose section needs equivalent locking). — applied in dvxret PR: new Cross-epic-patterns row.
- [E3] [high] [docs] **Pure-fn + CLI-passthrough trio — concordance bumped to 4 epics with 6 fresh dvx instances + 2 healthy variants.** Already promoted at plnret (mrg + prt + pln = 3 epics). dvx adds 6 fresh instances: dvx101 `claimSpec` + `devx devx-helper claim`; dvx102 `shouldCreateStory` + `devx devx-helper should-create-story`; dvx104 `coverageTouchedGate` + `parseOptOutMarkers` (library-only, no CLI passthrough yet — skill-body prose dispatch); dvx105 `probeRemoteCi`/`awaitRemoteCi` + `devx devx-helper await-remote-ci`; dvx106 `deriveMergeAdvice` extension to `merge-gate.ts` (variant: extension to existing CLI rather than new module); dvx107 `parseHandoffSnippet` + tests (library-only, validator-style — no CLI). Two healthy variants identified: **library + tests, no CLI yet** (appropriate when consumer is skill-body prose dispatch or test-only lock) and **extension to existing CLI** (appropriate when new logic is enrichment of existing decision surface, not a new domain). — applied in dvxret PR: Cross-epic-patterns row reinforced + variant guidance added.
- [E4] [high] [docs+skill] **Externalize behavior-as-CLI-primitive — concordance bumped to 4 epics.** Already promoted at plnret. dvx adds 4 fresh externalizations: dvx101 claim → `devx devx-helper claim` (replaces inlined git+fs sequence); dvx102 should-create-story → `devx devx-helper should-create-story` (replaces inlined inference in Phase 2); dvx105 await-remote-ci → `devx devx-helper await-remote-ci` (replaces inlined gh-run-list state machine in Phase 7); dvx106 advice routing — REMOVES per-mode "Behavior by mode" table from skill body Phase 8 entirely. Skill body's Phases 1, 2, 5, 7, 8 are now CLI-driven prose ("invoke the right CLI, parse JSON, react to exit code + payload"). The regression class "skill body says X, code says Y" is closed at every phase except Phase 3 (LLM-driven implementation) and Phase 6 (git-prose). — applied in dvxret PR: Cross-epic-patterns row reinforced.
- [E5] [high] [docs] **3-agent parallel adversarial review — concordance bumped to 3 epics with rich coverage.** Already promoted at plnret (prt + pln = 2 epics, 5 internal observations). dvx adds 3 fresh substantial-surface confirmations (dvx101: 11 actionable; dvx105: ~18 across 2 passes; dvx106: ~40 raw) + 2 single-pass below-threshold confirmations (dvx103: 2 findings on 290-LoC; dvx104: 1 LOW on 211-LoC core) + 1 environmental-fallback story (dvx107: 2-agent pass — bmad-edge-case-hunter agent unavailable in environment, surfaced 1 MED + 6 LOW including load-bearing phase9Body extractor bound). Cross-epic count now 3 epics (prt + pln + dvx) matching the section's nominal ≥3-epic threshold. The threshold heuristic from prt E3 / pln E5 holds — single-pass found everything load-bearing on dvx103+dvx104; 3-agent caught semantics on dvx101/dvx105/dvx106 that single-pass would have missed. — applied in dvxret PR: Cross-epic-patterns row reinforced with dvx data.
- [E6] [high] [docs] **`feedback_gh_pr_merge_in_worktree.md` empirically confirmed twice in dvx + structurally pinned in skill body + tests.** dvx106 PR #50 + dvx107 PR #51 both hit the regression class verbatim ("Merge command exited non-zero from worktree (`fatal: 'main' is already used by worktree`)") and both verified via `gh pr view <#> --json state,mergeCommit` returning `{"state":"MERGED","mergeCommit":{"oid":...}}`. dvx106 explicitly pins the contract in skill body Phase 8 ("**`gh pr merge` invoked from inside a worktree commonly exits non-zero while the remote merge actually succeeds** — never trust the gh exit code alone. The verify is authoritative.") + `test/devx-skill-phase8-discipline.test.ts` asserts the discipline. The auto-memory note now has skill-body + test backing — it can't regress silently. — applied (recorded as a structurally-pinned regression class).
- [E7] [high] [docs] **Status-log discipline: dvx is a clean 7/7 positive counterexample.** Already promoted at plnret with sup 5/5 + ini 4/8 + pln 4/6 = 3/3 epics confirming the corrective-needs-promotion threshold; mrg 0/3 + prt 0/2 = positive counterexamples. dvx adds **0/7 omit** — every story enumerates per-phase milestones, self-review finding counts (HIGH/MED/LOW), dogfood references, fix-forward outcomes, merge SHAs. Variance now reads as run-style polarized: mrg + prt + dvx (0% omit) vs sup + ini + pln (mixed). MP1.1 (the skill prompt-card edit requiring per-phase + finding-count enumeration) remains the corrective; dvx's 7/7 confirms the rich shape is achievable in practice but the prompt-card change is what prevents future regression. — applied in dvxret PR: MP1.1 reaffirmed (no closure yet).
- [E8] [high] [skill+config] **Self-review-non-skippable confirmed across Phase 0 + Phase 1's first 4 epics.** dvx adds 7 confirmations: dvx101 (3 HIGH + 5 MED + 3 LOW = 11 actionable across 3-agent pass — push-target/worktree-base split for split-branch, rename-rollback generalized, openExclusive partial-write unlinks before rethrow); dvx102 (canary-of-skip recorded — explicit decision logged per AC #5); dvx103 (1 HIGH + 1 MED on 290-LoC single-pass — verbatim AC1 phrasing + status-log section regex EOF bound); dvx104 (1 LOW on 211-LoC single-pass — editorial-vs-imperative phrasing); dvx105 (~18 across 2 3-agent passes — pinned headSha at awaitRemoteCi start via `git rev-parse <branch>` not HEAD, coerceGhRun strictness, maxPolls/pollMs production-safety guards); dvx106 (~40 raw across 3-agent — exit-2-emits-decision-without-advice for transient gh outages, cancelled/action_required → MANUAL not WAIT, filter-fallthrough preserves explicit gate advice); dvx107 (1 MED + 6 LOW across 2-agent fallback — phase9Body extractor bound to `^(### |## )/m`). 9 epics total now. The Cross-epic-patterns row's wording bumps to "spans all 5 shipped Phase 0 epics + the first 4 Phase 1 epics (mrg + prt + pln + dvx)." — applied in dvxret PR.
- [E9] [high] [skill] **`bmad-create-story` skip cumulative count bumped from 36/36 across 8 epics → 43/43 across 9 epics** (Phase 0 25/25 + mrg 3/3 + prt 2/2 + pln 6/6 + dvx 7/7). **Structurally closed at the contract level by dvx102** (`shouldCreateStory()` + `_internal.skip_create_story_canary` flag + `devx devx-helper should-create-story` CLI), but canary ships **off** by default — v0 behavior preserved (`bmad-create-story` invoked when no story file present, regardless of helper decision). The empirical pattern continues until the canary flips to `"active"` on one in-flight story (user-review-required per `self_healing.user_review_required_for: [skills]`). The next behavior shift requires either (a) user manually flipping the canary, or (b) `/devx-learn` Phase 5+ proposing the flip via memory entry. — applied in dvxret PR: CLAUDE.md "How /devx runs" Phase 2 inline note + the Cross-epic-patterns row both bumped to 43/43.
- [E10] [med] [docs] **v0 → v1 skill body refinement: every additive PR, no breaking change.** epic-devx-skill plan stated "v0 keeps working through the refinement — every story is an additive PR." Confirmed: dvx101–dvx107 all merged additively. Skill body went from 12-step inlined-prose v0 → CLI-driven 9-phase v1 in 7 PRs without ever leaving the user with a half-broken `/devx`. Each PR exercised the new primitive on the very next claim (or itself, in dvx105/dvx106 same-PR-dogfood). Pattern is **CLAUDE.md "Working agreements"-grade** for any future skill-body refinement epic: ship the helper first, dogfood it on the next PR (or same PR), refine the prose to invoke it, never break v0 in the same PR that introduces the v1 primitive. — pending-concordance: 1 epic. Revisit when next skill-body refinement epic ships (likely `/devx-manage` v0 → v1 refinement post-Phase-2).
- [E11] [med] [docs] **dvx107 ran with degraded reviewer pool (Edge Case Hunter unavailable in environment).** Surfaced 1 MED + 6 LOW across 2-agent fallback (Acceptance Auditor + Blind Hunter); load-bearing phase9Body extractor bound was caught. Risk: future stories may hit the same environmental limit silently. Corrective is twofold: (a) skill body Phase 4 should record reviewer-pool composition explicitly (which agents ran, which didn't, why) — currently MP1.1 covers finding counts but not reviewer identity; (b) `bmad-code-review` skill should surface a structured "reviewers attempted vs reviewers run" report. — pending-concordance: 1 epic; revisit when 2nd story hits the same environmental degradation. If recurs, file as MANUAL.md row asking for a Phase 4 prompt-card line.
- [E12] [med] [docs] **Test-only-lock pattern (dvx107) for skill-body prose — single-epic, pending-concordance.** dvx107's `parseHandoffSnippet()` + 18 tests pin the skill body's Handoff Snippet template prose shape (5 sections + final continue line + AC #4 suppression + Phase 9 dispatch) **without any production consumer**. Same conceptual shape as mrg103's dead-code-with-tests (latent contract) but for prose. Right shape when: (i) artifact is prose in load-bearing prompt file, (ii) consumer is the LLM running the skill, not a TS caller, (iii) prose drift is silent + high-blast. The ~150 LoC validator + 18 tests cost is justified when the prose is consumed by every `/devx` early-stop. Sub-pattern (c) of the dogfood-mid-epic three-shape taxonomy (E2). — pending-concordance: 1 epic. Revisit when a 2nd skill-body prose section needs equivalent locking — likely candidates: skill body's Phase 1 claim-line shape; skill body's Phase 8 advice routing dispatch table; skill body's Phase 7 PR body fallback template. *Amendment (mss104, 2026-07-29): the pattern outlived its first exemplar. The Handoff Snippet prose it pinned was retired by workstream mid-story-split — early halts now route to `devx split`, so the remainder is a first-class spec instead of prose to validate. The lock did NOT go away with the prose: `test/devx-skill-phase9-split.test.ts` is the successor exemplar, carrying `phase9Body()` forward verbatim and pinning the `devx split` invocation + both shape-routing sentences + a grep-zero assert on the retired tokens. Second data point for the sub-pattern, and a refinement to it: when the pinned prose is itself replaced, port the lock to the replacement in the same PR — a retirement that deletes the validator without a successor silently un-pins the phase.*
- [E13] [high] [skill+code] **Fresh /devx session walked into another live session's worktree.** dvxret itself, 2026-05-07: a fresh /devx invocation post-/clear ran with no `<hash>` arg, pulled `dvxret` from `DEV.md` (status `in-progress`, `[/]`), saw `.worktrees/dev-dvxret/` exist, and assumed "this is my prior session resuming." It was actually another live `/devx-2026-05-07T0842-98247` session's claim — same project, different conversation. Three independent signals were available and ignored: (1) `.devx-cache/locks/spec-dvxret.lock` exists with the owning session token; (2) spec frontmatter `owner:` field carries a session token the resuming agent never minted; (3) spec status log line "claimed by /devx in session <token>" names a session the resuming agent doesn't recognize. The fresh agent only avoided collision because the user interrupted before any worktree edit landed. **Failure mode under Phase 1 (single-agent):** bounded — typically the user catches the mistake before any commit lands; in this incident the worktree was untouched. **Failure mode under Phase 2+ (ManageAgent + parallel workers):** structural correctness gap — two unattended workers will both think they own the same spec, both run Phase 3 implement on the same worktree, both produce conflicting commits. The `/devx` skill body's Phase 1 only handles the fresh-claim path (via `devx devx-helper claim`); there's no equivalent "verify-this-is-my-claim" branch for the resume path, and CLAUDE.md "How /devx runs" §1 only describes the fresh-claim case. **Structural fix shape:** (a) add `devx devx-helper verify-claim <hash>` (or extend `claim`) to compare the lock file's owner token to the current session's token; on mismatch emit JSON `{error: "owned-by-other-session", lockOwner, currentSession}` + exit 3; (b) `.claude/commands/devx.md` Phase 1 adds an explicit resume-detection step: "if spec status is already `in-progress`, run verify-claim first; on owner-mismatch halt without touching the worktree and surface `<spec> is owned by session <X> — resume in that session or wait for the lock to release`." Skill-body change is user-review-required per `self_healing.user_review_required_for: [skills]`; the CLI primitive can ship as a regular dev story. — **filed as new dev spec `dev/dev-roc101-2026-05-07T08:50-devx-resume-owner-check.md` in this PR (blocked-by dvxret merge; slotted as a standalone follow-up under epic-devx-manage-minimal since it's load-bearing for mgr104's worker-spawn discipline). Stopgap docs change applied in this PR — see CLAUDE.md "Working agreements" "Verify claim ownership before resuming" bullet.**

#### epic-devx-manage-minimal

*All 14 findings below were added by the retro pass run during the mgrret PR. Like every Phase 1 retro, this section was placeholder-empty at retro start. The house-shaped retro file lives at `_bmad-output/implementation-artifacts/epic-devx-manage-minimal-retro-2026-07-05.md`. mgrret is the FIFTH and final Phase 1 retro — it closes Phase 1 at 5/5 epics — and the **FINAL BMAD-era retrospective**: the project migrates off BMAD per `v2/01-bmad-capture.md`, so higher-blast findings are marked `filed-as: v2 backlog` (the migration absorbs follow-ups; no new spec files emitted by this retro).*

- [E1] [high] [docs+config] **Retro stories absent from `sprint-status.yaml` — 10th and FINAL confirmation.** audret + cfgret + cliret + supret + iniret + mrgret + prtret + plnret + dvxret + mgrret = 10/10 retros. mgrret was emitted 2026-04-28, pre-pln102, so it required the standard mechanical backfill — exactly as dvxret predicted ("mgrret will be the last"). This PR flips `mgrret` → done + `epic-devx-manage-minimal` → done. **This is the last time sprint-status.yaml is touched**: `v2/01-bmad-capture.md` verdicts the file (409 lines, recurring drift-bug class MP0.1/MP0.2, zero consumers) as retired by the v2 migration. Cross-epic row bumped 9/9 → 10/10 + closed. — applied (final mechanical backfill in this PR).
- [E2] [high] [docs+code] **Atomic state writes via tmp+rename — promoted to cross-epic (the spec's AC, confirmed at 4 epics).** sup × 4 (`writeAtomic` in `supervisor-internal.ts` across stub/launchd/systemd/task-scheduler installers) + ini505 (consumes `installSupervisor()`) + pln102 (`writeRetroAtomically()` fixed-order multi-artifact variant — was pln E9 pending-concordance) + mgr102 (`state.ts` extends the primitive to mutating tick state; crash-mid-write tmp recovery tested both directions: promote-when-parent-missing + ignore-and-`.corrupt`-quarantine-when-poisoned). Key split surfaced by mgr102's Acceptance Auditor: the sup pattern has two halves — **tmp+rename atomicity (universal; the load-bearing half)** and SHA-256 content-hash idempotency (install-only; meaningless for mutating state where every write changes content). mgr102's AC cited the SHA-256 wording decoratively; the implementation extracted the applicable half and the AC was annotated in-place. The promotion covers the atomicity half; SHA-256 stays in the iniret idempotency row. — applied in mgrret PR: new Cross-epic-patterns row.
- [E3] [high] [code+config] **Typecheck-only CI failure class hit twice in one epic (mgr102 + mgr104) — local gate structurally aligned.** mgr102 PR #54 first CI run failed at `npm run typecheck` (`as Record<string, unknown>` overlap in a test file); mgr104 PR #56 failed the same step (`Parameters<typeof runManagerOnce>[0]["spawnFn"]` indexing possibly-undefined). Root cause: remote CI runs `tsc --noEmit` over tsconfig.json (includes tests); local `npm test` builds via tsconfig.build.json (excludes tests). mgr103 + mgr106 ran typecheck manually in-session — masking, not closing. — applied in mgrret PR: `package.json → scripts.test` now runs `npm run typecheck` before vitest; this retro PR is the first under the aligned gate.
- [E4] [med] [docs] **Phantom DEBUG filing — 2nd instance of the cliret E3 class.** mgr104's status log claims the typecheck gap was "filed as DEBUG entry post-merge" — DEBUG.md is empty; nothing was filed. Same class as cli301's phantom `debug-flow01`. Reconciled: superseded by E3's structural fix in this PR (corrective shipped; no retroactive filing). Class count: 2 confirmed instances (cli301 + mgr104). The v2 engine's status-line discipline should validate filed-as references at write time. — applied (superseded marker) + filed-as: v2 backlog (write-time validation rule).
- [E5] [high] [docs] **3-agent parallel adversarial review at 6/6 stories — first all-3-agent epic; concordance bumped to 4 epics.** Every mgr story crossed the substantial-surface threshold (~430–560 LoC production surfaces: locks, state machines, spawn plumbing, multi-regex parsers). ~97 unique actionable findings epic-wide (mgr101: 14 — 3H/8M/3L; mgr102: ~21 — 5H/8M/7L; mgr103: ~28 unique — 5H/11M/12L, AA 7/7 PASS; mgr104: 6 — 2H/3M/1L; mgr105: 19 — 4H/6M/9L, AA 7/7; mgr106: ~9 + 2 AA literal-wording NOTEs). ALL in-scope fixed in-place; every re-review clean. Load-bearing semantics every time: loop.ts roster-overwrite race resurrecting a dead PID (mgr104); empty/whitespace lock content treated as conservatively held — closes the open→write race that could produce two managers (mgr106); fence-aware backlog parsing fixing a real INTERVIEW.md misparse (mgr103); `appendInterviewRow` EACCES-wipe that would have clobbered INTERVIEW.md (mgr105). — applied in mgrret PR: Cross-epic-patterns row reinforced (prt + pln + dvx + mgr).
- [E6] [high] [docs] **Deferred-findings-with-forward-tagging as scope discipline.** mgr101's review tagged 7 out-of-scope findings routed **by hash to named sibling stories** (atomic-write recovery → mgr102; manager-crash restart → mgr105; stale-PID/PID-recycling → mgr106); mgr102 deferred 3 with per-finding rationale; mgr103 deferred 12; mgr105 accepted 2 race classes "mgr106-blocked" with code comments. The later stories actually landed the deferred fixes (mgr106's empty-lock-held + stale-PID reclaim covers mgr101's EC deferrals). Fix-forward discipline compatible with "don't expand scope": deferrals explicit, hash-addressed, auditable at retro. — pending-concordance: 1 epic (4 internal observations); v2 review-tour design should adopt the shape natively.
- [E7] [med] [docs] **Real-file smoke tests catch what fixtures don't.** mgr103's parser run against the repo's real INTERVIEW.md surfaced the code-fence example row (line 193, `- [x] Q#7 ...` inside a fence) parsing as a real answered question — doubling question count + emitting spurious unblock directives per tick. Fix: `blankFencedLines()`; real-file question count 9 → 8. Fixture-only tests would never have carried the poisoned example. Pattern: parsers of living repo files need ≥1 test against the actual file. — pending-concordance: 1 epic.
- [E8] [med] [docs] **Single-day epic: 6 stories in ~7h45m — fastest multi-story epic by wall-clock.** mgr101 claimed 2026-05-07T09:15; mgr106 merged ~17:00 local the same day (hours after dvxret unblocked the epic that morning). Test growth 1046 → 1309 (**+263 net — largest of any epic**; dvx +255, ini +225, pln +207). Per-story: mgr101 +45, mgr102 +37, mgr103 +56, mgr104 +33, mgr105 +44, mgr106 +48. Includes 3 fix-forward CI-red cycles (2 typecheck + 1 macOS etime). — recorded; Phase 1 cadence endpoint.
- [E9] [med] [docs] **Per-platform deviation with explicit rationale + dedicated test — 4th epic confirmation.** mgr106 CI red on macos-latest only: `ps -o etime=` 1-second resolution made the process-start-time probe return `now()` for a <1s-old process, tripping the PID-recycling cross-check on same-process re-acquire. Fix: 2s grace window in `classifyExistingLock` (subsumes etime resolution + clock jitter; real recycling involves seconds-to-minutes deltas) + 2 regression tests pinning grace bounds. — applied (recorded); Cross-epic-patterns row reinforced (cli + sup + ini + mgr).
- [E10] [med] [docs] **Planner drift handled inline — right-sized sibling of prt E2's halt-and-amend.** mgr101's spec premise was wrong ("Replace `src/commands/manage.ts` stub" — cli302 never created one; AC #5's help-annotation removal was moot). `/devx` recorded an explicit "phase 2 drift note" and delivered the real intent without halting. Bracketing rule with prt101: **halt-and-amend when drift changes what ships; drift-note-and-proceed when it only changes the description of what ships.** — recorded.
- [E11] [med] [skill] **`bmad-create-story` skip count: 49/49 across 10 epics — FINAL count.** mgr 6/6 skipped. Noted: per-story running tallies drifted in mgr status logs (mgr102 "44/44", mgr103 "45/45", mgr105 "44/44" — approximate); the authoritative count is the retro-time roll-up (43 + 6 = 49). The dvx102 canary never flipped and never will — `v2/01-bmad-capture.md`: the machinery "exists solely to bury `bmad-create-story` gracefully"; the v2 engine drops the step entirely. — applied in mgrret PR: CLAUDE.md Phase 2 inline note bumped to 49/49 + marked final.
- [E12] [high] [docs] **dvxret §3.6 revisit ("claimed but Phase 2 never started"): manager-side recovery shipped in-epic; spec-claim half closed mid-retro by roc101 PR #60.** mgr105's PID-recovery on init synthesizes exit events for dead roster PIDs (Dev-lens locked decision — lost exit events recovered); mgr106 reclaims the manager's own stale lock. The spec-claim half (worker claimed a spec then died pre-Phase-2; lock + owner token dangle) is exactly roc101 — which did NOT ship before mgr104, so mgr-epic workers ran `claude /devx <hash>` with no structural ownership check; the dvxret E13 collision class was live for the whole epic under any parallelism beyond hard-cap-1. *Update mid-mgrret (2026-07-05): roc101 SHIPPED via PR #60 (`devx devx-helper verify-claim` + Phase 1 resume-detection wire-up) — merged by a concurrent /devx session while this retro's PR was in its CI cycle; the structural gap is now closed and the v2 dispatcher (`v2/05-dispatcher.md`) inherits the primitive. The sequencing lesson stands: a follow-up named "load-bearing for mgr104" landed two months after mgr104 — ordering-critical follow-ups should block the stories that need them, not trail them.* — closed by roc101 PR #60 (concurrent with this retro; no new spec needed).
- [E13] [med] [docs] **Status-log discipline: 5/6 rich, mgr105 partially terse — variance is now intra-epic.** mgr101–104 + 106 enumerate per-phase milestones + finding counts + merge SHAs; mgr105 has a rich phase-4 line but omits phase-5 and phase-7 lines entirely. dvxret characterized the variance as run-style-polarized; mgr shows it inside a single epic/day. MP1.1 stays open; absorbed into the v2 Execute-stage status discipline (`v2/02-engine.md` pins the dvx103-style line format natively). — filed-as: v2 backlog (MP1.1 absorption).
- [E14] [low] [docs] **Locked party-mode decisions shipped verbatim at 4/4 bearing stories + `bmad-retrospective` gap superseded.** mgr101 (PM tick-summary line + exported `TICK_SUMMARY_RE`), mgr103 (Architect backlog-parser extraction to `src/lib/backlog/parse.ts`), mgr105 (Dev PID-recovery + Murat pure `backoffDecision`), mgr106 (Infra PID-recycling cross-check + Murat SIGTERM drain semantics) — all delivered; mgr106's AA noted 2 literal-wording mismatches (Promise-vs-sync, platformDetect-vs-defaultDetectOs) as NOTEs, not blockers. Separately: the recurring "bmad-retrospective is still not formally wired" gap carried by every retro is **superseded** — v2 retires the 63KB skill; the interim discipline IS the retro contract v2 re-homes natively. mgrret is the 10th and final artifact of the interim discipline. — recorded.

### Mobile companion v0.1 (plan: plan-7a2d1f)

#### epic-flutter-scaffold-ios-device

#### epic-github-connection-read

#### epic-bidirectional-writes-offline

#### epic-realtime-updates-push

---

## v2-migration (2026-07-05)

*Added by the FIRST native retro (`/devx` Stage: Retro, D-3 — no BMAD workflow
loaded), run inside v2o101 (V2.6) across the nine merged migration items:
v2s101 #59, roc101 #60, mgrret #61, v2e101 #62, v2e102 #63, v2x101 #64,
v2t101 #65, v2d101 #66, v2l101 #67. Retro artifact:
`_devx/retros/v2-migration-2026-07-05.md`. Row contract unchanged
(byte-compatible per D-3). Evidence below is reconstructed from spec status
logs + `gh pr view` metadata, verified per claim.*

- [E1] [high] [code+docs] **First real run of a new gate/primitive finds real bugs — 3 clean instances this migration, promoted to Cross-epic.** v2e102: the first real `devx gate prd` run on the v2x101 workstream surfaced the parseExpectations wrapped-field folding bug (a v2e101 parser defect its ~200 fixture tests never hit — house 78-col wrapping only exists in real artifacts); v2d101: the first real-repo `devx next` run surfaced 13 pre-existing drift defects (12 v1-era PLAN.md status-vocab mismatches + 1 vantage bound: worktree-local `.devx-cache` hides main's locks); v2t101: building the tour of its own PR's diff exposed slot literals being re-substituted by chained split/join (fixed to single-pass tokenized substitution). Same class as mgr E7 ("real-file smoke tests catch what fixtures don't", 1 epic at mgrret) — that sibling plus these three crosses the ≥3 threshold. — applied: new Cross-epic patterns row below.
- [E2] [high] [code] **Latent-until-unattended defects surface only when reviewed under the unattended frame.** v2l101's 3-agent pass found dvx101's `claimSpec` push-failure rollback running `git reset --hard HEAD~1` in the MAIN worktree (destroys unrelated user WIP repo-wide) — shipped 2026-05-05, harmless for ~2 months because an attended user would notice; catastrophic the first unattended night. The regression test for the fix then caught a SECOND instance in the same primitive (bare `git commit -m` sweeping user-staged files into the claim commit). Lesson: when a primitive built for attended flows is inherited by an unattended consumer, re-review it under the "nobody is watching" frame — the original review's severity model no longer applies. — pending-concordance: 1 epic (2 internal observations in one primitive); revisit when the next attended→unattended inheritance ships (ManageAgent parallelism is the natural candidate).
- [E3] [med] [docs] **The critique step's grounding rule paid for itself on its first live run.** v2e102's 4-lens plan critique (re-homed party-mode) accepted 8 findings including 1 HIGH: the E-2 eval named a wrong schema path — caught at plan time, before RED, where it would have made the eval vacuously pass. 2 lens findings were rejected WITH rationale in `decisions/2026-07-05-plan-critique.md` — the reject-with-rationale shape kept the critique advisory rather than gating (as designed, §4.5). Contrast: BMAD party-mode's aud-era record includes personas naming nonexistent paths (aud E3); the v2 grounding rule ("ground discussion in real code") is the corrective, and its first outing caught exactly a path-reality bug. — recorded; 1 run. Revisit after ~3 more critique runs.
- [E4] [high] [code] **Version-skewed argv semantics are a hang-immunity blind spot.** v2l101's only CI fix-forward: `node -p <prompt> -e <script>` last-flag-wins parsing differs between node 24 (local) and node 20 (CI) — both grace-kill tests hung 15s on both CI platforms. The loop itself would have hit the same class at 3am. Corrective shipped in-item (spawnFn seam injecting `node -e` directly, keeping real-process properties). Lesson: hang-immunity claims must be validated on the OLDEST supported runtime, not the dev machine's; anything that builds argv for a child process is version-sensitive surface. — applied (v2l101); recorded here for the class.
- [E5] [med] [code+docs] **Suite duration + test isolation are now real constraints.** Full suite grew from tens of seconds (Phase 0/1 era; the ini508 e2e harness ran 7 scenarios in ~26s) to 391–679s wall across two same-day v2o101 runs (~6.5–11 min, load-dependent; ~700s cpu), dominated by v2l101's real-git timing tests. One flake observed when two vitest runs raced in the same worktree (background agent's final suite ∥ coordinator's) — re-runs clean ×2. Implication for the coordinator/worker delegation pattern: a session must not fire-and-forget a full-suite run while another session runs one in the same worktree — serialize, or split worktrees. — pending: debug spec if the race recurs (v2l101 status log carries the same watch item); duration is a candidate for test-tier splitting (fast/slow) — the ~10 min line is already crossed under load.
- [E6] [high] [docs] **Drift detection's REPORTED-never-fixed posture validated on day one** — 12 of the 13 first-run defects were v1-era PLAN.md status-vocab mismatches that had survived every prior loop unnoticed; the 13th was a real design bound (worktree vantage) that got documented rather than papered over. The V2.6 "backlog hygiene" bullet is delivered structurally by this reporting (drift IS a reported defect on every `devx next` run); the 12 legacy rows remain open reconcile chores surfaced on every run until fixed. — applied (v2d101 shipped the mechanism); the legacy-row cleanup is deliberately left to explicit reconcile passes, not silent fixes.
- [E7] [med] [docs] **The whole migration ran in one calendar day (~7.5h, 9 PRs, V2.0→V2.5)** on the v1 loop per the bootstrap-discipline sequencing principle — the fastest phase-per-hour rate in project history (BMAD-era best: 6 stories/~7h45m for ONE epic-sized phase). Enablers visible in the logs: worker/coordinator split (CLI scope in a worker, `.claude/` skill edits user-foreground in the coordinator, per the harness permission gate), 3-agent review parallelism, and the phases' strict dependency chain eliminating coordination overhead. +665 net tests (1,309 → 1,974), net −247K lines (the BMAD ejection). — recorded; cadence baseline for v3-scale work.
- [E8] [high] [docs+config] **S-1 recorded honestly: planning surface 24,426 B (~23.9 KB — inside the 60 KB budget); full run incl. `devx.md` 65,767 B (~64.2 KB — ~7% over the end-to-end target, ~88% under the ~550 KB BMAD baseline).** The overshoot is `devx.md` carrying six arms (dispatch/execute/debug/address/retro/loop) where BMAD's ~48 KB covered execute alone; each §6 per-stage target is individually met. CI keeps gating the planning surface at 60 KB; a 2×-budget drift tripwire on the full surface landed in `test/engine-prose-budget.test.ts` (v2o101) so growth can't be silent. — pending [user]: whether to split `devx.md`'s arms into lazy-loaded stage files or raise `engine.prose_budget_kb` to ~70 with rationale; neither decided in code (no-silent-product-decisions).
- [E9] [med] [docs] **First real outcome verdict shipped through the loop it closes (dogfood, shape-(b) tightest):** v2o101 armed v2x101 (`--measure-by 2026-08-02`) and scored it early — verdict **keep, 3/3 goals hit** (G-1: 0 live BMAD refs via E-1 eval exit 0; G-2: `engine:` block validates via E-2; G-3: 1,974 ≥ 1,571 tests) — in the same PR that ships `devx outcome`. Early scoring (before measure_by) was the right ergonomic call for deterministic evals; the due-date only gates *nagging* (`devx next` row 5.5), not scoring. `_devx/workstreams/execute-rehome-bmad-eject/RESULTS.md` is the first RESULTS artifact. — applied.
- [E10] [low] [docs] **Review-pattern roll-up: ~111 unique actionable findings across 9 items, all fixed in-place, every re-review clean.** 3-agent shape on every substantial surface (v2x101/v2t101/v2d101/v2l101×2), single-pass on the two below-threshold items (v2s101: 2; roc101: 1 HIGH) — the CLAUDE.md threshold heuristic needed no amendment across the largest single-day volume to date. The 3-agent and 4-lens-critique mechanisms composed without overlap (critique = plan-time, review = diff-time). — recorded; reinforces the existing Cross-epic 3-agent row without needing changes to it.

---

## epic-harness-fold-in (2026-07-26)

*Added by hfiret — the first retro over an epic the overnight loop helped
implement (the loop shipped hfi101 + hfi104 end-to-end, implemented hfi102 to
~done before abandoning it at iteration budget, and wedged on hfi103 via an
infra misclassification; hfi103 + hfi105 shipped interactively). Retro
artifact: `_devx/workstreams/harness-fold-in/RETRO-2026-07-26.md`. Evidence
reconstructed from the five spec status logs, `gh pr view` metadata, and the
three `.devx-cache/loop/` morning reports, verified per claim.*

- [E1] [high] [code+skill] **The loop merge tail broke the dvx103 status-log discipline contract on first contact.** Every loop-shipped spec landed without a `phase 4:` line, redding `test/devx-status-log-discipline.test.ts` on main at the next branch cut (hfi101/PR #80 introduced it; hfi102's iteration 1 inherited the red; hfi104's iteration-1 learning *predicted the recurrence* before the fix existed and appended hfi101's retroactive line in-item). Structural fix mid-epic: debug-cf65aa (PR #82) — the merge tail now emits the line. — applied (PR #82); feeds the Cross-epic "unattended contact" promotion below.
- [E2] [high] [code] **Verification parking abandoned a done item.** hfi102's loop workers fired the ~7-min full suite and reported before observing it — iterations 6–8 each re-ran the same 3-agent self-review while "suite still in flight," exhausting the 8-iteration budget; the interactive revival found all 5 ACs implemented and the suite green (2253/2253 post-merge-with-main), so the abandonment cost a re-claim, not rework. Structural fix mid-epic: PR #82's iteration contract (never end with verification outstanding). Reinforces `LEARN.md § v2-migration` E5 (suite duration + isolation as real constraints — 2nd retro; the ~7-min suite is now an active shaper of loop iteration design, not just a wall-clock annoyance). — applied (PR #82); feeds the promotion below.
- [E3] [high] [code] **Infra failures were charged to the item.** loop-2026-07-24T21-19 counted 3 hung worker sessions (~32 output tokens total, machine asleep, 60-min ceiling kills stretched to 2–5.5h wall-clock) as 3 item strikes and abandoned hfi103 into wrong state: `blocked` + dead owner + DEV.md status-text drift + a bookkeeping-only preserved worktree — one hung item wedged the whole backlog. Structural fix mid-epic: debug-dc7514 (PR #84) — `infra-error` iteration class aborts the *run* not the item, abandon-path state hygiene, sleep-aware ceiling. — applied (PR #84); feeds the promotion below.
- [E4] [med] [code] **The iteration budget can't tell verification tail from stall.** hfi102 spent 5 iterations implementing and 3 re-verifying, then was abandoned as "8 iterations without acs_met" while effectively done. The budget's unit treats a verification-tail iteration identically to a no-progress iteration. Partially addressed by PR #82 (the tail shouldn't recur if workers park on their own suites); a `done-pending-verification` terminal state remains unbuilt. — pending: watch for budget-exhausted-while-done recurring before adding machinery.
- [E5] [high] [docs] **Abandon-preserves-work + morning-review "claims not verdicts" validated twice.** hfi102's revival was merge-main + full suite + PR (zero rework); hfi103's preserved worktree was correctly read as bookkeeping-only and discarded in one look. The post-run reconciliation discipline (reconstruct from disk, verify merges via `gh pr view`) read both states correctly on the first pass. — recorded.
- [E6] [med] [docs] **Two phase-4 evidence shapes now exist, both truthful.** Loop-shipped below-threshold items (hfi101, hfi104) carry retroactive `phase 4:` lines citing per-iteration verification (evals + full suite) in lieu of an interactive pass; substantial-surface items got real 3-agent passes — including hfi102's, run *by loop workers* across iterations 6–8 (first unattended 3-agent pass on record). hfi103 (7 unique: 2 MED, 5 LOW) + hfi105 (8 unique: 3 MED, 5 LOW) reaffirm the interactive shape catches real semantics bugs (drift-aggregate blind spot; nudge non-restatement pin). Reinforces the Cross-epic 3-agent row (update appended there). — recorded.
- [E7] [med] [code] **Red main is a multiplier on unattended confusion.** The hfi101-induced discipline red sat on main ~a day; four separate hfi102 iterations re-derived "that failure is baseline, discount it," and hfi104 fixed main's red from inside an unrelated item because its own suite couldn't go green otherwise. A loop run that starts with red main taxes every worker with re-learning the baseline and blurs the acs_met signal. — filed-as `dev/dev-lpf101-2026-07-26T15:57-loop-preflight-main-health.md` (loop preflight: probe main's health at run start; flag or refuse when main is red).
- [E8] [med] [memory] **The harness permission gate did NOT fire for a detached loop worker writing `.claude/commands/`.** hfi104's worker wrote `.claude/commands/devx-learn.md` unattended with no prompt — refining `project_skill_perms_block_subagents` (gate observed in interactive sessions + Task-tool subagents; the detached `claude` spawn is a third context that did not gate). One observation; possibly version-dependent. — applied (memory file updated with the refinement); Phase 2+ ManageAgent design should still assume the gate until a second observation confirms.
- [E9] [med] [code] **Loop token accounting is implausibly low — the budget rails don't measure.** The productive 1h50m run self-reported ~11.5k in / ~9.3k out across 13 iterations that produced +2,861 diff lines; a single real worker iteration exceeds that alone. Consequence: the 2M/item and 10M/run budgets can never trip — unattended safety rails that don't measure aren't rails. — filed-as `debug/debug-494590-2026-07-26T15:57-loop-token-accounting.md`.
- [E10] [high] [docs] **Promotion: the unattended-frame class crossed the rich-2-retro bar** (v2-migration E2's pending-concordance — 2 observations in one primitive — plus this epic's E1/E2/E3 = 5 observations across 2 retros; iniret promotion precedent). — applied: new Cross-epic row "Attended-era contracts break on first unattended contact" below.

---

## multi-loop-concurrency (2026-07-29)

*Added by mlcret — the retro over the epic that made N concurrent `devx
loop`s safe by construction, and which became its own first real
concurrency test: `mid-story-split` (mss101–mss104) executed interleaved
with it across 2026-07-28→29 — eleven PRs (#91–#101) merged in ~24h across
the two epics, a concurrent `/devx-learn` run, and a third session live on
`lpf101`. Retro artifact:
`_devx/workstreams/multi-loop-concurrency/RETRO-2026-07-29.md`. Evidence
reconstructed from the six spec status logs, `gh pr view` metadata for PRs
#91–#100, `git log` merge timestamps, and the live `.devx-cache/` +
`dist/` contents; where a status-log claim disagreed with the clock or the
filesystem, the filesystem won (E5). Shipped: 6 stories, +8,388/−794
across 70 files, ~88 unique actionable review findings (10 HIGH), all
fixed in-place; 7 of 8 expectations verified (E-7 outstanding).*

*Supersedes the three `/devx-learn` candidate rows (C1–C3, PR #92) filed
over the mlc101 session: C1 and C2 are promoted below as E8 and E9; C3's
concordance is folded into the Cross-epic "3-agent parallel adversarial
review" row.*

- [E1] [high] [code+skill] **The after-merge bookkeeping tail is still
  inline git prose, and it broke on first concurrent contact — the one
  hazard this epic did NOT convert into a primitive is the one that
  failed.** mlc106's mark-done commit `ac0ccf2` was staged with `git add
  -A` and swept in two files owned by the concurrently-live mss104 session
  (its phase 2/3 status-log lines + `_devx/workstreams/mid-story-split/
  todo.md`). Content landed intact, so the damage was attribution not
  loss, and reverting was correctly declined (it would have deleted a live
  peer's work from main); recorded as an erratum in `ba3c65b`. The
  violated contract is stated *twice* — `CLAUDE.md` Working agreements and
  `.claude/commands/devx.md` Phase 6, both saying "stage only files
  relevant to this item, never `git add -A`" — and being stated twice did
  not help, because prose is re-read under time pressure whereas a
  primitive cannot be called wrong. **It was not the only tail defect in
  the window**: mss103 appended its phase 2/3/4/5 status lines *after* the
  spec's `## Links` heading, and since the dvx103 discipline test bounds
  its scan to the `## Status log` section, main went red for every PR
  branched off it — mlc106 inherited that red on its first CI run and
  burned an investigation cycle proving it came from neither its own diff
  nor its own branch (repaired on main in `fb7561f`). Three distinct
  bookkeeping-tail defects in one window — unscoped stage, misplaced
  append, skipped lock release (E3) — against **zero** defects in the paths
  this epic converted to code. That ratio is the finding. The
  counterfactual is inside the same
  epic: every hazard that WAS converted (claim push race → `claimSpec`
  rebase-retry; backlog mutation → `withBacklogLock`; spec lock →
  `acquireSpecLock`) held silently across two live sessions. *Concurrent
  corrective, **merged mid-retro as PR #102** (`8bd514e`):* a peer
  `/devx-learn` session shipped the prose+test half while this retro was
  being written — an explicit-pathspec staging rule in Phase 8 of both
  skill-body copies, `devx-skill-phase8-discipline` assertions pinning
  `never `git add -A`` in **both** Phase 6 and Phase 8, plus two adjacent
  repairs this retro had NOT spotted: Phase 8's after-merge list was
  **misnumbered** (1,2,3,4,5,7,8,9) with its commit step citing a range
  `(4-6)` whose upper bound did not exist, and the workstream `todo sync`
  step was missing outright. Their note independently records the sweep as
  having happened **twice**. The two correctives are complementary: theirs
  makes the rule un-droppable as prose and fixes a genuinely broken step
  list; b931a1 removes the reliance on prose and covers the three defects
  neither touches (E2, E3, E5).
  Meta-observation worth keeping: two sessions found the same defect within
  hours — the concurrency model working *and* a coordination cost, since
  neither knew the other was on it; scoped claims (this epic's own mlc106
  deliverable) would have made it legible in `devx next`. — filed-as
  `dev-b931a1` (`devx devx-helper finalize <hash>`); reinforces the
  Cross-epic "Externalize behavior-as-CLI-primitive" row.
- [E2] [high] [code+docs] **Merged is not installed: the epic's headline
  feature is unreachable from the CLI that `/devx` itself invokes.**
  `devx` on PATH resolves to `<repo>/dist/cli.js`; `dist/` is gitignored
  and rebuilt by `npm run build` — and a story's local-CI gate runs inside
  its **worktree**, so it builds the worktree's `dist/`, never main's.
  Main's `dist/cli.js` was last built 2026-07-28 17:04 (four minutes after
  mlc105 merged, ~16h before mlc106 did). Verified at retro time on main:
  `devx loop --help | grep -c epic` → `0`, and `dist/lib/loop/scope.js`
  does not exist. mlc106 — the epic's largest story (+2,534) carrying the
  entire scope model — is shipped, tested, merged, and unreachable; an
  operator running E-7's checklist tonight would type `devx loop --epic
  <slug>` and get commander's `unknown option`, *the exact string E-6's
  RED eval asserted before the feature existed*. This also retro-explains
  the split lock formats on disk (`mlc101`/`mlc103`/`mlc104` legacy
  three-line vs `mlc106`+ JSON v1): mlc103 shipped the JSON body at 12:46,
  mlc104 claimed at 13:22 and still wrote legacy, because the claim runs
  from main's stale `dist/`. For a project that self-hosts its tooling
  this is a correctness surface, not a convenience one — every claim,
  gate, and merge decision between a primitive's merge and main's next
  rebuild runs on the previous build. — filed-as `dev-b931a1` (rebuild in
  the merge tail + `dist`-older-than-`HEAD` staleness warning); sharpens
  the Cross-epic "first real run against the live repo" row.
- [E3] [high] [code] **Spec locks leak at merge — four of six — because
  the interactive tail has no release step at all.** `.devx-cache/locks/`
  still holds `spec-mlc101/103/104/106.lock`, every owner PID dead, every
  spec `done`. mlc102 and mlc105 released theirs and both status logs say
  so explicitly, which is the tell: it happened because those two authors
  thought of it. `/devx` Phase 8's after-merge steps (pull → remove
  worktree → delete branch → update spec → update DEV.md → commit → file
  gaps → check epic) contain no release. The loop driver *does* release
  (mlc103's `ownsClaim` + guarded release); the interactive path never
  did. Harmless but permanent: mlc103's classifier reaps a dead-owner lock
  on the next *contending claim*, and a `done` spec is never claimed
  again, so nothing ever triggers the reap. G-3's letter holds (reapable
  in principle) while the repo accumulates debris the happy path should
  have cleaned. **Three specs now converge on spec-lock release and must
  not duplicate each other**: `dev-ee7049` (filed independently from the
  mss104 gap sweep, `78a10b6`) ships the `devx devx-helper release-lock`
  CLI wrapping `releaseSpecLockGuarded` for Phase 9's raw-`rm` handoff
  path; `dev-b931a1` should **consume** that CLI in the Phase 8 tail rather
  than write its own release; `dev-db36af` (`devx doctor`) sweeps the locks
  that already leaked. Note the pleasing symmetry — ee7049 was filed for
  the one release path that goes *around* the guarded primitive, while this
  row is about the one path that has *no* release at all; between them the
  interactive lock lifecycle is finally closed. — filed-as `dev-b931a1`
  (consume ee7049's CLI in the tail) + `dev-ee7049` (the CLI) +
  `dev-db36af` (offline sweep of already-accumulated locks).
- [E4] [med] [docs+skill] **When the 3-agent shape is environmentally
  unavailable, single-pass under-catches — compensate, don't just note
  it.** mlc105's session harness forbade spawning subagents, so it ran a
  single-pass 3-lens review and recorded the deviation rather than
  silently single-passing (correct discipline, worth saying plainly). But
  the surface was +1,513 across 13 files — well over the substantial
  threshold — and it returned **4** findings where its 3-agent peers on
  comparable surfaces returned 10, 12, 16, ~20 and 26. It did catch a real
  HIGH (a `".."` session token that sanitized to itself, aiming a
  recursive `rmSync` at `.devx-cache`), so this is not a failed review; a
  4-vs-16-median gap is the shape of under-catching, not of a cleaner
  diff. Second recorded instance of environmental unavailability
  (dvx107's 2-agent fallback was the first). Proposed fallback: sequential
  passes with a context reset between them. — pending (feeds the
  Cross-epic 3-agent row's environmental-fallback sub-pattern).
- [E5] [med] [docs+code] **Status-log timestamps are narrative-authored,
  not clock-read.** mlc106's tail reads `00:20` phase 5/6/7 → `00:35` CI
  red → `00:45` merged; the PR actually merged at `2026-07-29T15:24:22Z`
  after a re-run starting `14:53Z`. ~15 hours (an overnight pause + a
  next-morning CI re-run) compressed into 25 minutes of narrative, and the
  timezone silently switched from local `-06:00` — used by every other
  line in the epic — to UTC. Nothing downstream broke; it matters because
  retro evidence *is* the status log, and this retro caught it only by
  cross-checking `gh` and `git log`. — filed-as `dev-b931a1` (the tail
  already writes the merge line; have the primitive stamp it from the
  clock).
- [E6] [low] [docs] **The epic's own plan spec drifted, and `devx next`
  caught it.** `plan-20eb6f` frontmatter said `status: ready` while
  `PLAN.md` marked the row `[x]`; surfaced as a `status-mismatch` drift
  row by the tool rather than found by hand — the drift-as-defect
  contract working as designed. — applied (this PR: frontmatter → `done`,
  PLAN.md row reconciled).
- [E7] [med] [docs] **G-2 closed the epic unverified — the real overnight
  (E-7) was never run, and E2 now blocks it.** All six stories shipped and
  every CI-runnable expectation passed, but the P2 human checklist that is
  G-2's only evidence never executed: `.devx-cache/loop/instances/` is
  empty and the newest loop report predates the epic. An epic about making
  N concurrent loops safe was itself built entirely by concurrent
  *interactive* sessions. The night cannot run until main's `dist/` is
  rebuilt (E2). — filed-as `MANUAL.md MV-mlc.1`; outcome armed to
  2026-08-31 per G-2's own date.
- [E8] [high] [code] **Git worktree classification must use `git-dir !=
  git-common-dir`; any path-shape heuristic (`basename == ".git"`)
  misclassifies submodule and separate-git-dir MAIN checkouts.** mlc101's
  first-cut discriminator would have false-refused `devx loop`/`devx
  manage` and hard-blocked claims (no override) in those layouts; caught
  by the 3-agent review (Blind Hunter, verified against real git), fixed
  pre-merge. — applied (PR #91). *Promoted from candidate C1.*
- [E9] [high] [code] **Canonicalizing a value half-way is worse than
  consistently wrong — grep every consumer of the old default and convert
  them in the same change.** mlc101's first cut passed the canonical
  cacheDir to `acquireManagerLock` but not the tick, creating a NEW
  lock-guards-A/state-writes-B split that did not exist when everything
  was uniformly cwd-relative. Two reviewers flagged it independently;
  fixed pre-merge (cacheDir + cwd plumb, defaults removed from
  `managerLockPath`/`acquireManagerLock`). — applied (PR #91). *Promoted
  from candidate C2.* Note the family resemblance to E2: both are
  "half-migrated state universe" defects, one in a function's argument
  plumbing and one in the build artifact the whole system self-hosts on.
- [E10] [high] [code+docs] **RED-reason discipline paid for itself the
  first time it ran.** mlc101 re-ran its eval, saw it fail, and checked
  *why* — the `_fixture.ts` tsx path did not resolve in linked worktrees,
  so the CLI under test never spawned and the "refusal missing" assertion
  never executed. A red exit code was masking a harness that ran nothing.
  Fixed the infra, re-confirmed an honest RED, then implemented. mlc106
  applied the same discipline forward, capturing E-8's byte-identical
  dry-run baseline *before* touching anything and re-diffing after. — the
  contract shipped mid-epic in PR #92; recorded here as its first
  load-bearing catch.

---

## mid-story-split — candidates (learn 2026-07-29, for mssret) — SUPERSEDED 2026-08-21

*Added by a `/devx-learn` run over the mss104 session (PR #101), user-pruned.
Candidates only — mssret promotes, merges, or strikes these against the full
workstream's evidence; do not treat as promoted rows.*

**SUPERSEDED 2026-08-21 by `§ epic-mid-story-split (2026-08-21)`.** mssret
ran and confirmed both: C1 → F4, C2 → F3. Kept verbatim for audit (the
candidates predate the retro's evidence and record what a session-scoped
`/devx-learn` pass could see on its own).

- [C1] [med] [docs] **When a phase carries a mechanical eval, the eval's scan
  scope — not the AC enumeration — is the real acceptance surface.** mss104's
  ACs 1–4 named the files to sweep, but E-2 greps all of `.claude/commands/`,
  `src/`, `test/`, `docs/`, `v2/`; two stale comment tokens in
  `src/lib/devx/split.ts` kept the eval RED and appeared in no AC. The choice
  at execute time was to widen the sweep or narrow the eval, and narrowing a
  permanent invariant to match an incomplete AC list would have been the
  wrong trade. Generalization for `/devx-plan`: when a phase's verification
  is an eval, derive the AC file list *from* the eval's scope rather than
  enumerating by hand, or state explicitly that the eval is authoritative
  and the AC list illustrative. — applied within mss104 (tokens swept, PR
  #101); pending mssret promotion review.
- [C2] [med] [docs] **A test-only-lock that is retired must be ported to its
  replacement in the same PR, not just deleted.** dvx107's `parseHandoffSnippet`
  pinned skill-body prose that mss104 deleted; deleting the validator alone
  would have silently un-pinned Phase 9 at the exact moment its contract
  changed. `test/devx-skill-phase9-split.test.ts` carries `phase9Body()`
  across verbatim (including the load-bearing `^(### |## )` bound, itself a
  dvx107 review finding). — applied in PR #101 as an append-only amendment to
  `§ epic-devx-skill` E12 and the Cross-epic shape-(c) row; recorded here so
  mssret sees it without re-deriving. Sub-pattern concordance deliberately
  left at 1 epic: a successor to the same lock is not an independent
  observation.

---

## epic-story-graph (2026-08-06)

Workstream `story-graph` (plan-62bcd1), sgr101–sgr107, PRs #110/#111/#112/
#114/#117/#118/#120. Retro artifact:
`_devx/workstreams/story-graph/RETRO-2026-08-06.md`. The 2026-08-05
`/devx-learn` candidates C1–C4 are dispositioned in E9; two session findings
routed durably during that run stay in their specs (`debug-d7e8e5`
transient-gh-401 merge tail; `debug-7c1e93` spawn-flake corroboration).

- [E1] [high] [code+docs] **A generated repo-wide artifact committed on every
  mutation turns all concurrency into structural conflicts; its merge
  strategy must be "regenerate", and regen must stay out of the contention
  window.** GRAPH.md's summary banner made every contended claim conflict
  during `pull --rebase`, classifying `claim-contended` at ZERO retries used
  and silently disabling mlc104's rebase-retry (sgr104 Phase 4's most
  load-bearing finding; fix: the claim commit goes board-less the moment a
  race-shaped rejection is seen). Same class handled correctly twice at
  sgr106: real rebases re-rendered GRAPH.md via `devx graph` instead of
  hand-merging, per the plan's Phase 6 note. — applied in sgr104 + sgr106
  PRs; convention recorded for any future generated-committed file.
- [E2] [high] [code] **Reader and writer must agree about invalid input, or
  specs go silently invisible.** `readEngineState` never inspects
  `doc.errors`, so unparseable frontmatter reads as an all-defaults
  EngineState — while `applyEnginePatch` THROWS on the same file. Five
  shipped specs (mgr102, mgr103, cfg204, cli303, cli304) were invisible to
  `devx next`, the loop pick, every gate CLI, and the graph this way.
  sgr106 fixed the five data instances + added `frontmatterParseError`
  surfacing to backfill; the class stays open. — filed-as `debug-9f24c7`.
- [E3] [high] [code] **`await-remote-ci` is blind to CONFLICTING PRs, and
  the safe default is what contained it.** GitHub builds no merge ref for a
  conflicted PR, so `pull_request` workflows never fire; the probe read
  that as `empty` for 41 probes / ~50 min and escalated to INTERVIEW Q#16 —
  while `gh pr view --json mergeable` said `CONFLICTING` the whole time.
  Phase 7's "silent CI is a config bug, not a green light" rule held: cost
  was latency + one human question, never a bad merge. Under diamond
  parallelism this is an expected state (peers merging under the slowest
  PR), not an anomaly. — filed-as `debug-c94f14` (distinct `pr-conflicting`
  probe state + self-service routing).
- [E4] [high] [docs] **When parallel review fan-out is unavailable, the
  validated compensations are sequential multi-lens and an empirical
  real-repo leg — not a plain single pass.** 5 of 7 stories ran under a
  no-subagents session policy (each recorded the deviation in its status
  log rather than silently substituting). sgr105 applied the 3 lenses
  sequentially (7 findings on ~800 lines); sgr106 paired a single pass with
  the attended dry run, which caught the two most serious findings (derived
  edges contradicting explicit `Parallel-safe` annotations). Both shapes
  out-perform mlc105's measured plain-single-pass under-catch (4 vs peer
  median 16). — applied (recorded; feeds the cross-epic 3-agent-review
  row's environmental-fallback sub-pattern, updated below).
- [E5] [med] [code] **Never let the CLI framework share your exit-code
  namespace.** commander's `.requiredOption` enforces itself and exits 1
  BEFORE the action runs, and exit 1 was mark-done's documented "state
  mismatch" signal — a forgotten `--pr` would have read as a claimed-item
  mismatch to the skill body. Hand-parse required flags (→ 64) wherever
  exit codes are a consumer contract. — applied in sgr105.
- [E6] [high] [code+docs] **The local full-suite gate degraded to noise this
  epic: red at baseline in 4 of 7 stories.** Spawn-heavy files
  (loop-worker, manage-spawn×2, manage-crash-restart-loop) fail under peer
  vitest load and even at quiet baseline from a worktree (16/28 at sgr105's
  pre-story bisect point); `npm test` once exited 0 over a 24-red run (the
  summary line caught it, reaffirming `feedback_never_kill_the_gate.md`).
  Every affected story burned a bisect/isolation pass to disprove a
  regression, and the documented risk — "agents trained to wave failures
  through" — is now live. Touched-surface subsets + remote CI were the
  actual gates all epic. — filed-as `debug-620337` + `debug-5c8b21` +
  `debug-ecdcda` (joining `debug-7c1e93`); the structural answer (a
  load-aware gate profile or CI-first gating) belongs in the next planning
  pass over the debug backlog.
- [E7] [med] [skill] **QA-walkthrough emission named its file with the
  STORY's hash, making the story unmergeable by its own gate.** The by-hash
  resolver requires cross-dir uniqueness, so `devx merge-gate sgr103`
  failed with `resolves to 2 spec files` on the story's own Phase 8 (hit
  live, worked around by rename; sgr105/sgr106 used fresh hashes). Skill
  blast-radius → user review required. — filed-as `debug-ea4f41`.
- [E8] [med] [code] **The loop's `phase 4:` omission recurred AFTER its fix:
  PR #82 fixed the merge tail's commit, not the loop-shipped spec content.**
  rtl104 + rtl106 redded main for 3 consecutive runs on 2026-08-03; every
  open PR inherited it (sgr103 spent a fix-forward cycle on it). sgr103
  widened the discipline assertion's trigger (`status: done` OR `merged via
  PR` OR `phase 5:`/`phase 7:` lines) so an attended story now fails its
  OWN PR; the loop-side emission half is still open. — applied (assertion
  widening, sgr103 PR #112); filed-as `debug-3b9e07`.
- [E9] [med] [docs] **Dispositions of the 2026-08-05 `/devx-learn`
  candidates.** **C1** (fixture-defect RED — E-7's audited-drift shapes
  accidentally formed a cycle the CLI *correctly* refused, indistinguishable
  from missing-feature RED) — **promoted** as the third wrong-reason-RED
  shape into the new cross-epic honest-RED row below. **C2** (linked
  worktrees have no `node_modules`; resolve via `createRequire` + walk-up,
  promote to a shared `_fixture.ts` helper on 2nd observation) and **C3**
  (an fs-audit "no reads outside X" guard must enumerate every root the
  process could have baked in, not just the launch root) — **confirmed
  as-filed**, single observations, stay in-section. **C4** (`graph --check`
  >90× over budget under peer load) — **resolved-in-part**: quiet-machine
  measurement at this retro is 0.275 s on the 194-node board (~7× under
  G-1's 2 s budget), attributing the excursion to machine load; keep as
  noted, promote to a debug spec only if it recurs quiet. — applied.
- [E10] [med] [docs] **`dev-b931a1` needs a re-cut: sgr105 shipped its
  staging-scope half.** `mark-done` is the pathspec-returning merge-tail
  primitive that kills the `git add -A` peer-sweep; b931a1's remaining
  scope is spec-lock release (consumes ee7049), the clock-stamped merge
  line, and the post-merge `dist/` rebuild (the mlcret correctness-window
  finding). — applied: cross-reference appended to b931a1's status log in
  this PR; re-prioritization left to the user/ManageAgent.
- [E11] [low] [docs] **When the planning artifacts leave a contract
  undefined, the RED evals are the operative specification — read them for
  the contract, not only to watch them fail.** *(Post-retro addition: filed
  by a `/devx-learn` over the sgr102 session on 2026-08-07, after sgrret
  closed. Not weighed by the retro; a single observation.)* `heading-fallback`
  is named in sgr102's ACs, in design.md's four-code warning union, and in
  plan.md's Phase-2 success criteria — but defined in none of them. Its
  meaning was recoverable only from E-4's *negative* assertion ("ffm heading
  variant fell back instead of resolving its workstream hash"), which fixes
  it as "an epic heading that declares a section but names no linkage hash."
  The same read settled two more underspecified points the prose never
  stated: blocking-edge direction (`from` is the BLOCKED spec) and the
  drift rule (fires only where BOTH sources name blockers — E-4 pins pal444
  as warning and pal111 as must-not-warn). Distinct from the honest-RED row
  below, which governs *why* a red is red; this is about the eval as the
  definition of record. `/devx` Phase 2 already says to locate the RED
  artifacts, but frames re-running them as a `tests-first` obligation —
  sgr102 was `tests-after` and read them anyway. Candidate corrective: Phase
  2 prose says to read the phase's RED artifacts as the contract regardless
  of verification type. — pending; no code or skill change made.

## session-learn candidates (learn 2026-08-07, for the next retro)

*Added by a `/devx-learn` run over the sgr101 session (PR #110), user-pruned.
`epic-story-graph` had already retroed (sgrret, PR #121), so these have no
epic section to land in — the next retro promotes, merges, or strikes them
against its own evidence. Candidates only; do not treat as promoted rows.
The same run's two framework findings were applied directly (see the
`fw/learn-2026-08-07-…` PR) and are NOT repeated here.*

- [C1] [med] [docs] **On a change to a SHARED grammar, capture the live
  before-snapshot before writing code — it changes the design, not just the
  review.** sgr101's AC 6 asked for a before/after verdict diff as
  verification. Taking the "before" first instead turned it into a design
  input: implementing AC 1 literally (strip trailing punctuation) was
  measured to newly block two rows in this repo's own DEV.md
  (`Blocked-by: hfi101, hfi102 (both done).` gaining `both` + `done`), which
  is what produced the parenthetical-exclusion rule rather than a
  post-hoc discovery in review. Distinct from the sgrret E4 "empirical
  real-repo leg", which is a review-stage compensation: this is the same
  instrument used one stage earlier, as a design gate. Generalization for
  `/devx-plan`: a phase whose surface is a shared parser/grammar should
  carry its live before/after AC as a Phase-2 step, not only as
  verification. — pending (candidate).
- [C2] [med] [code+docs] **An existing regression test pins the code path it
  was written against; a new branch routed around it is unpinned, so a fixed
  bug can be reintroduced with the suite still green.** sgr101 widened epic
  heading detection with a new linkage-parenthetical branch. The mlc106
  EC#7 fixture (`Epic — devx (v2) engine`) kept passing the entire time,
  while the new branch would have truncated
  `Epic — devx (workstreams overview) engine` to `devx` — the exact
  epic-collapse bug EC#7 exists to prevent. Only adversarial self-review
  caught it; a second fixture now pins the new path. Generalization: when
  widening a matcher, re-run the FIXTURES OF PAST BUGS in that function
  through the new branch specifically — a green suite proves the old path
  still works, not that the new one is safe. — pending (candidate).

---

---

## epic-portability-install (2026-08-21)

Workstream `portability-install` (plan-b3f7a1), pin101–pin105, PRs #69/#70/
#73/#74/#75 + the two same-day debug fixes it found (#78, #79). Retro
artifact: `_devx/workstreams/portability-install/RETRO-2026-08-21.md`. Run
with pin105 still `[-] blocked` on `MANUAL.md MV-pin105.1` (human-gated
indefinitely); the four shipped phases have five weeks of downstream
evidence.

- [high] [code+docs] **The suite cannot see the packaging boundary; only a
  real install can.** Two defects shipped past a green suite:
  `debug-e3f1c2` (`install:global` produced a non-executable `devx` — tsc
  emits mode 644, the package `bin` points straight at it, and every test
  invoked `node dist/cli.js`, which does not need the exec bit) and
  `debug-b365ac` (`yaml` was a devDependency while `dist/lib/config-io.js`
  imports it at runtime, so a clean packed-tarball install crashed with
  `ERR_MODULE_NOT_FOUND` on every invocation — the repo's own
  `node_modules` had it, so nothing local could fail). Same shape: a
  property that only exists after the artifact leaves the repo.
  pin101's `test/skills-packaging.test.ts` is the durable answer — it runs
  a real `npm pack --dry-run --json` and asserts on the manifest, the only
  test in the suite that looks at the PACKAGE rather than the TREE. —
  applied (pin104 chmods in `scripts/build-info.mjs`, which runs on every
  build; `test/build-exec-bit.test.ts` pins it); cited in the Cross-epic
  "first real run" row as its earliest evidence.
- [high] [code] **An ownership marker must tolerate the whitespace real
  files carry.** CRLF, a BOM, or trailing whitespace on the
  `<!-- devx-skill v… -->` header misclassified a machine-owned file as
  user-owned — a PERMANENT upgrade wedge, because `skip-user-owned` is
  terminal. A Windows checkout would have frozen that user's skills forever
  with a `MANUAL.md` row as the only trace. Found by the 3-agent shape; the
  suite as written would never have produced a CRLF file. — applied in PR
  #70 with explicit CRLF/BOM tolerance tests.
- [high] [code] **A value with two producers has one too many.** pin103's
  defaults-provider mode precompute diverged from the skip table's `n7`
  inference: production-careful repos landed `mode=PROD` with
  `single-branch` + `initial_n=0` AND a false `q32` INTERVIEW record — the
  user would have been asked to confirm a decision the system had already
  made differently. Fixed by mirroring the skip table rather than
  re-deriving. Same family as the cross-epic `deriveBranch` row. — applied
  in PR #73.
- [med] [docs] **When implementation widens a type the spec described, say
  so in the PR body.** pin102 shipped a 4-member action union
  (`skip-same-version` added); the spec prose named three. The PR body told
  pin103's author explicitly: "pin103 must consume the union, not the spec
  prose's 3-action list" — in the artifact the consuming phase would
  actually read. — applied (PR #70 reviewer notes); worth a Phase 8
  gap-filing habit.
- [med] [miss] **A stale `node_modules` cost gate time in two separate
  phases of the same epic.** pin101 and pin102 both hit
  `test/tour-render.test.ts` failing on a missing `diff2html` (a dep added
  by v2t101) and both diagnosed it independently before concluding
  "unrelated to this diff." Two phases rediscovering the same environment
  problem means the check belongs BEFORE the gate, not inside the diagnosis
  of it. Sibling of `project_stale_dist_runtime_deps` (the `dist/` half). —
  pending: a one-line `/devx` Phase 5 preflight note.
- [low] [docs] **The dependency model has no way to express "blocked on a
  human."** pin105 has sat `[-] blocked` for five weeks, honestly — its
  scripted half merged and its live half needs the owner at a keyboard. But
  its `Blocked-by:` names pin103 and pin104, both done, so `devx doctor`'s
  dead-blocker detector (db36af) now flags it. That finding is TRUE: the
  row is blocked on `MANUAL.md MV-pin105.1`, which the annotation
  vocabulary cannot say. — filed: worth an INTERVIEW question.

---

## epic-mid-story-split (2026-08-21)

Workstream `mid-story-split` (plan-e0a67e), mss101–mss104, PRs #95/#97/#99/
#101. Retro artifact:
`_devx/workstreams/mid-story-split/RETRO-2026-08-21.md`. Supersedes the
`## mid-story-split — candidates` section above (C1 → F4, C2 → F3, both
confirmed).

- [high] [docs+skill] **A design premise about the EXISTING corpus is
  checkable in seconds, and is exactly what a design stage asserts without
  checking.** mss102's design said "specs without `branch:` (all existing
  specs) take the derive path" — false, because `validate-emit` requires
  every emitted spec to record its own derived branch. Implemented
  literally, the attach arm would have fired on ordinary claims and
  silently adopted a leftover same-named branch where the claim previously
  failed loudly. Two of three reviewers flagged it independently; the
  author narrowed to `recorded != derived` and filed INTERVIEW Q#14 with
  options rather than deciding silently. — pending: a `/devx-plan`
  design-stage line ("for every claim about what existing artifacts look
  like, name the command that verifies it").
- [high] [code] **A worker-supplied assertion about its own state is an
  input, not a fact.** mss103 rerouted budget exhaustion WITH committed
  progress from abandon to split; the budget rail then routed a
  commit-failure-preserved DIRTY worktree into the split, where
  `worktree remove --force` destroyed work `pushCurrentBranch` never
  committed — a strict work-loss regression versus the path it replaced.
  Found independently by two reviewers. Closed by `hasCommittedProgress()`,
  which also gates the worker path so the advisory clean-seam claim is
  VERIFIED, not trusted. — applied in PR #99.
- [med] [docs] **When a PR deletes a guard, the PR must name its
  successor.** dvx107's `parseHandoffSnippet` pinned skill-body prose that
  mss104 deleted; deleting the validator alone would have silently
  un-pinned Phase 9 at the exact moment its contract changed.
  `test/devx-skill-phase9-split.test.ts` carries `phase9Body()` across
  verbatim, including the load-bearing `^(### |## )` bound. — applied in PR
  #101 (was candidate C2).
- [med] [docs] **When a phase carries a mechanical eval, the eval's scan
  scope IS the acceptance surface, not the AC enumeration.** mss104's ACs
  named files to sweep; E-2 greps five whole trees, and two stale comment
  tokens in `src/lib/devx/split.ts` kept it RED while appearing in no AC.
  Narrowing a permanent invariant to match an incomplete AC list would have
  been the wrong trade. — applied within mss104 (was candidate C1);
  pending as a `/devx-plan` Plan-stage line.
- [med] [miss] **A rule stated in prose and violated twice is evidence the
  rule needs a mechanism — and the gap between those facts was three weeks
  of exposure.** mss104's own status-log lines were swept onto `main` by a
  concurrent session's `git add -A` (commit `ac0ccf2`, erratum `ba3c65b`):
  the E1 class `multi-loop-concurrency` had ALREADY recorded, recurring
  during this epic because the structural fix did not exist yet. Closed
  2026-08-21 by `b931a1`'s `finalize`, which stages exactly the pathspecs
  `mark-done` returns. — applied elsewhere (PR #138).
- [low] [miss] **A retro run 23 days late is an archival artifact, not a
  corrective one.** Three of this epic's findings were independently
  rediscovered and fixed by later work in the interval. The `*ret` item sat
  `ready` and unblocked the whole time and `devx next` never surfaced it. —
  filed: should a `*ret` older than N days raise a dispatcher row?

---

## epic-retro-listener (2026-08-21)

Workstream `retro-listener` (plan-620c74), rtl101–rtl106, PRs #104–#109.
Retro artifact: `_devx/workstreams/retro-listener/RETRO-2026-08-21.md`.
**The first epic in this project's history where the overnight loop shipped
EVERY phase** — 6/6, 16 iterations, +7,686/−54, no phase hitting its
iteration budget.

- [high] [docs+skill] **The loop's per-iteration `- Learning:` lines are the
  highest-density evidence this project produces, and nothing was reading
  them.** Sixteen iterations produced ~70 of them, written at the moment of
  discovery by the agent that hit them. Four worth keeping verbatim:
  (a) *a test that asserts inside a callback passed into `runLearnListen` is
  vacuous — the command's own catch swallows the thrown `expect()` and the
  test passes for the wrong reason*; (b) *`spawn.ts`'s UUID-shaped guard is
  narrower than `queue.ts`'s `isSafeSessionId`, so `classifyEntry` returns
  'ok' for an id `spawnRetro` then throws on — wedging the serial queue on
  the head entry across restarts*; (c) *E-9's `DEVX_RETRO=1`
  index-before-`claude` check only passed because the eval hardcoded a
  `/tmp` marker path — the real learn home is `~/.claude/devx`, so the path
  contains "claude" before the guard on every real machine*; (d) *a literal
  NUL byte in a test file makes plain `grep` silently find nothing — use
  `grep -a`; zero hits is a false negative, not evidence of absence*. —
  applied (these rows); filed: a `devx` surface that harvests
  `- Learning:` lines across a workstream at retro time.
- [high] [code] **A distribution step that only fires on the fresh-install
  path reaches almost nobody.** rtl105's hook install landed in the
  fresh-init phases, but `runInit` routes existing-devx repos to
  `runInitUpgrade` and returns early — so every repo that already had devx
  (the common path, including this one) would never inherit the listener
  registration, defeating the spec Goal's "zero per-repo setup." Fixed by
  reusing the extensible `RepairSurface` registry rather than duplicating
  logic. **Any new artifact `/devx-init` installs must be added to the
  upgrade path, and the upgrade path is the common one** — a repo is only
  initialized once. — applied in PR #108.
- [med] [code] **Adding a `RepairSurface` has a known two-item tail.** It
  silently changes any test enumerating surfaces (`ALL_SURFACES`, feeding
  `presentForAll`/`noopRepairs`) and any test asserting an exact `added`
  count with the new surface un-stubbed. Both fail loudly; the enumeration
  one is easy to miss while writing the surface itself. — applied;
  recorded as a checklist item for the next person extending the registry.
- [med] [miss] **A thin PR body is cheap for the writer and expensive for
  the retro.** All six PR bodies say only "Overnight loop item; iteration
  history is in the spec's Status log on this branch." The branches are
  deleted now, so `gh pr view` — the retro stage's own prescribed evidence
  source — returns almost nothing for six PRs totalling +7,686 lines. No
  evidence was actually lost (the status logs merged into `main` with the
  diff), but the retro CONTRACT pointed at the wrong artifact. — applied in
  this PR: `/devx` Stage: Retro step 1 now says where loop-shipped evidence
  lives.
- [low] [docs] **Positive evidence that the unattended-contract mitigation
  works.** rtl104 identified that AC 6 (human smoke: real tmux + Terminal
  spawn) was unreachable by an unattended loop — "it needs a human at a
  terminal, and the loop orchestrator owns the status log" — completed AC
  1–5 + 7, and surfaced AC 6 as the human-owned remainder rather than
  burning budget on it. `9946f9` exists as that follow-up. — cited in the
  Cross-epic "attended-era contracts" row.


## Cross-epic patterns

Findings that recur across multiple epics get promoted up to this section once they hit ≥3 concordant retros (the Phase 5 `epic-learn-agent` threshold; we mirror it manually here). These are higher-confidence and warrant skill / template / `CLAUDE.md` edits rather than memory.

- [high] [skill+docs] **Planner-emitted `branch:` frontmatter ignored `devx.config.yaml`.** Hit on every Phase 0 story across all 4 shipped epics (aud × 3, cfg × 4, cli × 5, sup × 5 = 17/17). Promoted because it crossed the ≥3-epic threshold immediately. — applied to `/devx-plan`, `/dev-plan`, `docs/DESIGN.md` on 2026-04-27.
- [high] [docs] **Source-of-truth precedence rule** (spec ACs > epic locked decisions > plan frontmatter > `devx.config.yaml` > skill defaults) — surfaced by cfg202 (XDG vs `~/.devx/`) + cli302 (party-mode vs stderr-exact-match). Promoted because two independent epics needed the same rule. — applied to `docs/DESIGN.md § Source-of-truth precedence` on 2026-04-27. *Update from pln104 (2026-05-05): enforced at planning time — `.claude/commands/devx-plan.md` Phase 6 step 5 documents the explicit override flow (lock decision → compare against draft epic + spec ACs → on conflict update epic Locked decisions + epic status log + spec AC + spec status log → validate-emit (pln103) check #6 catches structural drift via `[warn] [locked-decision-token-missing-from-spec]`). The rule is no longer something `/devx` re-discovers at claim time; the planner is the enforcement point.* *Update from plnret (2026-05-05): bidirectional application confirmed — pln102 hit a real conflict (spec AC #6 vs party-mode locked decision #7); author followed the locked decision AND rewrote AC #6 in the same PR per "fix the loser." First instance where the spec AC was the loser AND was rewritten in the same PR. Confirms the rule's mechanics are now load-bearing.*
- [high] [code] **`/devx` self-review step is non-skippable and consistently finds real bugs** — aud102 (4), aud103 (4), cfg201 (5), cfg202 (1 load-bearing — `setIn`-on-Scalar trap), cfg203 (2), cfg204 (5/12), cli301 (2 incl. one HIGH `realpathSync`), cli304 (1/8), ini502 (9 incl. CLAUDE.md merger gating + atomic-write tmp-cleanup), ini503 (12 — 4 HIGH: union-protection stripping existing restrictions / error-swallowing / queue-config-gating / parseRepoSlug regex match; 4 MED + 1 LOW), ini504 (1 — defensive mkdirSync + slugify hyphen-trim), ini506 (3 — per-segment URL encoding / backtick-fence escalation / queue `created:` timestamp preservation), mrg101 (3 — 1 MED audit-log reason text + 2 LOW plural noun + project-context test leak), mrg102 (4 — 2 MED dead helper + too-loose hash regex + 1 LOW sloppy test name + 1 prod-only ENOENT path masking "no PR yet"), prt102 (12 raw → 9 actionable across 3 parallel reviewers — 5 from Edge Case Hunter incl. UTF-8 BOM + headings-only AC + sliceAtMarker substring vs line-anchored + outside-project leak + empty template; 4 from Blind Hunter incl. multi-marker `replaceAll` + Windows path.sep; 3 from Acceptance Auditor incl. AC code-block test gap). cli302 + cli303 + sup all 5 + ini501/505/507/508 reaffirm by being clean OR by not enumerating in the status log (the "status-log terseness" pattern in `LEARN.md § epic-init-skill` E7 — "clean review" does not mean "skipped review"). mrg103 + prt101 explicitly enumerate zero findings ("self-review found nothing actionable" / "self-reviewed (zero actionable findings)" — see `LEARN.md § epic-merge-gate-modes` E7 for why explicit-zero is the correct shape). pln epic adds 6 confirmations: pln101 (1 HIGH branch_prefix unconditional default), pln102 (27 raw → HIGH/MED fixed across 3-agent pass — hash-prefix substring collision + sprint-status `- key:` over-anchoring + tmp-filename PID collision under future Phase 2 parallelism + mkdir-in-try/catch ordering + WARN-leftover-paths visibility), pln103 (27 raw → 11 substantive — CRLF normalization + code-fence tracking + duplicate-spec detection + multi-marker locked-decision splits + greedy body collection clamp + frontmatter quote/comment/empty tolerance), pln104 (3 MED — tokenWarns index lookup hardened + doc-check drift + skill-body overclaim softened), pln105 (16 raw → 14 fixed across all four mode branches + predicate verbatim assertion), pln106 (16 raw → 14 valid fixed including BH-HIGH-2 placeholder-vs-real-hash visual concern via column-position assertion). dvx epic adds 7 confirmations: dvx101 (3 HIGH + 5 MED + 3 LOW = 11 actionable across 3-agent pass — push-target/worktree-base split for split-branch + rename-rollback generalized to N artifacts + openExclusive partial-write unlinks before rethrow + Status:ready lookahead replaces `\b`), dvx102 (canary-of-skip recorded explicitly per AC #5), dvx103 (1 HIGH + 1 MED single-pass — verbatim AC1 phrasing + status-log section regex EOF bound), dvx104 (1 LOW single-pass — editorial-vs-imperative phrasing for dynamic integration-branch resolution), dvx105 (~18 across 2 3-agent passes — pinned headSha at awaitRemoteCi start via `git rev-parse <branch>` not HEAD + coerceGhRun strictness + maxPolls/pollMs production-safety guards), dvx106 (~40 raw across 3-agent — exit-2-emits-decision-without-advice for transient gh outages + cancelled/action_required → MANUAL not WAIT + filter-fallthrough preserves explicit gate advice), dvx107 (1 MED + 6 LOW across 2-agent fallback — phase9Body extractor bound to `^(### |## )/m`). mgr epic adds 6 confirmations (all via 3-agent parallel passes — first all-3-agent epic): mgr101 (14 in-scope — 3H/8M/3L incl. bad-shape state sanitization + writeSync-failure orphan-lock cleanup), mgr102 (~21 unique — 5H/8M/7L incl. per-field projection vs raw-spread tunneling + pre-promotion tmp shape check + EXDEV fallback), mgr103 (~28 unique — 5H/11M/12L incl. fence-aware parsing fixing a real INTERVIEW.md misparse), mgr104 (6 — 2H/3M/1L incl. loop.ts roster-overwrite race resurrecting a dead PID), mgr105 (19 — 4H/6M/9L incl. appendInterviewRow EACCES-wipe), mgr106 (~9 incl. empty-lock-treated-as-held closing the two-manager race). **Confirmed across all 5 shipped Phase 0 epics + all 5 Phase 1 epics (mrg + prt + pln + dvx + mgr)** — 10 epics total. — applied in cfgret PR: CLAUDE.md "Working agreements" now carries an explicit **"Self-review is non-skippable"** bullet.
- [high] [skill] **`bmad-create-story` step in `/devx` Phase 2 silently skipped on every parent story** — **49/49 stories across 10 epics — FINAL count** (aud × 3, cfg × 4, cli × 5, sup × 5, ini × 8 = Phase 0 25/25; mrg × 3 + prt × 2 + pln × 6 + dvx × 7 + mgr × 6 = Phase 1 24/24). No `_bmad-output/implementation-artifacts/story-*.md` file has ever been produced for a parent story (verified at dvxret start: only the 8 retro `epic-*-retro-*.md` files + this one + sprint-status.yaml exist). Spec ACs have been the de-facto source of truth in YOLO + empty-dream. — applied (docs only) in cfgret PR + reaffirmed in cliret + supret + iniret + mrgret + prtret + plnret + dvxret PRs (CLAUDE.md "How /devx runs" Phase 2 inline note bumped to the 43/43 cumulative count across Phase 0 + Phase 1's first 4 epics). **Structurally closed by dvx102** (merged 2026-05-05 PR #46) — `shouldCreateStory()` pure function + `devx devx-helper should-create-story` CLI + `_internal.skip_create_story_canary` flag + `.claude/commands/devx.md` Phase 2 rewrite. Canary ships off-by-default (helper decision logged but not honored — preserves v0 behavior); flip to `"active"` for one in-flight story to validate the conditional path; flip to `"default"` once the canary green-runs. The skill-prompt edit landed via the user merging dvx102's PR per `self_healing.user_review_required_for: [skills]`. Empirical pattern continues until canary flips. *Update from dvxret (2026-05-07): all 7 dvx stories recorded the helper's decision in their status logs per dvx102 AC #5 — the audit trail is structural now even though the behavior hasn't shifted.* *Update from mgrret (2026-07-05): mgr 6/6 skipped — 49/49 is the final count. The canary never flipped and never will: `v2/01-bmad-capture.md` retires `bmad-create-story` (and the canary machinery that existed solely to bury it gracefully) with the v2 migration.*
- [high] [docs] **Stale `develop`-branch references inside CLAUDE.md "How /devx runs" section** despite the project being single-branch (CLAUDE.md's own §"Branching model" already says so). Self-inconsistency in a load-bearing context file. — applied in cfgret PR: section rewritten to reflect single-branch reality (`feat/<type>-<hash>` off `main`; squash+delete-branch merge form). Promote to a periodic CLAUDE.md self-consistency check for future refactors.
- [high] [docs+skill] **Retro stories (`*ret`) absent from `sprint-status.yaml`** — confirmed across **all 10 retros run — final count** (audret PR #19, cfgret PR #20, cliret PR #21, supret PR #22, iniret PR #30, mrgret PR #34, prtret PR #37, plnret PR #44, dvxret PR #52, mgrret this PR). Promoted at cliret; reaffirmed at supret (4/4) + iniret (5/5) + mrgret (6/6) + prtret (7/7) + plnret (8/8) + dvxret (9/9). **Structurally closed by pln102** (2026-05-03) — `emitRetroStory()` + `writeRetroAtomically()` co-emit all three artifacts (spec / DEV.md / sprint-status.yaml) at planning time. plnret + dvxret were both emitted by `/devx-plan` on 2026-04-28 *before* pln102 shipped, so both required the standard mechanical backfill in their respective PRs; future retros emitted by `/devx-plan` after pln102's merge will have the row co-emitted automatically. **`MANUAL.md MP0.2` was flipped to done in plnret PR** — pln102 IS the skill change MP0.2 was waiting for. *Update from dvxret (2026-05-07): dvxret is the **second-to-last** retro requiring manual backfill; mgrret will be the last unless `/devx-plan` re-emits it post-merge. The structural fix is in place; the legacy-emitted backlog still needs one more manual backfill.* *Update from mgrret (2026-07-05): mgrret's backfill applied — 10/10 and **closed permanently**: sprint-status.yaml itself is retired by the v2 migration (`v2/01-bmad-capture.md`: 409 lines, zero consumers); the mgrret PR is the last touch of the file.*
- [high] [docs] **Per-platform deviation with explicit rationale + dedicated test** — confirmed across 3 epics: cli305 (WSL host-crossover detection: `uname -r` containing `microsoft` AND `npm config get prefix` matching `/mnt/c/`), sup404 (`${HOME}` substitution baked in at install because `wsl.exe --exec` doesn't spawn a shell), ini505 (re-uses cli305's WSL detection; files MANUAL.md not init failure on host-crossover). The convention is: "deviate where the platform demands; record the deviation in the status log; pin it in tests so the deviation can't regress silently." Promoted at iniret because it crossed the ≥3-epic threshold; **reinforced at mgrret (4th epic)** — mgr106's macOS-only CI red (`ps -o etime=` 1-second resolution made the PID-recycling probe return now() for <1s-old processes) fixed via a 2s grace window in `classifyExistingLock` + 2 regression tests pinning the grace bounds. — applied (recorded as cross-epic pattern). Worth memorializing in `/devx-plan` epic-shape prompts as "if a story is platform-sensitive, allow + expect explicit-rationale-in-status-log + dedicated platform test."
- [high] [docs] **MANUAL.md as a designed signal, not just an escape hatch** — confirmed across 2 epics with rich internal coverage: sup × 2 (sup402 MS.1 on-host kill-and-watch-restart proof at story-implementation time + sup405 verify-fail-but-don't-abort) + ini × 6 (ini503 free-tier-private + no-remote = 2 designed MANUAL filings; ini505 verification-failure files MANUAL but doesn't abort; ini506 BMAD-fail + gh-auth + no-remote = 3 designed MANUAL filings, all with explicit degradation paths). The convention is: "verification failure files MANUAL but never aborts the install/init flow; MANUAL is the designated channel for can't-verify-from-CI surfaces and graceful-degradation handoffs." Promoted at iniret matching the cfg+cli source-of-truth-precedence promotion precedent (2 epics with multiple internal observations) rather than the section's nominal ≥3-retro threshold; the 8 internal observations across 2 epics make the convention load-bearing for any future install / scaffold / onboarding work. — applied (recorded as cross-epic pattern). Worth memorializing in `/devx-plan` epic-shape prompts for installer-shape and onboarding-shape epics.
- [high] [config+docs] **Bypass-permissions mode does NOT auto-accept skill / agent / settings updates — those gates are structural to the harness, not config-knob-driven.** Surfaced 2026-05-03 in user feedback during the pln102 dev session: even when a session is launched with `--dangerously-skip-permissions` or YOLO autonomy, edits to skill files, agent definitions, and `.claude/settings.json` still pop a confirmation prompt the user has to accept manually. This is intentional security-of-prompt-injection design (a malicious skill-edit in a worktree could otherwise self-modify the harness without operator review), and it lines up with `devx.config.yaml → self_healing.user_review_required_for: [skills, prompts, agents]` — but the *harness gate* is independent of that knob and applies even when the knob is set permissively. **Implication for Phase 2+ (multi-worker / ManageAgent):** subagent workers running unattended cannot accept these prompts → any spawned agent that needs to edit a skill file will block forever. **Workarounds to consider:** (a) keep `LearnAgent` and any other skill-mutating agent strictly user-foreground (file-only intent → user reviews → user runs `/devx-learn apply`); (b) split intent-emission from intent-application — agent writes a candidate skill diff to `.devx-cache/proposed-skills/<name>.diff`, never edits the skill in place; (c) explore whether the harness exposes a programmatic "trust this directory's skill edits" flag that subagents could request once at session start. Filed as a Phase 2 design constraint — `epic-controller-autoscaling` planning must respect this. Cross-references INTERVIEW Q#8 (Phase 2 sequence locking) and `devx.config.yaml` self-healing block. Single-epic at promotion (this is a meta-finding about the harness, not about a shipped epic), but promoted because it's load-bearing for every future autonomous epic that touches skills / agents / `.claude/settings.json`.
- [med] [docs+template] **Idempotency state file pattern (`~/.devx/state/<thing>.installed.json` SHA-256)** — confirmed across 2 epics: sup × 4 internal observations (stub script + launchd + systemd + task-scheduler installers all use the SHA-256-on-disk primitive with matching-hash → no-op + return "kept"; differing-hash → uninstall-then-reinstall + return "rewritten") + ini505 re-uses `installSupervisor()` from sup → same primitive consumed cross-epic. Captured as project structure (`src/lib/supervisor-internal.ts` shared helpers); further work adopts by importing. The related-but-distinct sibling pattern "deferred-work-queue-on-disk" (`.devx-cache/pending-gh-ops.json` from ini503 / ini506) sits adjacent — same conceptual area (state for idempotent replay) but a different primitive (work queue, not install hash); stays single-epic pending-concordance. Promoted at iniret matching the cfg+cli source-of-truth-precedence promotion precedent (2 epics, strong same-primitive recurrence) rather than the section's nominal ≥3-retro threshold. — applied (recorded as cross-epic pattern).
- [high] [docs+code] **Pure-fn + CLI-passthrough + adversarial-testing trio.** When decision logic is grid-shaped (mode × CI × coverage × trust-gradient; substitution placeholders × line-anchoring discipline; config-shape × type × hash; epic structure × cross-reference checks), separate the decision (pure function in `src/lib/<domain>/<thing>.ts`, truth-table or golden-fixture tests) from the data collection (impure CLI wrapper in `src/commands/<command>.ts`, integration tests). Confirmed across **4 epics**: mrg (mrg101 `mergeGateFor` + mrg102 `devx merge-gate`) + prt (prt102 `renderPrBody` + `devx pr-body`) + pln (pln101 `deriveBranch` + `devx plan-helper derive-branch`; pln102 `emitRetroStory` + `writeRetroAtomically` + `devx plan-helper emit-retro-story`; pln103 `validateEmit` + `devx plan-helper validate-emit`) + **dvx (6 fresh instances)** (dvx101 `claimSpec` + `devx devx-helper claim`; dvx102 `shouldCreateStory` + `devx devx-helper should-create-story`; dvx104 `coverageTouchedGate` + `parseOptOutMarkers` — library-only variant; dvx105 `probeRemoteCi`/`awaitRemoteCi` + `devx devx-helper await-remote-ci`; dvx106 `deriveMergeAdvice` extension to `merge-gate.ts` — extension-to-existing-CLI variant; dvx107 `parseHandoffSnippet` — library-only validator-style variant). Promoted at plnret because it crossed the ≥3-epic threshold; reinforced at dvxret. **Two healthy variants identified** (dvxret 2026-05-07): (a) **library + tests, no CLI yet** — appropriate when the consumer is the skill body's prose dispatch (dvx104) or test-only lock with no production consumer (dvx107); (b) **extension to existing CLI** — appropriate when new logic is enrichment of an existing decision surface, not a new domain (dvx106 added `advice` array to `merge-gate.ts`). The pattern's micro-variant from pln102: when the I/O is sufficiently tight to the pure logic, keep them in the same file but export the pure piece separately — vs mrg's separate-file shape. All variants work; the right shape depends on what consumes the decision. — applied (recorded as cross-epic pattern). Worth memorializing in `/devx-plan` epic-shape prompts for any epic whose deliverable is grid-shaped decision logic.
- [high] [docs+skill] **Externalize behavior-as-CLI-primitive consumed via skill-body passthrough.** Any time a skill body says "behavior depends on X" where X has edge cases (mode, template shape, config shape, AC checklist, line-anchoring discipline, cross-reference graph), the X-handling logic should live in TS as a pure function and the skill body should call a CLI passthrough rather than re-implementing the X handling inline. Eliminates the regression class "skill body says X, code says Y." Confirmed across **4 epics**: mrg E4 (mode → `devx merge-gate`), prt E6 (PR body substitution → `devx pr-body`), pln × 3 (branch derivation → `devx plan-helper derive-branch`; retro emission → `devx plan-helper emit-retro-story`; cross-reference validation → `devx plan-helper validate-emit`), **dvx × 4** (claim → `devx devx-helper claim` — replaces inlined git+fs sequence; should-create-story → `devx devx-helper should-create-story` — replaces inlined Phase 2 inference; await-remote-ci → `devx devx-helper await-remote-ci` — replaces inlined gh-run-list state machine; advice routing — REMOVES per-mode "Behavior by mode" table from skill body Phase 8 entirely). Promoted at plnret because it crossed the ≥3-epic threshold; reinforced at dvxret. The skill body's role reduces to "invoke the right CLI and react to its exit code + payload." Pinned by `.claude/commands/devx.md` Phase 7 and 8 ("Never re-implement the substitution in the skill body — the CLI is the single source of truth"). After dvx ships, **skill body Phases 1, 2, 5, 7, 8 are CLI-driven prose** — only Phase 3 (LLM-driven implementation) and Phase 6 (git-prose) remain inline. Worth memorializing in `/devx-plan` epic-shape prompts as: "if a skill-body change is on the table, ask whether the change should externalize to a CLI primitive instead." — applied (recorded as cross-epic pattern). *Update from mlcret (2026-07-29, 5th epic — the sharpest instance yet, because the counterfactual is inside a single epic): multi-loop-concurrency converted three cross-session hazards into primitives (`claimSpec` rebase-retry, `withBacklogLock`, `acquireSpecLock`) and all three held silently through the first real concurrent execution — eleven PRs (#91–#101) merged in ~24h across two concurrent epics plus a `/devx-learn` run. The one cross-session hazard left as prose — `/devx` Phase 8's after-merge bookkeeping tail, whose "stage only files relevant to this item, never `git add -A`" rule is stated in BOTH `CLAUDE.md` and `.claude/commands/devx.md` — is precisely the one that broke: mlc106's mark-done commit swept a concurrently-live peer's files (`LEARN.md § multi-loop-concurrency` E1). Stating a contract twice in prose did not help; the lesson is that **duplication of prose is not a substitute for a primitive, and the tail that runs while peers are live is the highest-value remaining inline-git section**. `/devx` Phase 8 is now the last major non-CLI-driven phase; filed as `dev-b931a1`.* *Update from sgrret (2026-08-06): the Phase 8 after-merge tail got its primitive — sgr105's `devx devx-helper mark-done` (PR #118) performs the closing flips under the backlog lock and returns explicit pathspecs, making the "never `git add -A`" rule structural exactly as `claim` did for the opening flips. The skill body's after-merge section is now dispatch-on-exit-code prose. b931a1's remaining scope (lock release, clock-stamped merge line, post-merge dist rebuild) re-cut per § epic-story-graph E10.*
- [high] [docs+skill] **3-agent parallel adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor) on substantial-surface stories.** When a story's change has substantial surface area (>500 lines new code, multiple regex/substitution sites, integration with multi-marker on-disk state), parallel adversarial reviewers in different framings catch more than single-pass. Single-pass remains correct for below-threshold stories. Confirmed across **3 epics with rich internal coverage**: prt × 1 (prt102 — 12 raw → 9 actionable across 3 reviewers; vs prt101 single-pass on a 27-test, ~75-LoC surface) + pln × 4 (pln102: 27 raw across 3-agent pass; pln103: 27; pln105: 16; pln106: 16 — total 86 raw across 4 substantial stories, vs pln101 single-pass with 1 HIGH and pln104 single-pass with 3 MED) + **dvx × 3** (dvx101: 11 actionable; dvx105: ~18 across 2 passes — pinned headSha at awaitRemoteCi start via `git rev-parse <branch>` not HEAD; dvx106: ~40 raw — exit-2-emits-decision-without-advice for transient gh outages). Promoted at plnret matching the iniret precedent for "promote at 2 epics with rich internal coverage"; reinforced at dvxret which crossed the section's nominal ≥3-epic threshold; **reinforced again at mgrret (4th epic, first 6/6 all-3-agent epic — every mgr story crossed the threshold: ~430–560 LoC lock/state/spawn/parser surfaces; ~97 unique actionable findings epic-wide, all in-scope fixed, every re-review clean).** The orthogonal framings cover different failure-mode classes: **Blind Hunter** (no spec context) catches regex over-anchoring + multi-marker `replaceAll` + Windows `path.sep` + CRLF normalization; **Edge Case Hunter** catches BOM + substring collisions + tmp-filename PID races + hash-prefix substring collisions + sprint-status `- key:` over-anchoring + headSha mismatch under fix-forward pushes; **Acceptance Auditor** (spec compliance) catches AC code-block test gaps + AC `includes()` fallback breaks + AC wording precision + AC closure deferral. **Threshold heuristic confirmed at 3 epics:** pln104 (40-line skill body + 250-line test, single-pass, 3 MED) + dvx103 (290-LoC, single-pass, 1 HIGH + 1 MED) + dvx104 (211-LoC core, single-pass, 1 LOW) — all caught their load-bearing findings without 3-agent pass. **Environmental-fallback pattern (dvx107):** when `bmad-edge-case-hunter` agent type is unavailable, 2-agent fallback (Acceptance Auditor + Blind Hunter) still catches load-bearing semantics — recorded under epic-devx-skill E11 for revisit if recurs. — applied (recorded as cross-epic pattern). Worth memorializing in CLAUDE.md "Working agreements" as: "For substantial-surface stories (>500 lines / multi-regex / marker-bearing), prefer 3-agent parallel adversarial review over single-pass." (Already in CLAUDE.md per plnret update.) *Update from hfiret (2026-07-26): first unattended 3-agent pass on record — hfi102's loop workers ran the full shape across iterations 6–8 with all actionable findings fixed in-place and one documented no-change; hfi103 (7 unique: 2 MED drift-aggregate blind spot + status/hash identity split) and hfi105 (8 unique incl. the nudge non-restatement pin) reaffirm interactively; below-threshold loop items (hfi101, hfi104) shipped on per-iteration verification (evals + full suite) with truthful retroactive `phase 4:` lines instead — see `LEARN.md § epic-harness-fold-in` E6 for the two-evidence-shapes taxonomy.* *Update from mlcret (2026-07-29, 6th epic): 5 of 6 mlc stories ran the 3-agent shape over above-threshold surfaces (+710 to +2,534 lines each), returning ~88 unique actionable findings — 10 HIGH, every one a real behavioral defect rather than lint (worktree misclassification by path shape; a backlog-lock timeout aborting an entire loop run and stranding a merged PR unreconciled; an ungated lock reap that would have destroyed a live peer's claim — the E13 collision class reintroduced; `isRejectedPush` over-matching git's generic push trailer so policy refusals would have walked the whole backlog as fake contention; a `".."` session token aiming a recursive `rmSync` at `.devx-cache`; a scope intersecting to zero rows that took the lock, spawned nothing, and reported "0 attempted" with no reason). **The environmental-fallback sub-pattern now has its 2nd instance and its first measurement**: mlc105's harness forbade subagents, so it single-passed a +1,513/13-file surface and returned **4** findings against a 3-agent peer median of 16 on comparable diffs. It still caught a genuine HIGH, so single-pass is not worthless — but a 4-vs-16 gap on an above-threshold surface reads as under-catching, not as a cleaner diff. Corrective proposed at mlcret: when the parallel shape is unavailable, **compensate with sequential passes separated by a context reset** rather than collapsing to one pass and noting the deviation. See `LEARN.md § multi-loop-concurrency` E4.* *Update from sgrret (2026-08-06): the mlcret corrective is validated — 5 of 7 sgr stories ran under a no-subagents policy, and the two compensation shapes both out-caught plain single-pass: sgr105's sequential 3-lens (7 findings on ~800 lines) and sgr106's single-pass + attended real-repo dry run (14 findings, the dry run catching the two most serious). The empirical real-repo leg is hereby recorded as a second sanctioned compensation shape alongside sequential multi-lens; every deviating sgr story recorded the deviation in its status log rather than silently substituting. See § epic-story-graph E4.* *Update from the 2026-08-21 retro batch: mid-story-split adds 3 more observations (mss101/102/103, 48 findings fixed in-place, both HIGHs found only by the parallel shape), and portability-install adds the clearest counter-evidence for the OTHER direction — pin101 (~330 LoC), pin104 (docs-dominated) and pin105 (32-line diff, reviewed clean twice) all correctly took single-pass below the threshold in one epic. The heuristic is not 'always fan out'; it is 'fan out above the threshold', and both halves now have evidence.*
- [high] [skill+docs] **Status-log terseness pattern (corrective-needs-promotion).** Status logs sometimes omit per-phase milestones and self-review finding counts even when the underlying work is substantial. Confirmed across 3 epics: sup (5/5 uniform omit), ini (4/8 mixed), pln (4/6 mixed — pln103/105/106 had substantial 27/16/16-finding 3-agent reviews but none of those counts reached the spec status log). mrg (0/3 omit) + prt (0/2 omit) + **dvx (0/7 omit — every story enumerates per-phase milestones, finding counts by severity, dogfood references, fix-forward outcomes, merge SHAs)** are positive counterexamples that sharpen the case: the variance is real, the cause is `/devx` run-style rather than story-shape. Variance now reads as **run-style polarized**: mrg + prt + dvx (0% omit) vs sup + ini + pln (mixed); mgr adds an **intra-epic** data point (5/6 rich, mgr105 omits phase-5 + phase-7 lines within a single-day epic — see `LEARN.md § epic-devx-manage-minimal` E13). Promoted at plnret because it crossed the ≥3-epic threshold; reinforced at dvxret with dvx as the strongest positive counterexample (7/7). The corrective is a `/devx` skill prompt-card line ("Status-log entries MUST enumerate per-phase milestones AND self-review finding counts — use explicit-zero per `LEARN.md § epic-merge-gate-modes` E7 when applicable; never omit"). `skill` blast-radius → user-review-required (`self_healing.user_review_required_for: [skills]`); filed as `MANUAL.md MP1.1`.
- [high] [docs+skill] **Dogfood-mid-epic with three-shape taxonomy.** When an epic ships a CLI/library primitive that `/devx` itself consumes, the consumer can run inside the epic's own boundary in three distinct shapes: **(a) consumer-ships-runs-on-next-PR within epic** — primitive ships in PR N, runs on PR N+1 of the same epic (mrg E11: mrg103 consumed mrg102's CLI; dvx101's `claimSpec` consumed by every dvx102–dvx107 claim); **(b) consumer-ships-runs-on-SAME-PR** — the very PR that ships the primitive uses it for its own gating (prt E9: `devx pr-body` rendered prt102's own PR body; dvx105 awaitRemoteCi gated dvx105's own merge; dvx106 mergeGate dispatched its own advice array); **(c) test-only-lock with no production consumer** — the primitive ships only as a validator that pins prose/contract shape via tests, with no runtime consumer (dvx107 `parseHandoffSnippet` pins skill body's Handoff Snippet template; sister to mrg103's `promoteIntegrationToDefault` dead-code-with-tests but for prose). Confirmed across **4 epics**: mrg (shape a) + prt (shape b) + pln (shape (a)-variant — pln103 ran on pre-existing inputs mid-epic) + **dvx (all three shapes simultaneously)**. Promoted at dvxret because dvx exhibited richer shape variance than any prior epic. **The shape determines audit precedent strength**: shape (b) is tightest (the consumer ran on the PR that shipped it — strongest possible verification), shape (a) is next (consumer ran on the very next claim in the epic), shape (c) is structural (no runtime consumer but prose drift is structurally locked by tests). Worth memorializing in `/devx-plan` epic-shape prompts as: "for any epic shipping a CLI/library primitive that `/devx` itself consumes, identify which dogfood shape applies and target it explicitly in the epic plan." Sub-pattern (c) is single-epic at promotion (1 internal observation in dvx); pending-concordance separately for that sub-shape — revisit when a 2nd skill-body prose section needs equivalent locking. *Amendment (mss104, 2026-07-29): shape (c)'s founding exemplar (`parseHandoffSnippet`) was retired with the prose it pinned; `test/devx-skill-phase9-split.test.ts` (workstream mid-story-split, phase 4) is the successor — same shape, same file, pinning Phase 9's `devx split` routing instead. Sub-pattern concordance stays at 1 epic (a successor to the same lock is not an independent observation), but the exemplar pointer now resolves to live code — see § epic-devx-skill E12 for the retirement-porting refinement.*
- [high] [code+docs] **First real run against the live repo is a required verification step for any new gate/parser/primitive — fixtures never carry the repo's poison.** Confirmed across 4 concordant instances in 2+ epochs: mgr103 (parser vs the real INTERVIEW.md: code-fence example row parsed as a real answered question — `blankFencedLines()` corrective; was the 1-epic "real-file smoke tests" row candidate at mgrret) + v2e102 (first real `devx gate prd` run surfaced the parseExpectations wrapped-field folding bug that ~200 fixture tests missed — real artifacts wrap at the house 78-col width, fixtures don't) + v2d101 (first real-repo `devx next` run surfaced 13 pre-existing drift defects + 1 design bound) + v2t101 (building the tour of its own PR's diff exposed slot-literal re-substitution). The convention: before a new primitive's PR merges, run it once against the live repo (or its own PR's artifacts — the dogfood shapes above) and treat every surprise as a finding, not noise. Promoted at the v2-migration retro (2026-07-05) on ≥3 concordance; see `LEARN.md § v2-migration` E1. — applied (recorded as cross-epic pattern; the /devx skill's dogfood habit already produces the run — this row makes it load-bearing rather than incidental). *Update from sgrret (2026-08-06): +3 concordant instances in one epic — sgr102 (live-board build found 3 defects its 46 in-memory tests missed: the `MERGED_LINE_RE` loop-dialect gap, heading-fallback noise on closed epics, and the unreadable-backlog empty-board render), sgr103 (rendering its OWN PR's board caught the label-escaping bug that would have shipped a blank diagram in that very PR — shape (b) dogfood, tightest possible), sgr106 (the attended dry run caught 3 derived edges contradicting explicit `Parallel-safe` annotations — the review finding fixtures could not have produced). The live-repo leg was this epic's single best reviewer; see § epic-story-graph E4.* *Extension from mlcret (2026-07-29, 5th instance — the row needs a sharper predicate): "run it against the live repo" is not sufficient; it must **run the INSTALLED artifact, not the worktree build**, or it verifies a binary no consumer will ever execute. `devx` on PATH resolves to the main worktree's gitignored `dist/`, and a story's local-CI gate runs inside its own worktree — so `npm test` refreshes the worktree's `dist/` and never main's. Main's build was 16h stale when mlc106 merged, and at retro time `devx loop --help | grep -c epic` returned `0`: the epic's largest story (+2,534, the entire scope model) was shipped, tested, merged, and unreachable from the CLI `/devx` itself invokes. The same lag silently split the spec-lock body format on disk (mlc104 claimed a legacy lock 36 minutes after the JSON format merged, because the claim ran from main's stale build). For any self-hosting project, **the gap between merge and rebuild is a correctness window** in which every claim, gate, and merge decision runs on the previous build. See `LEARN.md § multi-loop-concurrency` E2; corrective filed as `dev-b931a1`.*
- [high] [code+docs] **Atomic state writes via tmp+rename.** Any on-disk state a devx process writes — install artifacts, planning artifacts, mutating tick state — goes through write-to-`*.tmp`-then-`rename`, never in-place writes. Confirmed across **4 epics**: sup × 4 (`writeAtomic` in `src/lib/supervisor-internal.ts` across all four install surfaces: stub + launchd + systemd + task-scheduler) + ini505 (consumes `installSupervisor()` → same primitive cross-epic) + pln102 (`writeRetroAtomically()` — the fixed-order multi-artifact variant: rename spec → DEV.md → sprint-status.yaml in fixed order, WARN-not-rollback on partial failure; was pln E9 pending-concordance at 1 epic) + mgr102 (`src/lib/manage/state.ts` extends the primitive to **mutating manager tick state** — `schedule.json`/`manager.json`/`heartbeat.json` — with crash-mid-write recovery tested both directions: leftover tmp promoted when the parent file is missing, ignored + quarantined to `.corrupt` when poisoned, plus EXDEV copyFile+unlink fallback for cross-device renames). Promoted at mgrret because it crossed the ≥3-epic threshold (the mgrret spec's explicit AC: sup × 4 + ini505 + mgr102, confirmed + pln102 reinforcement). **Scope note from mgr102's Acceptance Auditor:** the original sup pattern has two separable halves — tmp+rename atomicity (universal; this row) and SHA-256 content-hash idempotency (install-only; meaningless for mutating state where every write changes content — that half stays in the iniret "Idempotency state file pattern" row above). Two variants: single-artifact (sup/mgr) and fixed-order multi-artifact with WARN-not-rollback (pln102). — applied (recorded as cross-epic pattern at mgrret, the final BMAD-era retro); carried forward as a v2 engine ground rule for all dispatcher/state writes.
- [high] [code+docs+skill] **Single-actor-era contracts break on first multi-actor contact — treat a new consumer's first runs as a verification stage, not an incident.** *(Generalized at mlcret 2026-07-29 from the original title "Attended-era contracts break on first unattended contact"; the failure class turned out not to be specific to unattendedness — see the mlcret update below.)* When a primitive or contract built and reviewed under single-actor assumptions is inherited by a second actor — an unattended loop OR a concurrent peer session — expect a latent-defect class the original review's severity model could not see. Confirmed across 2 retros with rich internal coverage (iniret promotion precedent — 5 observations): v2-migration E2 × 2 (dvx101 `claimSpec` push-failure rollback ran `git reset --hard HEAD~1` in the MAIN worktree — harmless attended for ~2 months, catastrophic the first unattended night; the fix's regression test caught a second instance, bare `git commit` sweeping user-staged files) + epic-harness-fold-in × 3 (cf65aa: the loop merge tail never emitted the dvx103-mandated `phase 4:` line, so every loop-shipped spec redded main; verification parking: workers reported before observing their own ~7-min suite runs, abandoning a done item at iteration budget; dc7514: infra hangs charged as item failures wedged the backlog into drifted state — `blocked` + dead owner + DEV.md drift). The convention: (a) before a primitive built for attended flows is first run unattended, re-review it under the "nobody is watching" frame — destructive rollbacks, discipline contracts, and failure attribution are the known hot spots; (b) the first unattended runs ARE a verification stage — supervise them (MANUAL MV2.1's S-3 shape) and run the morning review treating the night's claims as claims; (c) failure taxonomies must separate substrate failure (infra, sleep, hang) from task failure — attribution errors poison backlog state. Correctives shipped structurally in PRs #82 + #84 and v2l101's rollback fix. — applied (recorded as cross-epic pattern at hfiret, 2026-07-26). *Generalized at mlcret (2026-07-29, 2nd concordant retro): the second actor does not have to be unattended. multi-loop-concurrency ran interleaved with mid-story-split — eleven PRs (#91–#101) merged in ~24h across two concurrent **interactive** epics plus a `/devx-learn` run — and produced the same defect shape: `/devx` Phase 8's after-merge `git add -A` swept a concurrently-live peer's files into mlc106's mark-done commit (`LEARN.md § multi-loop-concurrency` E1). Nothing about that contract was unattended; it was written when exactly one session could be mid-item, and it broke the first time two were. The predicate that actually holds is **"was this contract written assuming a single actor?"** — unattendedness is one way to acquire a second actor, concurrency is another, and both surface the same hot spots (destructive operations, discipline contracts, failure attribution, and now **staging scope**). Practical addition to (a): when re-reviewing a single-actor contract, ask not only "what if nobody is watching" but "what if a peer is writing to this repo right now."* *Update from epic-retro-listener (2026-08-21), 3rd concordant observation and the first where the MITIGATION is visible working: rtl104 identified that its AC 6 (human smoke — real tmux + Terminal spawn) was unreachable by an unattended loop, completed the other six ACs, and surfaced AC 6 as the human-owned remainder (`9946f9`) rather than faking it or burning budget. Making the unattended path DECLARE what it cannot do is the fix.*
- [high] [docs+skill] **A RED artifact must fail for the STATED reason — read the failure output and check it names the missing feature; three wrong-reason-RED shapes are now on record.** A red eval is only evidence when the assertion it claims to make actually ran. Confirmed across 2 epics / 3 concordant instances, each a distinct shape: **(1) infra breakage** — mlc101's E-2 refusal assertion "failed" with empty output because the fixture's tsx path didn't exist in linked worktrees; the CLI under test never spawned (this instance authored the Phase 2 rule now in `.claude/commands/devx.md`); **(2) transient** — sgr105's first E-5 run REDded with `devx graph` exit −1 + empty stdout under a concurrent `npm run build`; the honest RED (`unknown command 'mark-done'`) only appeared on re-run; **(3) fixture self-contradiction** — sgr107's E-7 fixture composed two audited-drift shapes that accidentally formed a blocking-edge cycle, so the CLI's *correct* refusal held the eval RED in a way indistinguishable from the missing-feature RED (fix: reroute the edge; a cycle is refused by design, not tolerated drift). Promoted at sgrret on the iniret precedent (2 epics, 3 concordant observations, rule already load-bearing). The discipline: on every RED re-run, classify the red as feature-shaped / infra-shaped / transient / fixture-shaped before writing any code; fixtures composing multiple defect shapes get checked for self-contradiction at authoring time. — applied (the Phase 2 skill rule shipped at mlc101; this row adds the shape taxonomy and the fixture-authoring check). Worth threading into `/devx-plan`'s RED-stage prose when it is next edited.
