# devx CI — Phase 0 surface area is the @devx/cli npm package at repo root.
#
# Matrix runs on macos-latest + ubuntu-latest (per cli305 AC#3). Windows-host
# install is documented in INSTALL.md but not gated in CI for Phase 0.
#
# Future workflow steps (when the surfaces land):
# - flutter analyze + flutter test for mobile/ (Phase 8 onward)
# - bun test for worker/ (Phase 4 mobile-relay)
#
# Spec: dev/dev-cli305-2026-04-26T19:35-cli-cross-platform-install.md
# Config: devx.config.yaml → ci.workflow_path

name: devx-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cli:
    name: cli (${{ matrix.os }} / node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
        node: ['20']
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history: the outline-check step diffs origin/main...HEAD.
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      # Outline protection L2: outline.md / OUTLINE.md changes never ride in
      # a PR — they reach main only via the human's `devx outline commit`.
      # PR-only: push-to-main has no base range to diff.
      - name: Outline check
        if: github.event_name == 'pull_request'
        run: node dist/cli.js outline check --diff "origin/${{ github.base_ref }}...HEAD"

      # debug-2d6fc1: a workstream story may edit plan/agent.md only inside
      # its own phase. dlr102 (PR #152) carried main's working copy of that
      # shared file into its branch and shipped dlr103's uncommitted edits.
      # In CI because CI is the gate a hand-merge still respects. The story
      # comes from GITHUB_HEAD_REF; non-story branches are skipped.
      - name: Workstream plan scope check
        if: github.event_name == 'pull_request'
        run: node dist/cli.js workstream scope-check --diff "origin/${{ github.base_ref }}...HEAD"
