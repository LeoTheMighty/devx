---
hash: v2t101
type: dev
created: 2026-07-05T13:04:00-06:00
title: V2.3 — static HTML review tour on every PR
from: v2/06-phases.md
plan: v2/
status: done
owner: /devx-2026-07-05T1155-56340
blocked_by: [v2x101]
branch: feat/dev-v2t101
---

## Goal

Every `/devx` PR carries a self-contained static review tour; human input
returns via PR comments. Per `v2/03-review-tour.md`.

## Acceptance criteria

- [ ] `devx tour build <hash>`: gather (local `git diff <base>...`, spec
      Goal/ACs, commits) → validate agent-emitted `tour.json` against the
      schema (retry protocol) → render single-file `tour.html`
      (`.devx-cache/tours/<hash>/`).
- [ ] `tour.html` template: adapted code-review-tour UI — inline JSON data
      island (`</script>` escaped), vendored diff2html JS+CSS + marked from
      node_modules, NO Mermaid (trail step tables only), system font stacks,
      zero network requests (asserted by a test scanning for `http(s)://` in
      src/href attributes).
- [ ] Tour model preserved: weight classes, dependency-ordered stops with
      must/should/skim + 4 flags, decision ledger, grep-verified trails
      (unverified edge → 🕳), blast radius incl. callers-not-updated,
      coverage rows seeded from spec ACs, `path:line` linkification →
      Total Diff deep links. No severity verdicts.
- [ ] Comment path: localStorage scratchpad + per-comment "Copy as PR
      comment" (`` `path:line` — text ``) + "Export review → clipboard".
      No server anywhere.
- [ ] `devx tour publish <hash>`: commits to orphan `devx-tours` branch
      (fetch-rebase-retry race-safe); `devx tour prune` retention.
- [ ] `pr-body`: `## 🗺 Review tour` section — htmlpreview link + raw
      fallback + orientation `<details>` markdown. Tour failures are
      fail-soft (PR opens regardless; stderr note).
- [ ] `/devx` Phase 7.5 wired (skill edit, user-foreground): build + publish
      + link before PR open.
- [ ] `/devx address <pr>`: comment→anchor mapping, in-scope fixes on the
      branch, per-comment replies, out-of-scope → debug/test specs; `devx:
      hold` + requested-changes check added to the merge tail (D-5).
- [ ] Schema/template drift-pin test (dvx107 pattern).
- [ ] Proof AC (S-2): this PR itself ships with a working tour; Leo reviews
      it via the tour.
- [ ] Full suite green.

## Technical notes

- JIRA/anything-external stripped at the source; spec ACs replace ticket
  intent. Big-diff subagent fan-out per semantic area; synthesis in one head.

## Status log

- 2026-07-05T13:04 — created from v2/06-phases.md § V2.3.
- 2026-07-05T11:55:40-06:00 — claimed by /devx in session /devx-2026-07-05T1155-56340
- 2026-07-05T12:05 — phase 2: spec ACs direct (v2 native); CLI/library scope in this worker (Phase 7.5 skill edit, `/devx address`, and the S-2 proof tour run in the coordinating session). Upstream code-review-tour UI studied first (assets/tour.html 911 lines + skill Phase 4 tour.json schema); v2/03-review-tour.md §1 keep/change/drop list applied verbatim.
- 2026-07-05T12:39 — implementation: `src/lib/tour/{schema,gather,render,publish,exec}.ts` + `src/lib/devx/hold-check.ts` + `src/commands/tour.ts` (`devx tour gather|validate|build|publish|prune`) + `devx devx-helper check-hold <pr>` + `devx pr-body --tour-url/--tour-orientation/--tour-unavailable` (fail-soft `## 🗺 Review tour` section, placeholder added to the canonical PR template + BUILTIN_TEMPLATE) + `_devx/templates/tour/tour-template.html` (single-file field-journal UI: inline JSON data island, vendored diff2html@3.4.48 + marked@12.0.2 pinned exact, localStorage-only scratchpad w/ "Copy as PR comment" + "Export review → clipboard", `path:line` deep links w/ flash + back-pill, j/k nav, trail step tables — NO mermaid, system fonts, zero network). Publish is git-plumbing against a temp GIT_INDEX_FILE (never touches the invoking worktree) with fetch+retry on non-fast-forward. 7 new test files + help/pr-body snapshot refreshes; E-1 eval stays green.
- 2026-07-05T13:10 — phase 4: 3-agent parallel adversarial review (Blind Hunter 12 / Edge Case Hunter 12 / Acceptance Auditor 6 findings; ~19 unique actionable, all fixed): (1) HIGH data-island breakout via the HTML `<!--`+`<script` double-escape state → every `<` now emitted as the `<` JSON escape (round-trip-safe) + risky-ordering test; (2) slot literals inside tour content (this PR's own diff!) re-substituted by chained split/join → single-pass tokenized substitution; (3) raw-HTML injection through marked output into innerHTML → sanitizeHtml (script/iframe/on*/javascript: stripped) + esc'd `<br>` joins; (4) fetch-ok/rev-parse-fail misread as branch-absent could orphan-root away all published tours → hard error; (5) localized git broke branch-absent stderr match → LC_ALL=C on fetch; (6) j/k mid-scroll section skip → last-heading-past-reading-line pick; (7) localStorage `"null"` bricked boot; (8) malformed `%`-hash URIError; (9) case-insensitive `</ScRiPt` vendor escape; (10) numstat rename `{old => new}` normalized to new path; (11) CRLF/BOM specs lost frontmatter → normalized at read; (12) gather now reuses engine `findSpecForHashIn` (dup-logic); (13) ls-tree `-z` (quoted-path prune blindness); (14) NaN meta numbers rejected; (15) coerced-id duplicate message clarified; (16) `--tour-json` resolves vs cwd + `tour validate` needs no project config; (17) `tourSectionSkipped` comment/code aligned; (18) external-tracker token rewording in src comments (D-10 hygiene); (19) validator one-pass caveat documented. Documented-not-changed (accepted v1 bounds, in module headers): hold-check gh ~100-comment page cap + inline-review-thread gap + deleted-author skip; prune's dev-only branch prefix; narrow non-FF retry match. Re-ran: typecheck + full vitest + E-1 green.
- 2026-07-05T12:40 — coordinator slice: devx.md Phase 7.5 (fail-soft tour attach) + D-5 hold check in Phase 8 + Stage: Address wired; test/devx-skill-tour-discipline.test.ts pins all of it. S-2 proof run follows the commit (tour of this PR's own diff).
- 2026-07-05T13:05 — PR https://github.com/LeoTheMighty/devx/pull/65 merged (b50ae57); check-hold ran live ({hold:false} → silence merged, D-5 exercised); worktree removed; lock released. V2.3 closed.
