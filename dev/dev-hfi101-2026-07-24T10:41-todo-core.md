---
hash: hfi101
type: dev
created: 2026-07-24T10:41:50-06:00
title: Todo core — template, parser, scaffold, gate isolation
from: plan/plan-eac479-2026-07-24T09:57-harness-fold-in.md
plan: _devx/workstreams/harness-fold-in
status: in-progress
owner: /devx-loop-2026-07-24T16-46-18-001-62080
blocked_by: []
branch: feat/dev-hfi101
---

## Goal

Land the ground layer everything else reads: the shipped
`_devx/templates/engine/todo.md` template, the pure engine module
`src/lib/engine/todo.ts` (parse / focus walk / drift / derived-line
truing), scaffold wiring in `createWorkstream`, and the two P0 pins —
scaffold honors the parse contract (E-1) and gates are firewalled from
todo.md (E-2). Phase 1 of workstream `harness-fold-in` (plan.md § Phase 1).

## Acceptance criteria

- [ ] `_devx/templates/engine/todo.md`: header contract as HTML comment +
      the 11-line lifecycle skeleton from design §"todo.md parse contract"
      (`- [ ] Stage: PRD` … `- [ ] Stage: Outcome`, gate labels fixed set
      `prd | coverage(design) | coverage(plan) | evals`). Ships via the
      existing `package.json` `files:` entry — no packaging change.
- [ ] `src/lib/engine/todo.ts` (pure, no I/O): `parseTodo`, `currentFocus`
      (frontmatter-stage-rooted DFS walk — never rooted at checkbox state),
      `computeTodoDrift` (both classes: gate-flag + phase-pointer, either
      direction, with 1-indexed line numbers), `trueDerivedLines` (derived
      lines only; free items byte-preserved), plus the `TodoItem` /
      `TodoDoc` / `TodoGroundTruth` / `TodoDrift` interfaces from design
      §Interfaces. Top-level derived lines match
      `/^- \[( |x)\] (Stage|Gate|Phase \d+): .+$/`; deeper nesting is
      opaque free text `{checked, depth, text}` — never validated.
- [ ] `src/lib/engine/workstream.ts`: `todo.md` added to `createWorkstream`'s
      write-if-missing template loop.
- [ ] `test/workstream-todo-scaffold.test.ts` (E-1 permanent suite): fresh
      scaffold produces todo.md with 100% of skeleton items in template
      order; `parseTodo` extracts all lifecycle items with 0 unparsed
      top-level lines.
- [ ] `test/gate-todo-isolation.test.ts` (E-2 permanent suite): static
      read-surface scan — 0 todo references in `src/commands/gate.ts` +
      `src/lib/engine/gate-prd.ts` / `gate-coverage.ts` / `gate-evals.ts`;
      gate verdict byte-identical across 4 todo fixtures (present / absent /
      fully checked / fully unchecked).
- [ ] Workstream evals E-1 + E-2 flip GREEN:
      `npx tsx harness-fold-in/evals/E-1_todo-scaffold.ts` and
      `…/E-2_gate-todo-isolation.ts` (cwd `_devx/workstreams`) exit 0.
- [ ] Full suite green (`npm test`, typecheck included).

## Technical notes

- `currentFocus` starts from the frontmatter-derived current stage, never
  from checkbox state (E-5's stale-checkbox mitigation); absent file →
  `null`.
- `computeTodoDrift` / `currentFocus` land here (pure, fully unit-testable)
  even though their CLI rendering lands in hfi103 — the module is one
  cohesive concern; the wiring is another.
- E-2's byte-identity fixtures run against today's gates — the invariant is
  provable before hfi102 touches gate.ts, and the static scan keeps it
  pinned afterward.
- RED evidence: `_devx/workstreams/harness-fold-in/evals/RED-report.md`
  (E-1, E-2 right-reason).

## Status log

- 2026-07-24 — emitted by /devx-plan RED stage (eac479, phase 1/5).
- 2026-07-24T10:46:18-06:00 — claimed by /devx in session /devx-loop-2026-07-24T16-46-18-001-62080
- 2026-07-24T16:48:44.735Z — [FAIL] loop iteration 1: The previous iteration crashed with a mid-response server error before persisting any work — the worktree is clean, no commits exist beyond the claim, and no status-log entries or learnings were recorded, so this was a no-op iteration.
- 2026-07-24T17:03:02.446Z — loop iteration 2: Landed the todo.md template, full pure src/lib/engine/todo.ts module, createWorkstream scaffold wiring, and the E-1 permanent suite — eval E-1 flipped GREEN and the full 2136-test suite passes.
  - Change: _devx/templates/engine/todo.md shipped: header-contract HTML comment + the 11-line lifecycle skeleton honoring the parse-contract regex (E-1 template assertions pass)
  - Change: src/lib/engine/todo.ts pure module complete: parseTodo, currentFocus (frontmatter-rooted DFS), computeTodoDrift (gate-flag + phase-pointer, both directions, 1-indexed lines), trueDerivedLines (free items byte-preserved), plus the TodoItem/TodoDoc/TodoGroundTruth/TodoDrift interfaces
  - Change: createWorkstream now scaffolds todo.md via the write-if-missing template loop (created.todo flag + noop computation extended; additive, no existing test broken)
  - Change: test/workstream-todo-scaffold.test.ts (E-1 permanent suite, 5 tests) added; test/engine-templates.test.ts extended to enumerate todo.md as the tenth shipped template
  - Change: Verified: eval E-1 exits 0 GREEN; E-4/E-5 pure-function assertions all pass (their remaining failures are only the Phase-3 permanent suites owned by hfi103); npm test fully green (2136 tests, build + typecheck included)
  - Learning: E-2 is now a one-file iteration: the static scan of all 4 gate modules is already clean, so only test/gate-todo-isolation.test.ts (static scan + 4-fixture byte-identity gate runs) is needed to flip it GREEN
  - Learning: TodoItem needed an additive `text` field (raw text after the checkbox) beyond the design interface — `label` alone ('2' for a phase) cannot satisfy E-5's focus-output assertions like 'Phase 2: verdicts → def456'
  - Learning: Stage→focus-section mapping settled at implementation: intake→PRD, done→Retro, retired→null (no focus for abandoned workstreams); Retro/Outcome truing keys off status:done and isOutcomeVerdict(outcome.status) reused from outcome.ts
  - Learning: E-4's fixture passes a partial EngineState (only stage + gateStatus), so computeTodoDrift must access truth.state.gateStatus/truth.phaseDone with optional chaining — assuming a full EngineState at runtime would crash the eval
