---
hash: uwg104
type: dev
created: 2026-08-21T14:30:00-06:00
title: "Live overnight ride-through (HUMAN-GATED — discharges MANUAL MV2.1)"
from: plan/plan-c8e2d4-2026-07-14T10:41-usage-window-governor.md
plan: plan/plan-c8e2d4-2026-07-14T10:41-usage-window-governor.md
status: ready
blocked_by: [uwg103]
branch: feat/dev-uwg104
---

## Goal

E-7 / G-1: one real supervised overnight run that rides at least one real
usage-window reset and merges work on both sides of it.

**This phase cannot be completed by an agent.** It is scoped as its own
phase precisely so Phases 1-3 ship and go green without it — the pin105
shape, chosen deliberately after `pinret` recorded what a mixed
human/scriptable phase costs (its scriptable half shipped in July; the phase
has been `[-] blocked` ever since, and its `Blocked-by:` cannot even name
the real reason — see INTERVIEW Q#17).

## Acceptance criteria

- [ ] AC 1: A `MANUAL.md` row asking the owner to run the supervised night.
- [ ] AC 2: The run is executed per
      `_devx/workstreams/usage-window-governor/evals/E-7_live-night.md`
      (under `caffeinate -dimsu`).
- [ ] AC 3: The morning report's Usage-window pauses section, the raw
      `loop:usage-pause` events, and the PRs merged either side of the pause
      are pasted into that eval file. **Capture them whether the run passed
      or failed** — a failed ride tells us which marker shape we missed, and
      the raw text is the corpus.
- [ ] AC 4: Any real marker text not matched by the shipped
      `USAGE_LIMIT_MARKERS` becomes a widened regex plus a unit case built
      from the real string.
- [ ] AC 5: `MANUAL.md` MV2.1 discharged.

## Technical notes

- `USAGE_LIMIT_MARKERS` is seeded from known shapes, not captured
  transcripts. This phase is the only thing that closes that gap.

## Status log

- 2026-08-21T14:30 — emitted by /devx-plan (all four gates passed: prd PASS,
  design CONCERNS, plan CONCERNS, evals CONCERNS; both CONCERNS verdicts are
  the human-gated E-7 and the parked FR-8 spike, recorded honestly rather
  than waived).

## Links

- Workstream: `_devx/workstreams/usage-window-governor/`
- Design decision: `decisions/D-UW1-pre-ladder-interception.md`
- RED artifact: `test/loop-usage-window.test.ts`
