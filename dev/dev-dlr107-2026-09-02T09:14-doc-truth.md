---
hash: dlr107
type: dev
created: 2026-09-02T09:14:00-06:00
title: "Doc truth"
status: in-progress
from: plan/plan-a494be-2026-09-01T14:31-docs-layout-resolution.md
plan: _devx/workstreams/docs-layout-resolution
phase: 7
blocked_by: [dlr105]
branch: feat/dev-dlr107
owner: /devx-2026-09-02T1454-70005
---
## Goal

Closes G-4. Both surfaces that describe layout resolution describe it
wrongly today, and a reader consults them BEFORE choosing a layout — so this
is not cleanup, it is the difference between the feature being discoverable
and being a trap. The claims become test-asserted rather than prose-reviewed.
Plan phase 7 of workstream docs-layout-resolution.

## Acceptance criteria

- [ ] AC 1 (RED-BEARING): `docs/CONFIG.md` section 15's artifact table is
      RESTRUCTURED to one row per `ArtifactKind` — 13 rows, of which
      `checkpoints/` and `RESULTS.md` are genuinely new and several others are
      SPLITS of today's 12 rows (design's human-only outline and its critique
      share one row, plan's share another, there are no `design-human` /
      `plan-human` rows, and `RED-report.md` has no row at all). FR-8's "gains
      its two missing rows" understates this; asserting set-equality while
      describing it as a two-row addition would ship a test that cannot pass.
      Adding a variant to `ArtifactKind` without a row fails the test.
- [ ] AC 2: The test does NOT diff prose. It asserts three checkable properties:
      the table has a row for every `ArtifactKind` the resolver handles (table
      parsed, union enumerated, sets must match); rule 5 and the schema
      description contain no claim about a surface that resolves layout-blind
      (an allowlist of surfaces that DO resolve through the layout,
      cross-checked against the resolver's real callers); and both documents'
      enum matches `DOCS_LAYOUTS`. Same "structure, not wording" posture as
      the Reading Guide check.
- [ ] AC 3: Section 15 gains the `devx layout migrate` invocation as the answer
      to "what does switching cost" — a question it currently raises and
      leaves hanging. `RETRO-<date>.md` is recorded as deliberately
      layout-independent (a real workstream artifact with no `ArtifactKind`
      and no table row), not omitted.
- [ ] AC 4: `_devx/config-schema.json`'s `docs_layout` property description is
      rewritten — it restates rule 5 verbatim and is the claim a reader hits
      via editor autocomplete. The enum stays `["workstream","project-level"]`
      and there is NO schema version bump: the value space is unchanged.
- [ ] AC 5: `evals/E-8_docs-truth.ts` flips GREEN and
      `test/engine-layout-docs-truth.test.ts` exists and passes. NOTE which
      half carries the RED: by the time this phase runs, phases 2 and 4 have
      implemented rule 5, so the claim-check already passes — the 13-row
      SET-EQUALITY is what fails RED.
- [ ] AC 6: A follow-up spec is filed (`dev/dev-<new-hash>-*.md` + `DEV.md`) for
      the nine skill-body path references that hardcode the folder shape
      (`.claude/commands/devx-plan.md:12,113,129,135,165,223`;
      `.claude/commands/devx.md:189,203,698`), sized against the S-1 prose
      budget. Under `project-level` an agent following those skills writes
      into the folder shape while the CLI reads the flat one — the same
      reader/writer split this workstream exists to kill, one layer out. It is
      out of scope HERE because the 60KB CI-gated budget has single-digit
      bytes of headroom, so rewording nine references is its own sized piece
      of work.
- [ ] AC 7: `devx graph --check` is green; `npm test` green. (The S-1
      prose-budget canary measures `_devx/templates/engine/**` plus
      `.claude/commands/devx-plan.md` — neither of which this phase edits — so
      citing it as a criterion here would be one a broken phase satisfies
      trivially. It is checked in AC 6's follow-up, which genuinely touches
      skill prose.)

## Technical notes

Plan: `plan/agent.md` section "7. Phase".

The table is RESTRUCTURED, not merely extended, and the PRD understates this.
The real table today has 12 rows in a different shape; the target is one row
per `ArtifactKind` in the map's own shape.

Sequenced after phase 6 so the docs describe a command that exists (R-10).
Parallel-safe with phase 6 in file terms, ordered after it in prose terms.

Revert-safe (revert the PR).

## Status log

- 2026-09-02T09:14 — emitted by /devx-plan (RED gate PASS; workstream
  docs-layout-resolution, plan phase 7).
- 2026-09-02T14:54:23-06:00 — claimed by /devx in session /devx-2026-09-02T1454-70005
