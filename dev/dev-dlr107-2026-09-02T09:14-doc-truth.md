---
hash: dlr107
type: dev
created: 2026-09-02T09:14:00-06:00
title: "Doc truth"
status: in-progress
from: plan/plan-a494be-2026-09-01T14:31-docs-layout-resolution.md
plan: _devx/workstreams/docs-layout-resolution
phase: 7
blocked_by: [dlr105]
spawned: [a57f22]
branch: feat/dev-dlr107
owner: /devx-2026-09-02T1454-70005
---
## Goal

Closes G-4. Both surfaces that describe layout resolution describe it
wrongly today, and a reader consults them BEFORE choosing a layout — so this
is not cleanup, it is the difference between the feature being discoverable
and being a trap. The claims become test-asserted rather than prose-reviewed.
Plan phase 7 of workstream docs-layout-resolution.

## Acceptance criteria

- [ ] AC 1 (RED-BEARING): `docs/CONFIG.md` section 15's artifact table is
      RESTRUCTURED to one row per `ArtifactKind` — 13 rows, of which
      `checkpoints/` and `RESULTS.md` are genuinely new and several others are
      SPLITS of today's 12 rows (design's human-only outline and its critique
      share one row, plan's share another, there are no `design-human` /
      `plan-human` rows, and `RED-report.md` has no row at all). FR-8's "gains
      its two missing rows" understates this; asserting set-equality while
      describing it as a two-row addition would ship a test that cannot pass.
      Adding a variant to `ArtifactKind` without a row fails the test.
- [ ] AC 2: The test does NOT diff prose. It asserts three checkable properties:
      the table has a row for every `ArtifactKind` the resolver handles (table
      parsed, union enumerated, sets must match); rule 5 and the schema
      description contain no claim about a surface that resolves layout-blind
      (an allowlist of surfaces that DO resolve through the layout,
      cross-checked against the resolver's real callers); and both documents'
      enum matches `DOCS_LAYOUTS`. Same "structure, not wording" posture as
      the Reading Guide check.
- [ ] AC 3: Section 15 gains the `devx layout migrate` invocation as the answer
      to "what does switching cost" — a question it currently raises and
      leaves hanging. `RETRO-<date>.md` is recorded as deliberately
      layout-independent (a real workstream artifact with no `ArtifactKind`
      and no table row), not omitted.
- [ ] AC 4: `_devx/config-schema.json`'s `docs_layout` property description is
      rewritten — it restates rule 5 verbatim and is the claim a reader hits
      via editor autocomplete. The enum stays `["workstream","project-level"]`
      and there is NO schema version bump: the value space is unchanged.
- [ ] AC 5: `evals/E-8_docs-truth.ts` flips GREEN and
      `test/engine-layout-docs-truth.test.ts` exists and passes. NOTE which
      half carries the RED: by the time this phase runs, phases 2 and 4 have
      implemented rule 5, so the claim-check already passes — the 13-row
      SET-EQUALITY is what fails RED.
- [ ] AC 6: A follow-up spec is filed (`dev/dev-<new-hash>-*.md` + `DEV.md`) for
      the nine skill-body path references that hardcode the folder shape
      (`.claude/commands/devx-plan.md:12,113,129,135,165,223`;
      `.claude/commands/devx.md:189,203,698`), sized against the S-1 prose
      budget. Under `project-level` an agent following those skills writes
      into the folder shape while the CLI reads the flat one — the same
      reader/writer split this workstream exists to kill, one layer out. It is
      out of scope HERE because the 60KB CI-gated budget has single-digit
      bytes of headroom, so rewording nine references is its own sized piece
      of work.
- [ ] AC 7: `devx graph --check` is green; `npm test` green. (The S-1
      prose-budget canary measures `_devx/templates/engine/**` plus
      `.claude/commands/devx-plan.md` — neither of which this phase edits — so
      citing it as a criterion here would be one a broken phase satisfies
      trivially. It is checked in AC 6's follow-up, which genuinely touches
      skill prose.)

## Technical notes

Plan: `plan/agent.md` section "7. Phase".

The table is RESTRUCTURED, not merely extended, and the PRD understates this.
The real table today has 12 rows in a different shape; the target is one row
per `ArtifactKind` in the map's own shape.

Sequenced after phase 6 so the docs describe a command that exists (R-10).
Parallel-safe with phase 6 in file terms, ordered after it in prose terms.

Revert-safe (revert the PR).

## Status log

- 2026-09-02T09:14 — emitted by /devx-plan (RED gate PASS; workstream
  docs-layout-resolution, plan phase 7).
- 2026-09-02T14:54:23-06:00 — claimed by /devx in session /devx-2026-09-02T1454-70005
- 2026-09-02T15:05 — phase 2: spec ACs direct (v2 native); 7 ACs;
  workstream=docs-layout-resolution; red-artifacts=evals/E-8_docs-truth.ts
  (re-run RED now: 12 rows vs the resolver's kinds, no `checkpoints` /
  `RESULTS.md` row, companion test absent — the RED names the missing
  feature, not harness breakage). Confirmed the plan's call on which half
  carries the RED: the layout-blind claim-check and the enum check both
  already pass.
- 2026-09-02T15:20 — phase 3: `ARTIFACT_KINDS` runtime row index added to
  `artifacts.ts` (the type alias erases to 5 tagged matches under E-8's AST
  fallback, so the 13-row set-equality is unassertable without it — the eval's
  own docstring names the runtime export as preferred); §15's table
  restructured to 13 kind-keyed rows + the `devx layout migrate` invocation +
  `RETRO-<date>.md` recorded as deliberately layout-independent; schema
  description rewritten (rule 5 verbatim, rule 4's unenforced caveat added,
  enum untouched, no version bump); `test/engine-layout-docs-truth.test.ts`
  (12 tests) pins the row set AND the path cells against `artifactRel()` —
  strictly more than E-8, which compares counts. E-8 GREEN. Follow-up
  `dev-a57f22` filed for the nine skill-body path references (AC 6).
- 2026-09-02T15:35 — phase 4: sequential multi-lens adversarial review (Blind
  Hunter -> Edge Case Hunter -> Acceptance Auditor, one pass each). The
  3-agent PARALLEL shape was unavailable: this session's policy forbids
  spawning subagents, so per /devx Phase 4 step 2b the sanctioned
  compensation ran instead — and an empirical leg alongside it, six negative
  controls against the real repo (lying path cell, deleted row, new
  `ArtifactKind` variant, truncated yaml enum, blind spelling in a comment vs
  in code) proving every new assertion can fail and that `codeOnly` is live.
  10 findings (2 HIGH, 4 MED, 4 LOW); ALL fixed in-place. Most load-bearing:
  the companion carried only ONE of AC 2's three properties — the
  layout-blind CLAIM check lived solely in E-8, and `devx.config.yaml` says
  workstream evals are "never part of `npm test`", so once the RED gate is
  behind us rule 5's claim would have been gated by nothing; it now runs in
  the suite. Close second: §15's own arithmetic (22 identities -> 13 rows)
  was unchecked prose and would rot on the next stage addition — the exact
  drift class this phase exists to close. Also fixed: three §15 claims
  scanned the whole file rather than the section (would pass on a §15 that
  says nothing); collection-time `expect()` in the table parser and the
  schema-node IIFE (a moved heading aborted the module instead of failing a
  test with the reason); an unguarded row token fell off `artifactRel`'s
  switch and compared against `…/undefined`; CONFIG.md's own
  `# workstream | project-level` enum comment was unchecked; a THIRD copy of
  `codeOnly` would have shipped (extracted to `test/helpers/code-only.ts`,
  the two byte-identical copies in dlr101's and dlr105's tests now import
  it); and the `RETRO-<date>.md` note misstated dlr106's finding as "six
  retro files" (it was six doc-set files across six workstreams). Separately,
  AC 6's premise did not survive measurement — the S-1 canary has 6,102 bytes
  of headroom, not "single-digit"; the follow-up spec records the measurement
  rather than inheriting the claim. Re-review clean.
- 2026-09-02T15:50 — phase 5: local gate green in the worktree (`npm test`
  4,172 passed / 0 failed across 139 + 38 files, REAL_EXIT=0 read from the
  log, not the exit code; +15 over dlr106's 4,157 — exactly this phase's new
  file). `npm run typecheck` clean. E-8 GREEN. Eval-lock check: E-8's step
  body is untouched — the RED was answered by restructuring the docs and
  adding the runtime row index, never by softening the eval. QA walkthrough
  emitted at `test/test-ac0751-2026-09-02T15:45-dlr107-qa-walkthrough.md`
  (3 machine checks executed with real output pasted, 2 human outstanding:
  the rendered table and the editor tooltip — both genuinely need eyes).
  Plan trued as-built in the same commit: phase 7 checkbox, two unplanned
  files stated with why, and T7.4's budget premise corrected.
- 2026-09-02T15:58 — phase 7: opened
  https://github.com/LeoTheMighty/devx/pull/157 (body rendered by `devx
  pr-body`; no unresolved placeholders). Written and pushed as a second
  commit on the branch BEFORE the merge, so the line rides the PR rather
  than being ported afterwards — dlr106's phase 5/7 lines missed their PR
  exactly this way (see commit 7c4600f).

