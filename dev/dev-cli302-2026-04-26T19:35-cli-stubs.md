---
hash: cli302
type: dev
created: 2026-04-26T19:35:00-07:00
title: Stub helper + 10 stub commands registered
from: _bmad-output/planning-artifacts/epic-cli-skeleton.md
plan: plan/plan-a01000-2026-04-26T19:30-foundation.md
status: ready
blocked_by: [cli301]
branch: develop/dev-cli302
---

## Goal

Implement `src/lib/stub.ts` with a `makeStub(phase, epic)` helper. Use it to register 10 stub commands (`ui`, `serve`, `tail`, `kill`, `restart`, `status`, `pause`, `resume`, `ask`, `eject`) — each in `src/commands/<name>.ts`. Stub message format: `not yet wired — ships in Phase <N> (epic-<slug>)`. Print to **stderr**, exit 0.

## Acceptance criteria

- [ ] `src/lib/stub.ts` exports `makeStub(phase: number, epic: string): CommandHandler`
- [ ] All 10 stub command files present at `src/commands/<name>.ts`, each calling `makeStub` with correct phase + epic-slug
- [ ] Phase + epic mapping (from `docs/ROADMAP.md`):
  - `ui` → Phase 4 epic-devx-ui-tui
  - `serve` → Phase 4 epic-devx-serve-web
  - `tail` → Phase 4 epic-devx-ui-tui
  - `kill` → Phase 2 epic-devx-concierge-skill (operator primitive)
  - `restart` → Phase 2 epic-devx-concierge-skill
  - `status` → Phase 2 epic-devx-concierge-skill
  - `pause` → Phase 2 epic-devx-manage-minimal
  - `resume` → Phase 2 epic-devx-manage-minimal
  - `ask` → Phase 2 epic-devx-concierge-skill
  - `eject` → Phase 10 epic-eject-cli
- [ ] `devx ui` stderr matches `not yet wired — ships in Phase 4 (epic-devx-ui-tui)` exactly; exit 0; stdout empty
- [ ] **`devx eject` is verified to do NOTHING destructive**: dedicated test runs against fixture repo and asserts no files modified, no `.devx-cache/` removed, no commands run
- [ ] Vitest covers exit code + message format for each stub

## Technical notes

- All stubs share the same `makeStub` body; per-command file is just `export default makeStub(N, "epic-slug")`.
- The eject test is non-negotiable — Leonid's signature red flag #1 is destructive surprise.

## Status log

- 2026-04-26T19:35 — created by /devx-plan
