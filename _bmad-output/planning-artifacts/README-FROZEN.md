# FROZEN — read-only history (2026-07-05)

`_bmad-output/` is the BMAD-era artifact archive (Phase 0 + Phase 1,
2026-04 → 2026-07). Nothing writes here after `mgrret` (the final
`bmad-retrospective` run). Links in shipped specs keep resolving; do not
move, rewrite, or delete these files.

The v2 native engine's artifacts live in `_devx/workstreams/`. See
`v2/01-bmad-capture.md` for the migration record and `v2/02-engine.md` for
the replacement artifact shapes.
